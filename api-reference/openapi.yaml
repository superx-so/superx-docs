openapi: 3.1.0
info:
  title: SuperX API
  version: "1.0.0"
  description: |
    The SuperX public API: your Twitter/X content, analytics, audience and
    scheduling data over REST.

    All endpoints require an API key (`Authorization: Bearer sxk_...`) except
    `GET /v1/docs`. Keys are created in the SuperX app under Account > API / MCP / CLI
    and are server-side secrets.

    Timestamps are UTC ISO-8601 in both directions; inputs must carry an explicit
    `Z` or numeric offset. Every authenticated response carries
    `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset` headers.
  contact:
    name: SuperX
    url: https://superx.so
servers:
  - url: https://api.superx.so
security:
  - apiKey: []
tags:
  - name: Identity
  - name: Content
  - name: Analytics
  - name: Inspiration
  - name: Audience
  - name: Contact Lists
  - name: Signals
  - name: Datasets
    description: |
      The audience collections Ask SuperX builds in the app: the repliers,
      quoters or reposters of a post, the members of an X list, your own
      posts or replies, or a research brief.

      Collect a new one with `POST /v1/datasets` (a big collection answers
      202 and finishes in the background), read it, export it, or copy the
      people in it into a contact list you created. Datasets are kept for 30
      days, and the ones Ask SuperX builds in the app show up here too.
  - name: Engage
    description: |
      The account's saved Engage feeds and the candidate posts in them, for
      an agent to score and a person to act on.

      Read-only by design. These endpoints return feed candidates for a
      person to review; replies are written and sent by a person in the
      SuperX app, which is why there is no reply endpoint here. Sending
      spammy, automated, or AI-generated replies that read as inauthentic
      may get your X account suspended under X's inauthentic-behavior
      rules and your SuperX account terminated. AI suggestions must be
      reviewed and meaningfully edited before they are sent, and you are
      solely responsible for what you post. Reply activity is logged and
      may be audited.
  - name: X Lookups
    description: |
      Read any public post, its top replies, any public profile, or an
      account's latest posts LIVE from X, rather than from SuperX's stored
      data.

      These are owner-scoped, so none of them take an `account_id`: nothing
      about a public lookup is per-X-account. They cost the tighter
      live-enrichment allowance and share a daily allowance of 300 live
      lookups with Ask SuperX inside the app, so use them for what you
      actually need rather than to sweep. Repeat lookups within 15 minutes
      may be served from a short-lived server-side cache.
  - name: Tools
    description: |
      The composer helpers from the SuperX app, over the API: rewrite a post,
      edit one selected piece of it, apply one preset rewrite, check a claim,
      or compare two versions.

      Every one of them returns TEXT and writes nothing. Nothing here is
      posted, scheduled or sent; save what you keep with
      `POST /v1/posts/draft` or `POST /v1/scheduled-posts`. They cost AI
      credits, measured from the models' real cost, and spend no live-data
      allowance.
  - name: Media
  - name: Workers
    description: |
      The posts the account's Workers have written, and the three things a
      person does with one: save it as a draft, schedule it, or dismiss it.

      A Worker is a scheduled AI writer set up in the SuperX app. Creating,
      editing and running one stays in the app, so there is no create or run
      endpoint here. What a run produces lands in the app's "To review" queue
      and is read with `GET /v1/workers/suggestions`.

      NOTHING HERE POSTS TO X. Drafting or scheduling a suggestion writes a
      post the user can see in the app and on `GET /v1/scheduled-posts`,
      exactly like the app's own buttons. AI output must be reviewed and
      meaningfully edited by a person before it goes out, so show the text to
      the user and confirm the time before you schedule anything.
  - name: Scheduling
  - name: Tags
  - name: Context
  - name: Queue
  - name: DMs
    description: |
      Queue direct messages into the account's own DM pipeline, and read or
      cancel what is waiting.

      NOTHING HERE SENDS A MESSAGE. `POST /v1/dm/campaigns` enqueues; the
      SuperX app's scheduler is what sends, within the account's daily and
      monthly DM limits, so the create response is counts, not deliveries.
      Unsent messages can be cancelled until the scheduler picks them up.

      You are responsible for the messages you queue. Unsolicited or
      automated bulk DMs may get an X account suspended under X's rules and
      a SuperX account terminated, so confirm the recipient list and the
      exact wording with the person on whose behalf you are queueing.
  - name: Articles
  - name: Meta

paths:
  /v1/me:
    get:
      tags: [Identity]
      operationId: getMe
      summary: Get the key owner
      description: Returns the key owner, their plan, the calling key's name and scopes, and the AI credit pool.
      responses:
        "200":
          description: The authenticated identity.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Me"
              example:
                data:
                  owner:
                    id: "1178367350552305665"
                    name: "Jordan Rivers"
                    email: "jordan@example.com"
                  plan:
                    tier: 2
                    label: "Pro"
                    is_trial: false
                  key:
                    id: 3
                    name: "My script"
                    key_prefix: "sxk_Ab3dEf9h"
                    scopes: ["read", "write"]
                    created_at: "2026-07-01T09:15:00.000Z"
                    last_used_at: "2026-07-06T08:41:03.000Z"
                  credits:
                    remaining: 712
                    pool: 750
                    period: "month"
                    period_end: "2026-10-01T00:00:00.000Z"
                    bonus: 0
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }

  /v1/accounts:
    get:
      tags: [Identity]
      operationId: listAccounts
      summary: List your accounts
      description: |
        The accounts this key may act on: your main account plus any linked
        accounts. When linked accounts cannot be verified, the response degrades
        to the main account only and carries a top-level `degraded: true` flag;
        retry shortly for the full list.
      responses:
        "200":
          description: Main and linked accounts.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Account" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
                  degraded:
                    type: boolean
                    description: Present and true only when linked accounts could not be verified and the list contains the main account only.
              example:
                data:
                  - id: "1178367350552305665"
                    username: "mainhandle"
                    name: "Main Account"
                    avatar_url: "https://pbs.twimg.com/profile_images/example.jpg"
                    is_main: true
                  - id: "1299139023923867648"
                    username: "brandhandle"
                    name: "Brand Account"
                    avatar_url: "https://pbs.twimg.com/profile_images/example2.jpg"
                    is_main: false
                pagination: { page: 1, limit: 2, has_more: false }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/posts:
    get:
      tags: [Content]
      operationId: listPosts
      summary: List published posts
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: type
          in: query
          description: Which posts to include.
          schema:
            type: string
            enum: [posts, replies, all]
            default: posts
        - $ref: "#/components/parameters/Since"
        - $ref: "#/components/parameters/Until"
        - name: sort
          in: query
          schema:
            type: string
            enum: [posted_at, likes, impressions]
            default: posted_at
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of posts, newest first by default.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Post" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "1941123456789012345"
                    text: "Consistency beats intensity. Post every day for 30 days and watch what happens."
                    posted_at: "2026-07-05T14:02:11.000Z"
                    url: "https://x.com/mainhandle/status/1941123456789012345"
                    is_reply: false
                    in_reply_to_id: null
                    is_quote: false
                    thread_id: "1941123456789012345"
                    metrics:
                      likes: 231
                      replies: 18
                      reposts: 40
                      quotes: 3
                      bookmarks: 55
                      impressions: 48210
                pagination: { page: 1, limit: 20, has_more: true }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/posts/analytics:
    get:
      tags: [Analytics]
      operationId: getPostAnalytics
      summary: Get analytics
      description: |
        Totals and a per-day series (posts, engagement, impressions, follower
        samples) for the selected account. Default range is the last 30 days;
        the maximum range is 366 days.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - $ref: "#/components/parameters/Since"
        - $ref: "#/components/parameters/Until"
      responses:
        "200":
          description: Totals, follower change and per-day series.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Analytics"
              example:
                data:
                  range:
                    since: "2026-06-06T00:00:00.000Z"
                    until: "2026-07-06T00:00:00.000Z"
                  totals:
                    posts: 92
                    likes: 4110
                    replies: 380
                    reposts: 512
                    quotes: 44
                    bookmarks: 903
                    impressions: 812400
                  followers:
                    start: 12180
                    end: 12544
                    change: 364
                  series:
                    - date: "2026-07-04"
                      posts: 3
                      likes: 140
                      replies: 12
                      reposts: 17
                      quotes: 1
                      bookmarks: 31
                      impressions: 27400
                      followers: 12520
                    - date: "2026-07-05"
                      posts: 4
                      likes: 231
                      replies: 18
                      reposts: 40
                      quotes: 3
                      bookmarks: 55
                      impressions: 48210
                      followers: 12544
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/posts/draft:
    post:
      tags: [Scheduling]
      operationId: draftPost
      summary: Write post drafts in your voice
      description: |
        Writes post drafts from a brief, in the account's own voice. Nothing
        is scheduled and nothing is stored: the response is text for you to
        review and edit, then send to `POST /v1/scheduled-posts` to save a
        draft or schedule it.

        Every draft copies the SHAPE of a proven post, never its content.
        Pass one in `mirror` (the text of a post from `GET /v1/inspiration`
        works well), or leave `mirror` out and a proven shape is picked for
        you, optionally biased toward a `collection`.

        Voice: `mine` (default) writes as the account in `account_id`, from
        that account's own posts, style guide and rules (your main account
        when `account_id` is omitted), `creator` writes in a creator's style,
        and `hybrid` keeps the account's substance with the creator's flavor.
        The creator's style guide is prepared in the
        background the first time you name them; that first call returns
        `409 creator_style_pending` and the retry a minute later succeeds.

        Costs AI credits per draft (drafts that fail are refunded), and a
        call takes roughly 20 to 40 seconds. `Idempotency-Key` is NOT
        supported: generation is not idempotent, and a replay would produce
        different text anyway.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [brief]
              properties:
                brief:
                  type: string
                  minLength: 1
                  maxLength: 2000
                  description: "What the post should say: the data, angle, or notes to write from."
                count:
                  type: integer
                  minimum: 1
                  maximum: 3
                  default: 1
                  description: How many drafts to write. Each one costs credits.
                voice:
                  type: string
                  enum: [mine, creator, hybrid]
                  default: mine
                  description: "Whose voice to write in. `mine` is the voice of the account in `account_id`: its own posts, style guide and rules. Omit `account_id` and that is your main account."
                creator:
                  type: string
                  description: X handle to borrow style from, for example `@naval`. Required when `voice` is `creator` or `hybrid`, rejected when `voice` is `mine`.
                mirror:
                  type: string
                  minLength: 50
                  maxLength: 1500
                  description: "Plain text of a proven post whose shape to copy. Only the form is reused, never the content. Pick a mirror with room for your data: a two-line aphorism squeezes the facts out. Omit to have a shape picked for you."
                collection:
                  type: string
                  description: "Format collection id to bias the picked shape toward, for example `numbered_list` or `contrarian_take`. Used only when `mirror` is omitted; an id that is unknown or not usable as a mirror fails with `400 invalid_collection`, which lists the ids that do work."
                instructions:
                  type: string
                  maxLength: 500
                  description: Extra style instructions for this batch.
                account_id:
                  type: string
                  description: "Any account you own, meaning your main account (the default when omitted) or one linked to it. With `voice: mine` the drafts use this account's own posts and style guide. An account shared with you returns `403 writes_main_account_only`."
            examples:
              simple:
                summary: One draft in your own voice
                value:
                  brief: "We cut churn from 6.2% to 3.8% by replacing the onboarding video with a 3-step checklist."
              mirrored:
                summary: Three drafts copying a proven post's shape
                value:
                  brief: "We shipped a public API. 40 developers signed up in 48 hours."
                  count: 3
                  mirror: "Most people think they need more discipline.\n\nThey do not.\n\nThey need a smaller first step. Discipline is what you build after the habit already exists, not before."
              creator_voice:
                summary: Your substance, a creator's flavor
                value:
                  brief: "Removing our free plan tripled conversion."
                  voice: "hybrid"
                  creator: "@naval"
      responses:
        "201":
          description: The generated drafts. Nothing was scheduled or stored.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      drafts:
                        type: array
                        items:
                          type: object
                          properties:
                            text:
                              type: string
                              description: The draft post text. Send it to `POST /v1/scheduled-posts` when the user is happy with it.
                            mirrored:
                              type: object
                              nullable: true
                              description: Which proven shape this draft copied, or `null` when the draft copied no shape (no usable one was found and it was written from the brief alone).
                              properties:
                                source:
                                  type: string
                                  enum: [you, collection]
                                  description: "`you` when you passed `mirror`, `collection` when a shape was picked for you."
                                collection:
                                  type: string
                                  description: Format collection the shape came from. Absent when `source` is `you`.
                                reference_id:
                                  type: string
                                  description: Id of the post whose shape was copied. Absent when `source` is `you`.
                      credits_charged:
                        type: integer
                        description: AI credits kept for this call. Drafts that failed are refunded and are not counted here.
              example:
                data:
                  drafts:
                    - text: "Most founders think they need better retention strategies.\n\nThey do not.\n\nThey need simpler onboarding."
                      mirrored: { source: "you" }
                  credits_charged: 3
        "400":
          description: >-
            Invalid or missing field (`invalid_parameter`), a `mirror` that
            cannot be used as a shape (`mirror_rejected`, with the reason),
            an unknown or unusable `collection` (`invalid_collection`), a
            `creator` SuperX has no posts for (`unknown_creator`), or a
            creator whose style guide could not be built at all
            (`creator_style_unavailable`, terminal: do not retry).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: mirror_rejected
                  message: "That post cannot be used as a mirror (url). Pass the plain text of a substantial post with no links and no engagement bait."
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >-
            Read-only key (`insufficient_scope`), an account shared with you
            (`writes_main_account_only`), or a lapsed subscription (legacy
            string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: writes_main_account_only
                  message: "Accounts shared with you are read-only through the API. Drafting works on your own main and linked accounts."
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "409":
          description: >-
            The creator's style guide is being prepared
            (`creator_style_pending`). It is ready in about a minute; retry
            the same request.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: creator_style_pending
                  message: "Style guide for @naval is being prepared, retry in about a minute."
        "429":
          description: >-
            Rate limited (`rate_limited`, honor `Retry-After`), the AI credit
            pool is used up (`ai_credits_exhausted`, with the credit
            counters), or the plan's daily cap on drafting runs is spent
            (`ai_action_limited`, honor `Retry-After`). No plan caps drafting
            today; the shape is documented so a future cap cannot surprise a
            client.
          headers:
            Retry-After:
              description: >-
                Seconds to wait before retrying. Sent on every flavour of this
                429.
              schema:
                type: integer
            X-Credits-Remaining:
              description: >-
                Credits left in the pool. Sent only on
                `ai_credits_exhausted`, never on `rate_limited` or
                `ai_action_limited`, which do not consult the pool.
              schema:
                type: integer
            X-Credits-Reset:
              description: >-
                Unix timestamp (seconds) when the pool refills. Sent only on
                `ai_credits_exhausted`, on the same terms as
                `X-Credits-Remaining`.
              schema:
                type: integer
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/CreditExhaustedError"
                  - $ref: "#/components/schemas/ActionLimitedError"
                  - $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error:
                  code: ai_credits_exhausted
                  message: "Not enough AI credits. Resets in 5 hours."
                  credits_required: 9
                  credits_remaining: 2
                  reset_at: 1790812800
        "500": { $ref: "#/components/responses/InternalError" }
        "502":
          description: Every draft failed to generate (`generation_failed`). All credits for the call were refunded.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: generation_failed
                  message: "Drafting failed and no credits were spent. Try again shortly."

  /v1/posts/remix:
    post:
      tags: [Scheduling]
      operationId: remixPost
      summary: Rewrite a post in your voice
      description: |
        Rewrites a post in the account's own voice, as close to or as far from
        the original as you ask. `closeness: 0` keeps only the idea,
        `closeness: 100` stays very close to the original wording.

        This is the same engine as the app composer's Remix button, so a
        script and the app produce the same kind of output.

        Nothing is posted, scheduled or stored: the response is TEXT for a
        person to review, then send to `POST /v1/posts/draft` or
        `POST /v1/scheduled-posts`.

        Costs AI credits, measured from the models' real cost and typically 2.
        Spends no live-data allowance. Needs a key with the `write` scope.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RemixPostRequest" }
            example:
              text: "Most founders think they have a traffic problem. They have a positioning problem."
              closeness: 70
              instructions: "Make it a question."
      responses:
        "200":
          description: The remixed text.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      text:
                        type: string
                        description: The remixed post. Nothing was saved; send it on when a person is happy with it.
                  meta: { $ref: "#/components/schemas/ToolMeta" }
              example:
                data:
                  text: "what if it was never a traffic problem?"
                meta:
                  credits_charged: 2
                  account_id: "1178367350552305665"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/tools/inline-edit:
    post:
      tags: [Tools]
      operationId: inlineEditText
      summary: Edit one selected piece of a post
      description: |
        Edits the piece of a post you select, keeping the style of the text
        around it. Pass the selection as `text` and the whole post as
        `full_text`, and the edit is written to drop straight back into place.

        Steer it with a free-text `instruction`, a preset `edit_type`, or
        both. At least one of the two is required.

        Returns TEXT. Nothing is posted, scheduled or saved.

        Costs AI credits, measured and typically 1. Spends no live-data
        allowance. Needs a key with the `write` scope.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InlineEditRequest" }
            example:
              text: "Most founders think they have a traffic problem."
              full_text: "Most founders think they have a traffic problem. They have a positioning problem."
              edit_type: hook
      responses:
        "200":
          description: The edited text.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      edited_text:
                        type: string
                        description: The edited selection, ready to drop back into the post.
                      original_text:
                        type: string
                        description: The selection you sent, echoed back so a diff is easy.
                  meta: { $ref: "#/components/schemas/ToolMeta" }
              example:
                data:
                  edited_text: "you don't have a traffic problem."
                  original_text: "Most founders think they have a traffic problem."
                meta:
                  credits_charged: 1
                  account_id: "1178367350552305665"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/tools/rephrase:
    post:
      tags: [Tools]
      operationId: rephraseText
      summary: Rewrite a post one preset way
      description: |
        Applies one preset rewrite to a whole post.

        The style presets (`improve`, `hook`, `engaging`, `humorous`,
        `positive`, `creative`, `sarcastic`, `inspirational`) write in the
        account's own voice. The mechanical ones (`grammar`, `translate`,
        `clarity`, `details`, `concise`) do not: they leave the voice alone
        and only do the job named.

        `hook` rewrites the FIRST paragraph only and returns it with the rest
        of the post unchanged behind it.

        Returns TEXT. Nothing is posted, scheduled or saved.

        Costs AI credits, measured and typically 1. Spends no live-data
        allowance. Needs a key with the `write` scope.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RephraseRequest" }
            example:
              type: concise
              text: "I have been thinking for quite a while now about the fact that most of the founders I speak to are convinced their biggest problem is traffic."
      responses:
        "200":
          description: The rewritten text.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      text: { type: string }
                  meta: { $ref: "#/components/schemas/ToolMeta" }
              example:
                data:
                  text: "most founders are sure their problem is traffic. it rarely is."
                meta:
                  credits_charged: 1
                  account_id: "1178367350552305665"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/tools/factcheck:
    post:
      tags: [Tools]
      operationId: factCheckText
      summary: Check a statement against a web search
      description: |
        Runs a web search for the statement and reports `true`, `false` or
        `unknown` with a one-sentence comment and the sources it read.

        **This is a model's reading of a couple of search results, not a
        guarantee.** Show the `sources` alongside the verdict and never
        present it as settled fact. `unknown` is a real and common answer,
        including when the search returned nothing usable.

        Nothing is posted, and nothing is written onto a post: the app's own
        fact-check button is what stores a verdict against a post.

        Costs AI credits, measured and typically 1. Spends no live-data
        allowance. Needs a key with the `write` scope.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FactCheckRequest" }
            example:
              text: "X passed 600 million daily active users in 2026."
      responses:
        "200":
          description: The verdict and its sources.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/FactCheckResult" }
                  meta: { $ref: "#/components/schemas/ToolMeta" }
              example:
                data:
                  result: unknown
                  comment: "The sources do not report a figure for 2026."
                  sources:
                    - link: "https://example.com/report"
                      title: "Platform usage report"
                      snippet: "Daily active users were last disclosed in 2024."
                meta:
                  credits_charged: 1
                  account_id: "1178367350552305665"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/tools/algorithm-predict:
    post:
      tags: [Tools]
      operationId: predictAlgorithm
      summary: Compare two versions of a post
      description: |
        Scores two versions of a post from 0 to 100 against what the X
        timeline tends to reward (hook strength, what drives replies and
        quotes, structure, shareability), and suggests a few short
        improvements for each.

        **The scores are a model's opinion.** They are useful for comparing
        two drafts against each other, not for predicting real reach, and
        should be reported that way.

        Nothing is posted or scheduled.

        Costs AI credits, measured and typically 1. Spends no live-data
        allowance. Needs a key with the `write` scope.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AlgorithmPredictRequest" }
            example:
              version_a: "Most founders think they have a traffic problem. They have a positioning problem."
              version_b: "you don't have a traffic problem. you have a positioning problem."
      responses:
        "200":
          description: A score and improvements for each version.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      version_a: { $ref: "#/components/schemas/AlgorithmPredictVersion" }
                      version_b: { $ref: "#/components/schemas/AlgorithmPredictVersion" }
                  meta: { $ref: "#/components/schemas/ToolMeta" }
              example:
                data:
                  version_a:
                    score: 62
                    improvements:
                      - text: "Open with the reversal"
                        tooltip: "Lead with the surprising half so the first line earns the second."
                  version_b:
                    score: 78
                    improvements:
                      - text: "Add one concrete example"
                        tooltip: "A named situation makes the claim land instead of reading as a slogan."
                meta:
                  credits_charged: 1
                  account_id: "1178367350552305665"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/replies:
    get:
      tags: [Content]
      operationId: listReplies
      summary: List replies you sent
      description: |
        Replies the selected account has posted, newest first.

        Page 1 of an unfiltered listing also includes replies sent from the
        SuperX app's Engage tab in the last 4 hours. Those live only in
        SuperX until X reports the post back, so without them the newest
        replies would be missing here while the app already shows them. They
        carry `metrics_pending: true` with all-zero `metrics` (nothing is
        measured yet), and because they are prepended, page 1 can return more
        than `limit` items; `has_more` still describes the stored replies
        only. Later pages and any `since`/`until` query never include them.
        Merged replies are deduplicated against page 1 only, so one that has
        already been stored and pushed onto page 2 can appear twice across
        pages: deduplicate by `id` if you page through the whole list.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - $ref: "#/components/parameters/Since"
        - $ref: "#/components/parameters/Until"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of replies.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Post" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "1941200000000000001"
                    text: "Agreed. The first 100 posts are practice."
                    posted_at: "2026-07-05T16:20:00.000Z"
                    url: "https://x.com/mainhandle/status/1941200000000000001"
                    is_reply: true
                    in_reply_to_id: "1941199999999999999"
                    is_quote: false
                    thread_id: "1941199999999999999"
                    metrics:
                      likes: 12
                      replies: 1
                      reposts: 0
                      quotes: 0
                      bookmarks: 2
                      impressions: 1840
                pagination: { page: 1, limit: 20, has_more: false }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/replies/received:
    get:
      tags: [Audience]
      operationId: listReceivedReplies
      summary: List replies you received
      description: Every stored reply the selected account has received across all of its posts, with the replier's profile and the replied-to post when available.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: sort
          in: query
          schema:
            type: string
            enum: [recent, most_liked]
            default: recent
        - $ref: "#/components/parameters/Since"
        - $ref: "#/components/parameters/Until"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of received replies.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ReceivedReply" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "1941300000000000002"
                    text: "This thread changed how I plan my week."
                    posted_at: "2026-07-03T10:12:45.000Z"
                    metrics: { likes: 9 }
                    author:
                      id: "944883311"
                      username: "replierhandle"
                      name: "Replier Name"
                      avatar_url: "https://pbs.twimg.com/profile_images/..."
                    in_reply_to:
                      id: "1941299999999999998"
                      text: "How I plan a content week in 20 minutes:"
                      posted_at: "2026-07-03T09:00:00.000Z"
                pagination: { page: 1, limit: 20, has_more: false, total: 14 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/inspiration:
    get:
      tags: [Inspiration]
      operationId: searchInspiration
      summary: Search the inspiration library
      description: |
        Search a library of 50M+ real high-performing posts by topic. Use the
        results for structures, hooks and angles to remix, never to copy.

        The library is user-level, so there is no `account_id` parameter.
        Results are relevance-ranked: the strongest matches come first, more
        loosely related posts after. A page can return fewer posts than
        `limit` once weak and promotional matches are removed. Pagination is
        heuristic; `has_more` means more library pages may exist (pages run
        1 to 7).

        Each result carries an `outlier_score`: the post's engagement rate vs
        the expected rate for its author's follower tier, x10 (10 = expected,
        higher = overperformed). Null when the author's follower count is
        unknown.
      parameters:
        - name: q
          in: query
          required: true
          description: Topic or theme to search for (1-200 characters).
          schema:
            type: string
            minLength: 1
            maxLength: 200
        - name: sort
          in: query
          description: |
            Result ordering. `relevant` (default) is relevance-ranked, the
            strongest matches first.
          schema:
            type: string
            enum: [relevant, recent, likes, reposts, impressions, outlier]
            default: relevant
        - name: min_likes
          in: query
          description: Only posts with at least this many likes.
          schema: { type: integer, minimum: 0 }
        - name: min_reposts
          in: query
          description: Only posts with at least this many reposts.
          schema: { type: integer, minimum: 0 }
        - name: min_replies
          in: query
          description: Only posts with at least this many replies.
          schema: { type: integer, minimum: 0 }
        - name: min_bookmarks
          in: query
          description: Only posts with at least this many bookmarks.
          schema: { type: integer, minimum: 0 }
        - name: min_impressions
          in: query
          description: Only posts with at least this many impressions.
          schema: { type: integer, minimum: 0 }
        - name: min_length
          in: query
          description: Only posts with at least this many characters.
          schema: { type: integer, minimum: 0 }
        - name: min_followers
          in: query
          description: Only posts from authors with at least this many followers.
          schema: { type: integer, minimum: 0 }
        - name: max_followers
          in: query
          description: Only posts from authors with at most this many followers.
          schema: { type: integer, minimum: 0 }
        - $ref: "#/components/parameters/Since"
        - $ref: "#/components/parameters/Until"
        - name: lang
          in: query
          description: Language code.
          schema:
            type: string
            default: en
        - name: exclude_topics
          in: query
          description: Comma-separated topics to exclude (keyword filter).
          schema:
            type: string
            example: "crypto,politics"
        - name: limit
          in: query
          description: Maximum posts per page, 1 to 50. Pages can be shorter after filtering.
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
        - name: page
          in: query
          description: 1-based page number, 1 to 7.
          schema:
            type: integer
            minimum: 1
            maximum: 7
            default: 1
      responses:
        "200":
          description: A page of matching posts.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/InspirationPost" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "1938765432109876543"
                    url: "https://x.com/buildersam/status/1938765432109876543"
                    text: "I built my first product in 30 days with zero audience. Here is exactly what I would do differently:"
                    posted_at: "2026-06-20T15:04:00.000Z"
                    lang: "en"
                    likes: 4211
                    replies: 312
                    reposts: 388
                    bookmarks: 1904
                    impressions: 512000
                    outlier_score: 14.2
                    author:
                      handle: "buildersam"
                      name: "Sam Fields"
                      followers: 8421
                      verified: true
                pagination: { page: 1, limit: 20, has_more: true }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/inspiration/media:
    get:
      tags: [Inspiration]
      operationId: searchInspirationMedia
      summary: Search the inspiration media index
      description: |
        Search the cross-platform media index behind the SuperX app's
        Inspiration > Media tab: short-form video and image posts from X,
        Instagram, YouTube, Threads, Reddit and LinkedIn, with captions, a
        content summary and engagement counts. Use it for visual format and
        hook research, never to copy.

        Media file URLs are not exposed through the API; open `source_url`
        for the original post.

        Owner-scoped, so there is no `account_id`. Omit `q` to BROWSE the
        newest media instead of searching; `meta.mode` reports which ran, and
        only `search` results carry a `score`. Unlike the app there is no
        personalisation: the app fills an empty search box from the signed-in
        person's profile and click history, which an owner-scoped key has no
        equivalent of.

        There is NO pagination: the index returns at most 120 items per
        query, and `limit` only trims that. Ask for the 120 and filter on
        your side rather than looking for a page parameter.

        Counts as a read and costs no enrichment units. The media index has
        two caps of its own, per account: a burst of 20 refilling one every
        3 seconds, and 500 FRESH searches per UTC day. Repeats of a recent
        identical search are served from a short-lived cache and do not count
        against the daily cap. Both return `429 rate_limited` with
        `Retry-After`.
      parameters:
        - name: q
          in: query
          description: What to search for (max 300 characters). Omit to browse the newest media.
          schema: { type: string, maxLength: 300 }
        - name: platforms
          in: query
          description: |
            Comma-separated platforms to include. Default:
            `instagram,youtube,x,threads`.
          schema:
            type: string
            example: "youtube,instagram"
        - name: time_filter
          in: query
          description: How recent the media must be.
          schema:
            type: string
            enum: [all, "24h", "7d", "30d"]
            default: all
        - name: media_type
          in: query
          description: Media kind.
          schema:
            type: string
            enum: [all, video, image]
            default: all
        - name: content_type
          in: query
          description: |
            Free-text content-type label as stored in the index (max 50
            characters). There is no enumerated list: a label the index does
            not use returns zero items and still counts against the daily
            search cap, so leave it off unless you know the label.
          schema: { type: string, maxLength: 50 }
        - name: limit
          in: query
          description: Items to return (1-120). 120 is everything one query can return.
          schema: { type: integer, minimum: 1, maximum: 120, default: 20 }
      responses:
        "200":
          description: Matching media items.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/MediaItem" }
                  meta:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum: [search, browse]
                        description: search = ranked against your query, browse = newest first.
                      total:
                        type: integer
                        description: Items the index returned before the `limit` slice.
              example:
                data:
                  - id: "ig_3421887766"
                    platform: instagram
                    title: "3 hooks that made this reel go to 2M"
                    caption: "The first second is everything."
                    summary: "Creator breaks down three opening lines and why each one holds attention."
                    content_type: "educational"
                    source_url: "https://instagram.com/p/Cxyz123"
                    author_username: "hookschool"
                    author_url: "https://instagram.com/hookschool"
                    posted_at: "2026-08-28T12:11:00.000Z"
                    like_count: 48211
                    view_count: 2041882
                    comment_count: 611
                    share_count: 2044
                    engagement_tier: "high"
                    score: 0.7412
                meta: { mode: search, total: 120 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/audience/{kind}:
    get:
      tags: [Audience]
      operationId: getAudience
      summary: Read an audience list
      description: |
        A page of the account's followers, following, repliers or reposters
        from SuperX's synced snapshot.

        These four are the system lists in the app's Contacts tab. They live
        in SuperX's own audience store rather than in the contact lists you
        create, which is why `/v1/contact-lists/{id}/members` will not read
        them and this endpoint exists.

        Paging is by CURSOR, not page number: pass `pagination.next_cursor`
        back as `cursor` to continue. A cursor points at the last row of the
        page you were given, so new rows arriving mid-walk never shift the
        pages you already read. There is no `total`; `meta.synced_count` is
        the size of the whole list.

        `followers` and `following` come from the follow graph SuperX syncs
        for the account, so `meta` reports the sync `status`, the plan's
        `backfill_cap` and whether the account is deeper than that cap.
        `repliers` and `reposters` are a rolling 90-day window
        (`meta.window_days`), so someone whose last reply ages past 90 days
        drops out and reappears on their next reply.

        `engaged_count` is replies plus reposts in the last 90 days for the
        follow lists, and this list's own action count for `repliers` and
        `reposters`. `icp_score` and `icp_rationale` are always null here:
        scoring belongs to signal-agent deposits, not to the snapshot.
      parameters:
        - name: kind
          in: path
          required: true
          schema:
            type: string
            enum: [followers, following, repliers, reposters]
        - $ref: "#/components/parameters/AccountId"
        - name: cursor
          in: query
          description: next_cursor from the previous page. Omit for the first page.
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: A page of the audience.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/AudienceMember" }
                  meta: { $ref: "#/components/schemas/AudienceMeta" }
                  pagination: { $ref: "#/components/schemas/CursorPagination" }
              example:
                data:
                  - id: "44196397001234567"
                    x_user_id: "44196397001234567"
                    username: "activefan"
                    name: "Sam Porter"
                    avatar_url: "https://pbs.twimg.com/profile_images/example3.jpg"
                    location: "Lisbon"
                    followers_count: 3120
                    following_count: 480
                    engaged_count: 12
                    source: followers
                    added_at: "2026-08-30T09:14:00.000Z"
                    icp_score: null
                    icp_rationale: null
                    can_dm: true
                meta:
                  kind: followers
                  account_id: "9HVHDe4WsJfJcR5SFauMD"
                  x_account_id: "1178367350552305665"
                  synced_count: 13270
                  status: complete
                  backfill_cap: 25000
                  is_capped: false
                  window_days: null
                pagination:
                  limit: 50
                  has_more: true
                  next_cursor: "MTc1NjU0NDA0MDAwMDo0NDE5NjM5NzAwMTIzNDU2Nw"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/contacts:
    get:
      tags: [Audience]
      operationId: listContacts
      summary: List engaged contacts
      description: People who engaged with the selected account over the moving 90-day window, with reply and repost tallies.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: sort
          in: query
          schema:
            type: string
            enum: [engagement, replies, reposts]
            default: engagement
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of contacts.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Contact" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "44196397001234567"
                    username: "activefan"
                    name: "Sam Porter"
                    avatar_url: "https://pbs.twimg.com/profile_images/example3.jpg"
                    replies_90d: 14
                    reposts_90d: 6
                pagination: { page: 1, limit: 20, has_more: true, total: 181 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/contacts/{id}:
    get:
      tags: [Audience]
      operationId: getContact
      summary: Get one contact
      description: |
        One person's stored profile (handle, name, bio, location, website,
        follower counts, verified status) plus the lists you created that they
        are a member of, each with the `member_id` the remove endpoints take.

        LIMITED TO THE ACCOUNT'S KNOWN CONTACTS. The id must be someone the
        selected account has a relationship with: a person who has replied to
        or reposted its posts, a member of one of its contact lists (manual or
        system), or a scored signal lead. Any other id returns 404
        `contact_not_found`, even when SuperX holds a cached profile for it -
        this is not a general X profile lookup.

        The default read serves the stored profile and costs one read.
        `refresh=true` refreshes a stale profile from X and therefore also
        counts against the tighter live-contact-enrichment limit, the same
        bucket `POST /v1/contact-lists/{id}/members` uses. The `refresh=true`
        enrichment unit is charged only after the contact gate passes, so an
        id outside your contacts does not spend one.
      parameters:
        - name: id
          in: path
          required: true
          description: The person's numeric X user id (from `GET /v1/contacts`, list members, or signal leads).
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
        - name: refresh
          in: query
          description: Refresh the profile from X when the stored copy is stale. Costs one enrichment unit. The `refresh=true` enrichment unit is charged only after the contact gate passes, so an id outside your contacts does not spend one.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: The contact.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ContactDetail" }
              example:
                data:
                  id: "944883311"
                  username: "founderhandle"
                  name: "Founder Name"
                  avatar_url: "https://pbs.twimg.com/profile_images/..."
                  bio: "Bootstrapping a SaaS in public."
                  location: "Austin, TX"
                  url: "https://example.com"
                  followers_count: 12800
                  following_count: 640
                  is_blue_verified: true
                  verified_type: null
                  lists:
                    - id: "k1l2m3n4o5p6q7r8s9t0u"
                      name: "Founder prospects"
                      member_id: "m1a2b3c4d5e6f7g8h9i0j"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/ContactNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contacts/{id}/replies:
    get:
      tags: [Audience]
      operationId: listContactReplies
      summary: List one contact's replies to you
      description: The stored reply history from one contact to the selected account, with the replied-to post when available.
      parameters:
        - name: id
          in: path
          required: true
          description: The contact's id from `GET /v1/contacts`.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
        - name: sort
          in: query
          schema:
            type: string
            enum: [recent, most_liked]
            default: recent
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of the contact's replies.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ContactReply" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "1941300000000000002"
                    text: "This thread changed how I plan my week."
                    posted_at: "2026-07-03T10:12:45.000Z"
                    metrics: { likes: 9 }
                    in_reply_to:
                      id: "1941299999999999998"
                      text: "How I plan a content week in 20 minutes:"
                      posted_at: "2026-07-03T09:00:00.000Z"
                pagination: { page: 1, limit: 20, has_more: false, total: 14 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/contacts/{id}/notes:
    get:
      tags: [Audience]
      operationId: listContactNotes
      summary: List notes on a contact
      description: |
        The private notes this account has written about one person, newest
        first. Notes live inside SuperX and are never posted anywhere. Not
        paginated: notes are per-contact and few.

        Works for any X id the account already has a note on, whether or not
        that person is still one of its contacts; an id with no notes returns
        an empty array, not a 404. Only creating a note requires a known
        contact.
      parameters:
        - name: id
          in: path
          required: true
          description: The person's numeric X user id.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The contact's notes.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ContactNote" }
              example:
                data:
                  - id: "n1a2b3c4d5e6f7g8h9i0j"
                    body: "Met at the SaaS meetup. Wants a demo in September."
                    created_at: "2026-08-14T16:20:00.000Z"
                    updated_at: "2026-08-14T16:20:00.000Z"
                    created_by:
                      x_user_id: "44196397"
                      username: "yourhandle"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    post:
      tags: [Audience]
      operationId: createContactNote
      summary: Add a note to a contact
      description: |
        Write a private note about one person. The note is attributed to the
        account it was written as (`created_by`), which for an API write is the
        account named by `account_id` (your main account when omitted).

        Creating a note requires a KNOWN contact: the id must be one of the
        account's engagers, contact-list members or signal leads, so an id
        outside that set returns 404 `contact_not_found`. This is the only
        note operation with that restriction - reading, editing and deleting
        work on any id the account already has a note on.

        No `Idempotency-Key`: notes have no uniqueness constraint, so a replay
        creates a second note. Duplicates are visible in the list read and
        deletable. Works for your main account or any account linked to it;
        accounts shared with you are read-only. Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The person's numeric X user id.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
                  minLength: 1
                  maxLength: 5000
                account_id:
                  type: string
                  description: The account to write as. Defaults to your main account.
            example:
              body: "Met at the SaaS meetup. Wants a demo in September."
      responses:
        "201":
          description: The created note.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ContactNote" }
              example:
                data:
                  id: "n1a2b3c4d5e6f7g8h9i0j"
                  body: "Met at the SaaS meetup. Wants a demo in September."
                  created_at: "2026-09-06T10:00:00.000Z"
                  updated_at: "2026-09-06T10:00:00.000Z"
                  created_by:
                    x_user_id: "44196397"
                    username: "yourhandle"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ContactNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contacts/{id}/notes/{noteId}:
    patch:
      tags: [Audience]
      operationId: updateContactNote
      summary: Edit a note on a contact
      description: |
        Replace one note's body. The note must belong to the contact in the
        path: a note id from another contact returns 404 `note_not_found`.
        That is the only check - editing works on any id the account already
        has a note on, whether or not that person is still one of its
        contacts.
        Works for your main account or any account linked to it; accounts
        shared with you are read-only. Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The person's numeric X user id.
          schema:
            type: string
        - name: noteId
          in: path
          required: true
          description: The note id from `GET /v1/contacts/{id}/notes`.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
                  minLength: 1
                  maxLength: 5000
                account_id:
                  type: string
            example:
              body: "Demo booked for 12 September."
      responses:
        "200":
          description: The updated note.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ContactNote" }
              example:
                data:
                  id: "n1a2b3c4d5e6f7g8h9i0j"
                  body: "Demo booked for 12 September."
                  created_at: "2026-08-14T16:20:00.000Z"
                  updated_at: "2026-09-06T10:05:00.000Z"
                  created_by:
                    x_user_id: "44196397"
                    username: "yourhandle"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/NoteNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    delete:
      tags: [Audience]
      operationId: deleteContactNote
      summary: Delete a note on a contact
      description: |
        Returns 204 with no body. Deleting an already-deleted note, or a note
        belonging to another contact, returns 404 `note_not_found`. That is
        the only check - deleting works on any id the account already has a
        note on, whether or not that person is still one of its contacts.
        Works for
        your main account or any account linked to it; accounts shared with you
        are read-only. Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The person's numeric X user id.
          schema:
            type: string
        - name: noteId
          in: path
          required: true
          description: The note id from `GET /v1/contacts/{id}/notes`.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "204":
          description: The note was deleted.
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/NoteNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contact-lists:
    get:
      tags: [Contact Lists]
      operationId: listContactLists
      summary: List contact lists
      description: |
        The selected account's contact lists: system lists first (Followers,
        Following, Repliers, Reposters; `is_system: true` with a `kind`), then
        lists you created, newest first. System lists are read-only and
        index-only: their members are not readable through the API and
        `member_count` is 0 for them.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The account's contact lists.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ContactList" }
              example:
                data:
                  - id: "sys_f1a2b3c4d5e6f7g8h9i0j"
                    name: "Followers"
                    is_system: true
                    kind: "followers"
                    member_count: 0
                    created_at: "2026-05-01T09:00:00.000Z"
                    updated_at: "2026-05-01T09:00:00.000Z"
                  - id: "k1l2m3n4o5p6q7r8s9t0u"
                    name: "Founder prospects"
                    is_system: false
                    kind: null
                    member_count: 42
                    created_at: "2026-06-20T14:30:00.000Z"
                    updated_at: "2026-07-01T08:15:00.000Z"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

    post:
      tags: [Contact Lists]
      operationId: createContactList
      summary: Create a contact list
      description: |
        Create a list you can add people to. Names are NOT unique: the SuperX
        app allows two lists with the same name and this endpoint matches it,
        so read `GET /v1/contact-lists` first if the caller expects one list
        per name. No `Idempotency-Key`: a replay creates a second list, which
        is visible in the index and deletable.

        Works for your main account or any account linked to it (`account_id`);
        accounts shared with you are read-only. Needs the write scope.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 120
                account_id:
                  type: string
            example:
              name: "Founder prospects"
      responses:
        "201":
          description: The created list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ContactList" }
              example:
                data:
                  id: "k1l2m3n4o5p6q7r8s9t0u"
                  name: "Founder prospects"
                  is_system: false
                  kind: null
                  member_count: 0
                  created_at: "2026-09-06T10:00:00.000Z"
                  updated_at: "2026-09-06T10:00:00.000Z"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contact-lists/{id}:
    patch:
      tags: [Contact Lists]
      operationId: renameContactList
      summary: Rename a contact list
      description: |
        Rename a list you created. System lists are managed automatically and
        return 400 `system_list_read_only`. Works for your main account or any
        account linked to it (`account_id`); accounts shared with you are
        read-only. Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The list id from `GET /v1/contact-lists`.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 120
                account_id:
                  type: string
            example:
              name: "Founder prospects Q4"
      responses:
        "200":
          description: The updated list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ContactList" }
              example:
                data:
                  id: "k1l2m3n4o5p6q7r8s9t0u"
                  name: "Founder prospects Q4"
                  is_system: false
                  kind: null
                  member_count: 42
                  created_at: "2026-06-20T14:30:00.000Z"
                  updated_at: "2026-09-06T10:05:00.000Z"
        "400": { $ref: "#/components/responses/SystemListReadOnly" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ListNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    delete:
      tags: [Contact Lists]
      operationId: deleteContactList
      summary: Delete a contact list
      description: |
        Delete a list you created, and with it its membership. The people
        themselves are untouched, but a signal agent depositing into this list
        stops until it is repointed in the SuperX app. Returns 204 with no
        body; a second delete returns 404 `list_not_found`. System lists return
        400 `system_list_read_only`.

        Works for your main account or any account linked to it (`account_id`);
        accounts shared with you are read-only. Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The list id from `GET /v1/contact-lists`.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "204":
          description: The list was deleted.
        "400": { $ref: "#/components/responses/SystemListReadOnly" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ListNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contact-lists/{id}/members:
    get:
      tags: [Contact Lists]
      operationId: listContactListMembers
      summary: List contact list members
      description: |
        Members of one contact list you created. Includes a 90-day
        `engaged_count` (replies + reposts to the account) per member.
        System lists return 400 `system_list_not_supported`.
      parameters:
        - name: id
          in: path
          required: true
          description: The list id from `GET /v1/contact-lists`.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
        - name: q
          in: query
          description: Filter members by handle or name substring.
          schema:
            type: string
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of list members.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ContactListMember" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "m1a2b3c4d5e6f7g8h9i0j"
                    x_user_id: "944883311"
                    username: "founderhandle"
                    name: "Founder Name"
                    avatar_url: "https://pbs.twimg.com/profile_images/..."
                    location: "Austin, TX"
                    followers_count: 12800
                    following_count: 640
                    engaged_count: 7
                    source: "manual"
                    added_at: "2026-06-25T11:00:00.000Z"
                pagination: { page: 1, limit: 20, has_more: true, total: 42 }
        "400": { $ref: "#/components/responses/SystemListNotSupported" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/ListNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    post:
      tags: [Contact Lists]
      operationId: addContactListMember
      summary: Add a contact list member
      description: |
        Add a person to a list you created, by `handle` (live profile lookup)
        or by `x_user_id` (numeric X user id already known to SuperX).
        Exactly one of the two. Naturally idempotent: adding someone already
        in the list returns 200 with the existing member and
        `duplicate: true`, and writes nothing. Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only.
        Needs the write scope. No `Idempotency-Key` support (none is
        needed).

        Counts against a separate live-contact-enrichment rate limit (varies
        by plan), in addition to the normal write limits.
      parameters:
        - name: id
          in: path
          required: true
          description: The list id from `GET /v1/contact-lists`.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                handle:
                  type: string
                  description: X username, with or without the @.
                x_user_id:
                  type: string
                  description: Numeric X user id.
            examples:
              byHandle:
                value: { handle: "founderhandle" }
              byId:
                value: { x_user_id: "944883311" }
      responses:
        "201":
          description: The member was added.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/AddedContactListMember" }
              example:
                data:
                  id: "m1a2b3c4d5e6f7g8h9i0j"
                  x_user_id: "944883311"
                  username: "founderhandle"
                  name: "Founder Name"
                  avatar_url: "https://pbs.twimg.com/profile_images/..."
                  added_at: "2026-07-08T10:00:00.000Z"
        "200":
          description: Already a member; nothing changed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/AddedContactListMember" }
                  duplicate:
                    type: boolean
              example:
                data:
                  id: "m1a2b3c4d5e6f7g8h9i0j"
                  x_user_id: "944883311"
                  username: "founderhandle"
                  name: "Founder Name"
                  avatar_url: "https://pbs.twimg.com/profile_images/..."
                  added_at: "2026-06-25T11:00:00.000Z"
                duplicate: true
        "400": { $ref: "#/components/responses/SystemListReadOnly" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ListOrUserNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contact-lists/{id}/members/{memberId}:
    delete:
      tags: [Contact Lists]
      operationId: removeContactListMember
      summary: Remove a contact list member
      description: |
        Remove a member from a list you created. Returns 204 with no body.
        Removing an already-removed member returns 404 `member_not_found`.
        Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only.
        Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The list id from `GET /v1/contact-lists`.
          schema:
            type: string
        - name: memberId
          in: path
          required: true
          description: The member id from `GET /v1/contact-lists/{id}/members`.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "204":
          description: The member was removed.
        "400": { $ref: "#/components/responses/SystemListReadOnly" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/MemberNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contact-lists/{id}/members/bulk:
    post:
      tags: [Contact Lists]
      operationId: addContactListMembers
      summary: Add contact list members in bulk
      description: |
        Add up to 500 people to a list you created, by numeric X user id.

        This endpoint does NO live profile lookup, which is why it costs one
        write and no enrichment: it builds each member from the profile SuperX
        already stores. Ids SuperX has never seen come back in `not_found` and
        are NOT added, so no placeholder members are ever created. Add those
        one at a time with `POST /v1/contact-lists/{id}/members` and a
        `handle`, which does resolve live (and costs one enrichment unit).

        Re-adding an existing member is counted in `duplicates`, never an
        error. Duplicate ids inside one call are collapsed. System lists return
        400 `system_list_read_only`.

        Works for your main account or any account linked to it (`account_id`);
        accounts shared with you are read-only. Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The list id from `GET /v1/contact-lists`.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [x_user_ids]
              properties:
                x_user_ids:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items:
                    type: string
                  description: Numeric X user ids.
                account_id:
                  type: string
            example:
              x_user_ids: ["944883311", "44196397", "1234567890"]
      responses:
        "200":
          description: The bulk add finished. Read the counters for what happened.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/BulkAddResult" }
              example:
                data:
                  added: 2
                  duplicates: 0
                  failed: 0
                  not_found: ["1234567890"]
                  total_in_list_after: 44
        "400": { $ref: "#/components/responses/SystemListReadOnly" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ListNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/contact-lists/{id}/members/bulk-delete:
    post:
      tags: [Contact Lists]
      operationId: removeContactListMembers
      summary: Remove contact list members in bulk
      description: |
        Remove up to 500 members from a list you created, by member id (the
        `id` from `GET /v1/contact-lists/{id}/members`).

        POST rather than DELETE because the ids travel in a body. The delete is
        scoped to this list, so ids that are not in it are skipped and
        `deleted` can be lower than the number you sent. There is deliberately
        no "delete everything matching a filter" mode. System lists return 400
        `system_list_read_only`.

        Works for your main account or any account linked to it (`account_id`);
        accounts shared with you are read-only. Needs the write scope.
      parameters:
        - name: id
          in: path
          required: true
          description: The list id from `GET /v1/contact-lists`.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [member_ids]
              properties:
                member_ids:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items:
                    type: string
                  description: Member ids from `GET /v1/contact-lists/{id}/members`.
                account_id:
                  type: string
            example:
              member_ids: ["m1a2b3c4d5e6f7g8h9i0j", "m2b3c4d5e6f7g8h9i0j1k"]
      responses:
        "200":
          description: The bulk removal finished.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/BulkRemoveResult" }
              example:
                data:
                  deleted: 2
                  total_in_list_after: 42
        "400": { $ref: "#/components/responses/SystemListReadOnly" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ListNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/signals/agents:
    get:
      tags: [Signals]
      operationId: listSignalAgents
      summary: List signal agents
      description: |
        The account's signal agents: the automated lead finders from the
        SuperX app. Each agent watches one or more signals (profiles,
        followers, keywords, or lists) and scores the people it finds
        against its ideal customer profile. Agents can be created, paused,
        resumed, and deleted through the API; name, ICP, precision, and
        destination edits happen in the SuperX app. `destination_list_id`
        joins to `GET /v1/contact-lists` for the target list's name. Not
        paginated (agent counts are capped per account).
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The account's signal agents.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/SignalAgent" }
              example:
                data:
                  - id: 3
                    name: "Founder prospects"
                    status: active
                    status_detail: null
                    icp_description: "Early-stage SaaS founders posting about growth"
                    precision_mode: high
                    destination_list_id: "contact:V1StGXR8_Z5jdHi6B-myT"
                    created_at: "2026-06-20T09:00:00.000Z"
                    updated_at: "2026-07-01T18:30:00.000Z"
                    deposited_count: 42
                    last_checked: "2026-07-08T06:15:00.000Z"
                    signals:
                      - id: 7
                        type: profile_watch
                        handle: "naval"
                        name: "Naval"
                        avatar: "https://pbs.twimg.com/profile_images/..."
                        query: null
                        list_name: null
                        status: active
                        status_detail: null
                        last_run_at: "2026-07-08T06:15:00.000Z"
                        created_at: "2026-06-20T09:00:00.000Z"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

    post:
      tags: [Signals]
      operationId: createSignalAgent
      summary: Create a signal agent
      description: |
        Create a signal agent: it watches X for the given keywords and scores
        the people it finds against `icp_description`. Matching leads land in
        Signals (and `GET /v1/signals/leads`) over the following minutes and
        days; nothing is returned synchronously.

        `keywords` is shorthand for keyword watches; `signals` carries all
        four watch types (`keyword_watch`, `profile_watch`, `follower_watch`,
        `list_watch`). Send either or both: 1-5 distinct entries combined,
        keywords first. Omit both and 1-3 keyword watches are suggested from
        the ICP (if suggestion fails, nothing is created and you get 502
        `upstream_error`). Omit `destination_list_id` and a contact list
        named `Leads: <agent name>` is created for the leads, flagged
        `destination_list_created: true` in the response.

        PARTIAL SUCCESS: entries the add path rejects (an unknown handle, a
        private list, a duplicate, the per-agent signal cap) come back in
        `warnings` and the agent is still created from the entries that
        landed. Only when EVERY entry fails is the agent rolled back and an
        error returned. Read `warnings` before reporting what the agent
        watches, and re-add the fixed entries with
        `POST /v1/signals/agents/{id}/signals`.

        A create carrying any `profile_watch`, `follower_watch` or
        `list_watch` entry resolves those targets live and consumes ONE unit
        of the enrichment allowance (one per call, not per entry).

        Plan limits apply (403 `cap_reached`). Supports `Idempotency-Key`
        (max 64 chars): retries with the same key and body replay the
        original response with `Idempotency-Replayed: true`. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, icp_description]
              properties:
                name:
                  type: string
                  maxLength: 80
                  description: Agent name shown in SuperX.
                icp_description:
                  type: string
                  maxLength: 500
                  description: Who the ideal leads are; found people are scored against this.
                precision_mode:
                  type: string
                  enum: [high, discovery]
                  default: high
                  description: high = fewer, stricter matches; discovery = broader net.
                keywords:
                  type: array
                  minItems: 1
                  maxItems: 5
                  items:
                    type: string
                    maxLength: 180
                  description: Plain-language descriptions of what the target customer posts about. Omit to auto-suggest from the ICP. Keywords beyond your plan's per-agent signal limit are dropped; the response lists the signals actually created.
                signals:
                  type: array
                  minItems: 1
                  maxItems: 5
                  items: { $ref: "#/components/schemas/SignalInput" }
                  description: Non-keyword watches to add alongside `keywords`. Combined with `keywords`, at most 5 distinct entries.
                destination_list_id:
                  type: string
                  description: Contact list id (from `GET /v1/contact-lists`) that receives the leads. Omit to auto-create one.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              name: "Build in public founders"
              icp_description: "Indie founders building SaaS in public, sharing MRR and launch updates"
              precision_mode: high
              keywords: ["building in public", "just shipped my MVP"]
      responses:
        "201":
          description: The created agent with its keyword signals, plus whether a destination list was auto-created.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
            Idempotency-Replayed:
              description: Present and "true" when this response was replayed from a previous request with the same Idempotency-Key.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/SignalAgent"
                      - type: object
                        properties:
                          destination_list_created:
                            type: boolean
                            description: true when destination_list_id was omitted and a list was created for the agent.
                          warnings:
                            type: array
                            description: Present only when at least one keyword or signal entry was rejected. The agent was created from the entries that landed.
                            items: { $ref: "#/components/schemas/SignalWarning" }
              example:
                data:
                  id: 12
                  name: "Build in public founders"
                  status: active
                  status_detail: null
                  icp_description: "Indie founders building SaaS in public, sharing MRR and launch updates"
                  precision_mode: high
                  destination_list_id: "contact:V1StGXR8_Z5jdHi6B-myT"
                  created_at: "2026-07-08T10:00:00.000Z"
                  updated_at: "2026-07-08T10:00:00.000Z"
                  deposited_count: 0
                  last_checked: null
                  signals:
                    - id: 31
                      type: keyword_watch
                      handle: null
                      name: null
                      avatar: null
                      query: "building in public"
                      list_name: null
                      status: active
                      status_detail: null
                      last_run_at: null
                      created_at: "2026-07-08T10:00:00.000Z"
                  destination_list_created: true
        "400":
          description: A parameter is missing, malformed, or out of range (`invalid_parameter`), or the create was rejected upstream (`invalid_request`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: invalid_parameter
                  message: "icp_description is required (1-500 characters)"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an account shared with you (`writes_main_account_only`; shared accounts are read-only through the API, your own linked accounts are not), lapsed subscription (legacy string envelope), or the plan's agent limit is reached (`cap_reached`).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: cap_reached
                  message: "You've reached your plan's signal agent limit (1 on the pro plan). Pause and delete an agent, or upgrade your plan."
        "404": { $ref: "#/components/responses/ListNotFound" }
        "409":
          description: The Idempotency-Key was already used with a different request body.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: idempotency_key_reuse
                  message: "This Idempotency-Key was already used with a different request body."
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/signals/agents/{id}:
    patch:
      tags: [Signals]
      operationId: updateSignalAgent
      summary: Update a signal agent
      description: |
        Edit an agent's `name`, `icp_description`, `precision_mode`,
        `destination_list_id`, `status`, or any combination. At least one
        field is required.

        Editing the ICP changes how NEW leads are scored; leads already found
        keep their scores. A `destination_list_id` that is not one of your
        own usable lists returns 404 `list_not_found`. `status` is `active`
        or `paused`: paused agents stop finding leads, and resuming picks up
        where the agent left off (a resume is rejected with
        `invalid_request` when the agent's stored destination list is gone -
        send a new `destination_list_id` first).

        The agent's SIGNALS are not edited here: each one resolves its target
        upstream, so they have their own endpoints
        (`POST /v1/signals/agents/{id}/signals`,
        `DELETE /v1/signals/agents/{id}/signals/{signalId}`). Naturally
        idempotent, so there is no `Idempotency-Key` support. Works for your
        main account or any account linked to it (`account_id`); accounts
        shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          description: The signal agent's numeric id (from `GET /v1/signals/agents`).
          schema:
            type: integer
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 80
                icp_description:
                  type: string
                  minLength: 1
                  maxLength: 500
                precision_mode:
                  type: string
                  enum: [high, discovery]
                destination_list_id:
                  type: string
                  description: Contact list id (from `GET /v1/contact-lists`) that receives the leads.
                status:
                  type: string
                  enum: [active, paused]
            example:
              status: paused
      responses:
        "200":
          description: The updated agent.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/SignalAgent" }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AgentOrListNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

    delete:
      tags: [Signals]
      operationId: deleteSignalAgent
      summary: Delete a signal agent
      description: |
        Delete an agent. The leads it already saved and its destination
        contact list stay untouched. Deleting the same id again returns 404
        `agent_not_found`. Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          description: The signal agent's numeric id (from `GET /v1/signals/agents`).
          schema:
            type: integer
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The agent was deleted.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                      deleted:
                        type: boolean
              example:
                data:
                  id: 12
                  deleted: true
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AgentNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/signals/agents/{id}/signals:
    post:
      tags: [Signals]
      operationId: addSignalAgentSignal
      summary: Add a signal to an agent
      description: |
        Add one thing for the agent to watch: a search (`keyword_watch`), an
        account's posts (`profile_watch`), the people an account follows
        (`follower_watch`), or the members of a public X list
        (`list_watch`). New leads start arriving over the following minutes
        and days; nothing is returned synchronously.

        One target per call: each add resolves its target live or compiles
        the search, so there is no batch form. Each plan caps how many
        signals one agent may hold (403 `cap_reached`) and an agent may watch
        each target once (409 `duplicate_signal`). `profile_watch`,
        `follower_watch` and `list_watch` consume one unit of the enrichment
        allowance; `keyword_watch` consumes none. Only PUBLIC X lists can be
        watched.

        No `Idempotency-Key`: the duplicate rule already makes a repeat
        harmless. Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          description: The signal agent's numeric id (from `GET /v1/signals/agents`).
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: "#/components/schemas/SignalInput"
                - type: object
                  properties:
                    account_id:
                      type: string
                      description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              type: follower_watch
              handle: "naval"
      responses:
        "201":
          description: The created signal.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/SignalAgentSignal" }
              example:
                data:
                  id: 118
                  type: follower_watch
                  handle: "naval"
                  name: "Naval"
                  avatar: "https://pbs.twimg.com/profile_images/..."
                  query: null
                  list_name: null
                  status: active
                  status_detail: null
                  last_run_at: null
                  created_at: "2026-09-07T10:00:00.000Z"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an account shared with you (`writes_main_account_only`), a lapsed subscription, or the agent's per-plan signal limit is reached (`cap_reached`).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: cap_reached
                  message: "This agent has reached its limit of 3 signals on the pro plan. Remove a signal in SuperX or upgrade your plan."
        "404": { $ref: "#/components/responses/SignalTargetNotFound" }
        "409": { $ref: "#/components/responses/DuplicateSignal" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/signals/agents/{id}/signals/{signalId}:
    delete:
      tags: [Signals]
      operationId: removeSignalAgentSignal
      summary: Remove a signal from an agent
      description: |
        Remove one signal. Leads it already found stay, and so does the
        agent's de-duplication state. Removing the agent's LAST signal is
        allowed: the agent stays, finding nothing, until a signal is added
        back. Deleting the same id again returns 404 `signal_not_found`.
        Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          description: The signal agent's numeric id (from `GET /v1/signals/agents`).
          schema:
            type: integer
        - name: signalId
          in: path
          required: true
          description: The signal's numeric id (from the agent's `signals` in `GET /v1/signals/agents`).
          schema:
            type: integer
        - $ref: "#/components/parameters/AccountId"
      responses:
        "204":
          description: The signal was removed.
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/SignalNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/signals/leads/{id}/feedback:
    post:
      tags: [Signals]
      operationId: setSignalLeadFeedback
      summary: Set feedback on a lead
      description: |
        Record the account's verdict on one lead - `fit`, `not_fit`, or
        `null` to clear it.

        The id is the numeric LEAD id from `GET /v1/signals/leads`, not the
        person's X user id: the same person can be a lead of two agents with
        a different verdict on each. The verdict is what the scorer's
        examples and the lead sweep learn from, so it is worth recording on
        leads you act on, and it is mirrored onto the person's row in the
        agent's destination contact list. It reads back as `feedback` /
        `feedback_at` on `GET /v1/signals/leads`.

        Naturally idempotent (the verdict is a value, not a counter), so
        there is no `Idempotency-Key` support. Works for your main account or
        any account linked to it (`account_id`); accounts shared with you are
        read-only.
      parameters:
        - name: id
          in: path
          required: true
          description: The lead's numeric id (from `GET /v1/signals/leads`).
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [feedback]
              properties:
                feedback:
                  type: string
                  nullable: true
                  enum: [fit, not_fit, null]
                  description: fit = a good lead, not_fit = a bad one, null clears the verdict.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              feedback: fit
      responses:
        "200":
          description: The stored verdict.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/LeadFeedback" }
              example:
                data:
                  id: 4821
                  feedback: fit
                  feedback_at: "2026-09-07T10:00:00.000Z"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/LeadNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/signals/leads/search:
    post:
      tags: [Signals]
      operationId: searchLeads
      summary: Search for leads on X now
      description: |
        One live keyword search over X, scored against an ideal-customer
        profile, returned in the response.

        **It creates nothing.** No signal agent, no saved leads, no contact
        list membership: the leads exist only in this response. For an
        audience that keeps filling up on its own, create a signal agent
        (`POST /v1/signals/agents`) instead, and read what it finds with
        `GET /v1/signals/leads`.

        Keywords are plain language, not search operators: phrases the people
        you want would actually post, comma-separated (or as a list) for
        alternatives. `precision: high` keeps only confident matches;
        `discovery` (the default) also returns adjacent ones.

        The search takes up to a minute. Each lead comes from ONE matched
        post, so treat `posts_count` as lifetime volume rather than proof of
        current activity.

        **Cost.** Reading X live costs AI credits, measured from what the
        search actually spends: at least 1 credit for any search that reaches
        X, typically 1-2. It also uses one of the plan's daily lead searches
        (`429 ai_action_limited` with `scope: "account"`) and a slice of a
        platform-wide fair-use ceiling on live-data actions (`scope:
        "platform"`, shared with every account, so wait and retry rather than
        assuming your own quota is gone). A search that never reaches X is
        not charged.

        Nothing is written by this endpoint, but it still needs a key with
        the `write` scope, because it spends AI credits and live requests:
        every non-GET `/v1` request needs that scope anyway. The MCP
        `search_leads` tool needs the write scope for the same reason. On
        both surfaces an account shared with you works, unlike the endpoints
        that really write: nothing is written to the account, and the credits
        come from the key owner's pool.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LeadSearchRequest" }
            example:
              keywords: "losing customers to churn, cancellations killing my MRR"
              icp_description: "Founders of small B2B SaaS companies who are worried about retention and would try a churn-reduction tool."
              precision: discovery
              max_leads: 10
      responses:
        "200":
          description: |
            The search finished. `leads` is empty when nothing qualified,
            with a `note` saying so.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/LeadSearchResult" }
                  note:
                    type: string
              example:
                data:
                  query_used: "(\"losing customers to churn\" OR \"cancellations killing my MRR\") -filter:replies -filter:retweets"
                  query_source: compiled
                  precision: discovery
                  requests_used: 5
                  posts_scanned: 87
                  candidates_scored: 30
                  credits_charged: 1
                  leads:
                    - user:
                        x_user_id: "1178367350552305665"
                        username: "robj3d3"
                        name: "Rob"
                        avatar_url: "https://pbs.twimg.com/profile_images/abc_normal.jpg"
                        followers_count: 8213
                        posts_count: 4102
                      icp_score: 4
                      icp_rationale: "Runs a small B2B SaaS and is actively complaining about cancellations."
                      provenance:
                        action: authored
                        tweet_text: "third cancellation this week and I still cannot tell why they left"
                        target_handle: null
                note: "Nothing was saved: this search creates no signal agent and stores no leads."
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/signals/leads:
    get:
      tags: [Signals]
      operationId: listSignalLeads
      summary: List signal leads
      description: |
        The leads the account's signal agents have found, newest first:
        people who matched an agent's ideal customer profile, with their
        profile, the match score and rationale, whether the lead has been
        saved to the agent's contact list yet (`deposited`/`deposited_at`),
        and provenance describing how the lead was discovered. Leads under
        paused or errored agents remain listed (the agent's status is on
        every row).
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: agent_id
          in: query
          description: Narrow to one agent by its numeric id (from `GET /v1/signals/agents`). An unknown or foreign id returns 404 `agent_not_found`.
          schema:
            type: integer
        - name: deposited
          in: query
          description: true returns only leads already saved to the agent's contact list; false returns only new ones. Omit for both.
          schema:
            type: boolean
        - $ref: "#/components/parameters/Since"
        - $ref: "#/components/parameters/Until"
        - name: limit
          in: query
          description: Items per page, up to 100. Default 50.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of leads, discovered_at descending.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/SignalLead" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: 918
                    agent: { id: 3, name: "Founder prospects", status: active }
                    user:
                      x_user_id: "944883311"
                      username: "founderhandle"
                      name: "Founder Name"
                      avatar_url: "https://pbs.twimg.com/profile_images/..."
                      followers_count: 4210
                      following_count: 388
                      location: "Austin, TX"
                    icp_score: 3
                    icp_rationale: "Bootstrapped SaaS founder actively posting about growth experiments."
                    deposited: true
                    deposited_at: "2026-07-07T12:00:00.000Z"
                    discovered_at: "2026-07-07T09:41:00.000Z"
                    provenance:
                      type: profile_watch
                      action: reply
                      source_ref: "1941300000000000002"
                      tweet_id: "1941300000000000002"
                      tweet_text: "We doubled activation by rewriting onboarding emails."
                      target_handle: "naval"
                pagination: { page: 1, limit: 50, has_more: false, total: 1 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AgentNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/signals/keywords/suggest:
    post:
      tags: [Signals]
      operationId: suggestKeywords
      summary: Suggest keyword watches
      description: |
        Turns a plain description of an audience into 2 or 3 keyword-watch
        ideas, the same ones the SuperX app stages when a user sets up a
        signal agent. Each suggestion is plain language, not an X search
        query: compilation happens when the keyword is added to an agent.

        Free. Nothing is created and no AI credits are charged. Pass a
        suggestion you like as a `keywords` entry on
        `POST /v1/signals/agents`, or as a `keyword_watch` signal on
        `POST /v1/signals/agents/{id}/signals`, to start watching it.

        This helper runs in SuperX's own process rather than through the
        app, so the app's short-term limiter does not apply and your plan's
        request rate limit is the only bound. It is still a model call each
        time, so keep it occasional.

        Needs a key with the `write` scope. Every non-GET route on this API
        does, and this one is a POST because the description goes in the
        body; it still creates nothing.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [icp_description]
              properties:
                icp_description:
                  type: string
                  minLength: 3
                  maxLength: 500
                  description: Who the ideal customer is, in plain language.
                account_id: { type: string }
            example:
              icp_description: "B2B SaaS founders worried about churn"
      responses:
        "200":
          description: The suggested keyword watches.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      suggestions:
                        type: array
                        items: { $ref: "#/components/schemas/KeywordSuggestion" }
              example:
                data:
                  suggestions:
                    - query: "churn is killing us"
                    - query: "looking for retention tools"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/signals/icp/expand:
    post:
      tags: [Signals]
      operationId: expandIcp
      summary: Expand an audience description into a rubric
      description: |
        Turns a plain description of an audience into the structured rubric
        signal agents score people against: who the ideal customer is, the
        needs that make someone a fit, the disqualifiers, and one example of
        a match and a non-match.

        Free. Nothing is created and no AI credits are charged. The rubric is
        a reading of the description rather than a field you can store:
        agents are created with `icp_description`, so use this to sharpen
        that text before calling `POST /v1/signals/agents`, or to score
        people yourself.

        This helper runs in SuperX's own process rather than through the
        app, so the app's short-term limiter does not apply and your plan's
        request rate limit is the only bound. It is still a model call each
        time, so keep it occasional.

        Needs a key with the `write` scope. Every non-GET route on this API
        does, and this one is a POST because the description goes in the
        body; it still creates nothing.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [icp_description]
              properties:
                icp_description:
                  type: string
                  minLength: 3
                  maxLength: 500
                  description: Who the ideal customer is, in plain language.
                account_id: { type: string }
            example:
              icp_description: "Indie founders building SaaS in public"
      responses:
        "200":
          description: The scoring rubric.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      rubric: { $ref: "#/components/schemas/IcpRubric" }
              example:
                data:
                  rubric:
                    who: "Solo founders shipping a SaaS product in public"
                    needs: ["growing an audience while building", "launch distribution"]
                    disqualifiers: ["agency owners", "accounts that only repost"]
                    example_match: "building my SaaS in public, $2k MRR, shipping weekly"
                    example_non_match: "growth agency for SaaS brands, DM for rates"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/signals/icp/expand-from-url:
    post:
      tags: [Signals]
      operationId: expandIcpFromUrl
      summary: Build an audience profile from a website
      description: |
        Reads a product or company website and infers who its ideal customers
        are on X: an audience description, the scoring rubric, and
        keyword-watch ideas, in one call. A bare domain is accepted.

        Free: no AI credits. The page read runs on the account's allowance of
        20 page reads a day, shared with the SuperX app, so an exhausted
        allowance returns `429 ai_action_limited` with `scope: "account"`.
        Unlike the two helpers above, this one goes through the SuperX app,
        so the app's short-term limiter binds it too: more than 10 calls in
        10 minutes returns `429 rate_limited`, which clears in about a
        minute. Takes up to a minute.

        Nothing is created, but the call needs a key with the `write` scope,
        as every non-GET route on this API does. Pass `icp_description` to
        `POST /v1/signals/agents` as the agent's ICP, and the suggestions as
        its keywords.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [url]
              properties:
                url:
                  type: string
                  maxLength: 2000
                  description: The website to read. A bare domain is accepted.
                account_id: { type: string }
            example:
              url: "superx.so"
      responses:
        "200":
          description: The inferred audience description, rubric and keyword ideas.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      icp_description: { type: string }
                      rubric: { $ref: "#/components/schemas/IcpRubric" }
                      suggestions:
                        type: array
                        items: { $ref: "#/components/schemas/KeywordSuggestion" }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "422":
          description: The page could not be read (`scrape_failed`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: scrape_failed
                  message: "That page could not be read. Check the url and try again."
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/engage/feeds:
    get:
      tags: [Engage]
      operationId: listEngageFeeds
      summary: List Engage feeds
      description: |
        The account's saved Engage feeds: the keyword and list feeds it
        browses in the SuperX app's Engage tab. Feeds are created and edited
        in the app; this endpoint only reads them. `fetch_units` is what one
        fetch of that feed costs against the feed bucket (list feeds count as
        up to 3, resolved exactly at fetch time). This endpoint never touches
        the feed bucket itself.

        Read-only by design. These endpoints return feed candidates for a
        person to review; replies are written and sent by a person in the
        SuperX app, which is why there is no reply endpoint here. Sending
        spammy, automated, or AI-generated replies that read as inauthentic
        may get your X account suspended under X's inauthentic-behavior
        rules and your SuperX account terminated. AI suggestions must be
        reviewed and meaningfully edited before they are sent, and you are
        solely responsible for what you post. Reply activity is logged and
        may be audited.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The account's saved feeds.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/EngageFeed" }
              example:
                data:
                  - id: "feed_kw_1"
                    name: "Bootstrapped SaaS"
                    type: keywords
                    keywords: ["bootstrapped saas", "indie hackers"]
                    active: true
                    fetch_units: 1
                  - id: "feed_list_2"
                    name: "Founders I follow"
                    type: list
                    list_id: "184"
                    list_name: "Founders"
                    active: false
                    fetch_units: 3
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

    post:
      tags: [Engage]
      operationId: createEngageFeed
      summary: Create an Engage feed
      description: |
        Save a new Engage feed on the account: a set of keywords, a public X
        list, or one of the account's own contact lists. The feed appears in
        `GET /v1/engage/feeds`, can be fetched with
        `GET /v1/engage/feeds/{id}/posts`, and shows up for the person in the
        SuperX app's Engage tab.

        The new feed does NOT become the feed the app has open: the API must
        not move someone's view while they are working in it. The response
        carries the account's current `active` feed unchanged.

        Send exactly ONE source: `keywords` (1-5), `x_list_id` or
        `x_list_url`, or `list_id`. `type` must agree with the fields you
        send. An X list is looked up live and only PUBLIC lists are accepted,
        so those calls also consume one unit of the tighter enrichment
        allowance; keyword and contact-list feeds consume none. Up to 8 feeds
        per account. Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, type]
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 40
                  description: Feed name shown in SuperX.
                type:
                  type: string
                  enum: [keywords, x_list, list]
                  description: Must match the source fields sent below.
                keywords:
                  type: array
                  minItems: 1
                  maxItems: 5
                  items:
                    type: string
                    maxLength: 100
                  description: For type keywords. Trimmed, de-duplicated case-insensitively.
                x_list_id:
                  type: string
                  description: For type x_list. A numeric X list id. Use this OR x_list_url.
                x_list_url:
                  type: string
                  description: For type x_list. A link like https://x.com/i/lists/1234567890.
                list_id:
                  type: string
                  description: For type list. A contact list id from `GET /v1/contact-lists`.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              name: "Bootstrapped SaaS"
              type: keywords
              keywords: ["bootstrapped saas", "indie hackers"]
      responses:
        "201":
          description: The created feed. `active` is false unless the account had no active feed.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/EngageFeed" }
              example:
                data:
                  id: "V1StGXR8_Z5jdHi6B-myT"
                  name: "Bootstrapped SaaS"
                  type: keywords
                  keywords: ["bootstrapped saas", "indie hackers"]
                  active: false
                  fetch_units: 1
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/FeedSourceNotFound" }
        "409": { $ref: "#/components/responses/FeedLimitReached" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/engage/feeds/{id}:
    patch:
      tags: [Engage]
      operationId: updateEngageFeed
      summary: Update an Engage feed
      description: |
        Rename a saved feed, replace what it watches, or both. Send at most
        ONE source family per call (`keywords`, `x_list_id`/`x_list_url`, or
        `list_id`); two families in one body is a 400, because the intent is
        ambiguous. The feed keeps its id and its place in the account's feed
        list, so a feed may change type and anything holding its id keeps
        working. The active feed is not changed.

        Swapping in an X list is a live lookup (public lists only) and
        consumes one unit of the enrichment allowance. Naturally idempotent,
        so there is no `Idempotency-Key` support. Works for your main account
        or any account linked to it (`account_id`); accounts shared with you
        are read-only.
      parameters:
        - name: id
          in: path
          required: true
          description: The feed id (from `GET /v1/engage/feeds`).
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 40
                keywords:
                  type: array
                  minItems: 1
                  maxItems: 5
                  items:
                    type: string
                    maxLength: 100
                  description: Replace the feed's keywords (makes it a keyword feed).
                x_list_id:
                  type: string
                  description: Point the feed at this numeric X list id. Use this OR x_list_url.
                x_list_url:
                  type: string
                  description: Point the feed at this X list link.
                list_id:
                  type: string
                  description: Point the feed at this contact list.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              name: "Bootstrapped SaaS 2026"
      responses:
        "200":
          description: The updated feed.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/EngageFeed" }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/FeedSourceNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

    delete:
      tags: [Engage]
      operationId: deleteEngageFeed
      summary: Delete an Engage feed
      description: |
        Remove a saved feed. If it was the feed the SuperX app has open, the
        first remaining feed takes over (or none, when the last feed goes).
        Posts the feed surfaced are not stored on it and are unaffected.
        Deleting the same id again returns 404 `feed_not_found`. Works for
        your main account or any account linked to it (`account_id`);
        accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          description: The feed id (from `GET /v1/engage/feeds`).
          schema: { type: string }
        - $ref: "#/components/parameters/AccountId"
      responses:
        "204":
          description: The feed was deleted.
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/FeedNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/engage/feeds/{id}/posts:
    get:
      tags: [Engage]
      operationId: getEngageFeedPosts
      summary: Get posts from an Engage feed
      description: |
        Candidate posts from one saved feed, with the author profile and
        engagement metrics you need to score them for relevance, author size
        and recency.

        Paging works through `exclude_post_ids`, not page numbers: pass the
        ids you already hold to get the next batch. There is no `pagination`
        object; `has_more` tells you whether the feed had more to give.

        `limit` applies to keyword feeds. List feeds ignore it upstream and
        return one page per fetch: about 10 posts for a member list, 20 to 25
        for an imported X list (`limit` only trims that page). The size
        follows the list's resolved kind rather than the feed's `type`, so a
        `list` feed backed by a pure imported X list pages like an `x_list`
        feed. Page list feeds with `exclude_post_ids` instead of raising
        `limit`.

        Each fetch charges the plan's feed bucket, and a list feed that
        rotates its members costs 3 units instead of 1. On a keyword feed a
        50-post page costs exactly the same as a 20-post page, so ask for
        `limit=50` a few times a day and filter on your side rather than
        polling; feeds refresh over hours, so fetching more often than
        hourly returns the same posts. Feed fetches also run a few at a time
        across all API users; on a `429` with `Retry-After`, wait and retry.
        The bucket is charged before the fetch, so a fetch that then fails
        upstream still costs its units.

        Posts returned here count as seen and are demoted in later fetches.
        On a list feed, paging with `exclude_post_ids` also advances, and
        `fresh=true` resets, the same list cursor the SuperX app uses, so
        both move the position a person browsing that feed in the app sees.
        A plain fetch after a pause resets that cursor too, matching the
        app's own session timeout.

        Read-only by design. These endpoints return feed candidates for a
        person to review; replies are written and sent by a person in the
        SuperX app, which is why there is no reply endpoint here. Sending
        spammy, automated, or AI-generated replies that read as inauthentic
        may get your X account suspended under X's inauthentic-behavior
        rules and your SuperX account terminated. AI suggestions must be
        reviewed and meaningfully edited before they are sent, and you are
        solely responsible for what you post. Reply activity is logged and
        may be audited.
      parameters:
        - name: id
          in: path
          required: true
          description: Feed id from `GET /v1/engage/feeds`.
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
        - name: limit
          in: query
          description: >-
            Posts to return, up to 50. Default 20. Applies to keyword feeds;
            list feeds return one page per fetch whatever you ask for, about
            10 posts for a member list and 20 to 25 for an imported X list.
            On a keyword feed a big page costs the same as a small one.
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
        - name: mode
          in: query
          description: Ranking for keyword feeds. `top` (default) blends quality and relevance, `latest` is pure recency.
          schema:
            type: string
            enum: [top, latest]
            default: top
        - name: fresh
          in: query
          description: Skip the short-lived result cache and refetch. Default false.
          schema:
            type: boolean
            default: false
        - name: include_replied
          in: query
          description: "Keep posts the account already replied to, flagged with `replied: true`. Default false removes them."
          schema:
            type: boolean
            default: false
        - name: exclude_post_ids
          in: query
          description: Comma-separated post ids to leave out, up to 100. This is how you page.
          schema:
            type: string
      responses:
        "200":
          description: Candidate posts from the feed.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/EngageFeedPost" }
                  has_more:
                    type: boolean
                  feed:
                    type: object
                    properties:
                      id: { type: string }
                      name: { type: string }
                      type:
                        type: string
                        enum: [keywords, list, x_list]
              example:
                data:
                  - id: "1941300000000000002"
                    text: "We doubled activation by rewriting onboarding emails."
                    created_at: "2026-09-01T09:41:00.000Z"
                    url: "https://x.com/founderhandle/status/1941300000000000002"
                    author:
                      id: "944883311"
                      username: "founderhandle"
                      name: "Founder Name"
                      description: "Bootstrapping a SaaS in public."
                      followers_count: 4210
                      following_count: 388
                      verified: true
                    metrics:
                      likes: 142
                      replies: 18
                      reposts: 9
                      bookmarks: 27
                      impressions: 21400
                    media: []
                has_more: true
                feed: { id: "feed_kw_1", name: "Bootstrapped SaaS", type: keywords }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/FeedNotFound" }
        "429": { $ref: "#/components/responses/FeedRateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/engage/mentions:
    get:
      tags: [Engage]
      operationId: getMentions
      summary: Get mentions of the account
      description: |
        The posts that @-mention the selected account right now, newest first
        (or ranked by engagement with `sort=top`), each with the post it
        replies to and one further level of ancestry.

        This reads X live rather than SuperX's stored posts, and one call
        walks several upstream pages plus the parent posts, so it charges a
        flat 3 units of the plan's feed bucket. Read a page and work from it
        rather than polling.

        By default, mentions the account has already replied to on X are left
        out. `include_replied=true` keeps them and sets `replied` on each one.
        `mention_type` separates a direct reply to one of the account's posts
        (`reply`) from any other @-mention (`mention`).

        Paging is by cursor: pass `pagination.next_cursor` back as `cursor`.

        The app's Mentions tab also hides posts you skipped or blocked there.
        That list is a UI preference stored with the app, so it is not applied
        here: the API returns every mention and lets you filter your own way.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: cursor
          in: query
          description: next_cursor from the previous page. Omit for the first page.
          schema:
            type: string
        - name: sort
          in: query
          schema:
            type: string
            enum: [latest, top]
            default: latest
        - name: include_replied
          in: query
          description: Keep mentions already replied to, flagged with `replied`.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: A page of mentions.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/MentionPost" }
                  pagination: { $ref: "#/components/schemas/CursorPagination" }
              example:
                data:
                  - id: "2097720536478446009"
                    text: "@robj3d3 how do you handle the scheduling side of this?"
                    created_at: "2026-09-09T16:15:24.000Z"
                    url: "https://x.com/activefan/status/2097720536478446009"
                    author:
                      id: "44196397001234567"
                      username: "activefan"
                      name: "Sam Porter"
                      description: "Building in public."
                      followers_count: 3120
                      following_count: 480
                      verified: false
                    metrics:
                      likes: 4
                      replies: 1
                      reposts: 0
                      bookmarks: 0
                      impressions: 940
                    media: []
                    mention_type: reply
                    parent_post:
                      id: "2097710000000000000"
                      text: "Here is how the queue works."
                      created_at: "2026-09-09T15:02:00.000Z"
                      author:
                        id: "1178367350552305665"
                        username: "robj3d3"
                        name: "Rob Hallam"
                        avatar_url: "https://pbs.twimg.com/profile_images/example_400x400.jpg"
                pagination:
                  has_more: true
                  next_cursor: "DAADDAABCgABF..."
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "409": { $ref: "#/components/responses/ProfileNotSynced" }
        "429": { $ref: "#/components/responses/FeedRateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/engage/reply-draft:
    post:
      tags: [Engage]
      operationId: draftEngageReply
      summary: Draft one reply to a post
      description: |
        Writes ONE reply draft in the account's own voice, using the same
        engine, prompts and voice settings as the Generate Reply button in the
        SuperX app.

        **The draft is text. A person posts it.** No endpoint in this API
        posts a reply, and this one does not either: it returns wording for
        someone to read, edit and send from X or from the SuperX app. Sending
        replies that read as inauthentic can get an X account suspended under
        X's inauthentic-behavior rules and a SuperX account terminated. AI
        output must be reviewed and meaningfully edited by a person before it
        is posted.

        Name the post exactly one of two ways:

        - `post_id` reads the post LIVE, so the draft sees the real text, the
          real author and the quoted post if there is one. That read costs one
          live lookup on top of the credit, from the same allowance the
          `/v1/x/*` endpoints use.
        - `post` supplies the text (and optionally the author) yourself, and
          nothing is read live.

        Add `conversation` (oldest first) when the post sits in a thread, and
        `quoted_post` when it quotes something you already have. `thoughts` is
        what the person wants the reply to convey: ask them for it rather than
        inventing an opinion on their behalf. `tone` picks the register.

        Costs AI credits, measured from the models' real cost and typically 2
        (the reply write plus the shared post-editor pass that runs after it).
        Needs a key with the `write` scope. Accounts shared with you work
        here: the draft changes nothing on the account, and the credits come
        from the key owner's pool.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplyDraftRequest" }
            examples:
              by_post_id:
                summary: Read the post live
                value:
                  post_id: "1899000000000000000"
                  thoughts: "agree, and we saw the same thing after switching onboarding"
                  tone: concise
              by_text:
                summary: Supply the post yourself
                value:
                  post:
                    text: "Most founders think they have a traffic problem."
                    author_name: "Pieter Levels"
                    author_handle: "levelsio"
                  tone: engaging
      responses:
        "200":
          description: The reply draft.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      reply:
                        type: string
                        description: The draft reply. Nothing was posted or sent.
                      post_id:
                        type: string
                        nullable: true
                        description: The post replied to, when you named one by id.
                      author_handle:
                        type: string
                        nullable: true
                        description: The author replied to, without the @, when it is known.
                  meta: { $ref: "#/components/schemas/ToolMeta" }
                  note:
                    type: string
              example:
                data:
                  reply: "had the exact same thing, it was positioning the whole time"
                  post_id: "1899000000000000000"
                  author_handle: "levelsio"
                meta:
                  credits_charged: 2
                  account_id: "1178367350552305665"
                note: "The draft is text. Nothing is posted or sent; a person reviews and posts it."
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404":
          description: No public post with that id, or it is protected or deleted.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: post_not_found
                  message: "No public post with that id."
        "422":
          description: The post was read but carries no text to reply to. Supply the text yourself with `post`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: post_has_no_text
                  message: "That post carries no text to reply to. Supply the text yourself with `post` instead."
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/media:
    post:
      tags: [Media]
      operationId: createMediaUpload
      summary: Presign an image upload
      description: |
        Registers an image upload and returns a presigned `upload_url`. PUT
        the raw file bytes to `upload_url` within 20 minutes, with the
        `Content-Type` header set to the same `file_type` you declared here.
        Then reference the returned `object_key` in `parts[].media` on the
        scheduled-posts create/update endpoints.

        Images only: `image/jpeg`, `image/png`, `image/webp` (5 MB max each)
        and `image/gif` (15 MB max). The attach step re-checks the real bytes
        (size and magic-byte type), so a mismatched upload fails at attach
        time. Uploads never attached to a post are deleted after 24 hours.

        Needs the write scope. Quota: 100 uploads per key per day, on top of
        the normal write rate limits. No `Idempotency-Key` handling: a
        duplicate presign is just an unused upload that expires.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [filename, file_type, size]
              properties:
                filename:
                  type: string
                  description: Original filename; sanitized into the object key.
                file_type:
                  type: string
                  enum: [image/jpeg, image/png, image/webp, image/gif]
                size:
                  type: integer
                  description: File size in bytes. 5 MB max (15 MB for GIF).
            example:
              filename: "chart.png"
              file_type: "image/png"
              size: 48213
      responses:
        "201":
          description: The registered upload and its presigned PUT URL.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      object_key:
                        type: string
                        description: Pass this in `parts[].media` when creating or editing a post.
                      url:
                        type: string
                        description: The public URL the file will have once uploaded.
                      upload_url:
                        type: string
                        description: Presigned PUT URL. Upload the raw bytes here with `Content-Type` equal to `file_type`.
                      expires_at:
                        type: string
                        format: date-time
                        description: When the upload_url stops working (20 minutes).
              example:
                data:
                  object_key: "u123/api_1751980800000_9f3a1c2b_chart.png"
                  url: "https://media.superx.so/u123/api_1751980800000_9f3a1c2b_chart.png"
                  upload_url: "https://<bucket-host>/u123/api_1751980800000_9f3a1c2b_chart.png?X-Amz-..."
                  expires_at: "2026-07-08T15:20:00.000Z"
        "400":
          description: Invalid parameter, unsupported type (`unsupported_media_type`; videos land here), or over the size cap (`media_too_large`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: unsupported_media_type
                  message: "file_type must be one of: image/jpeg, image/png, image/webp, image/gif. Videos are not supported yet."
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`) or a lapsed subscription (legacy string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: insufficient_scope
                  message: "This API key is read-only. Create a key with the write scope to use this endpoint."
        "429":
          description: Write rate limit (`rate_limited`) or the daily media quota (`media_quota_exceeded`, 100/day). `Retry-After` indicates the wait.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: media_quota_exceeded
                  message: "Daily media upload limit reached (100/day). Retry after the indicated delay."
        "500": { $ref: "#/components/responses/InternalError" }
        "503":
          description: Media storage is not configured (`media_not_configured`) or temporarily unavailable (`upstream_unavailable`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: media_not_configured
                  message: "Media storage is not configured on this server. Try again later."

  /v1/workers:
    get:
      tags: [Workers]
      operationId: listWorkers
      summary: List Workers
      description: |
        The account's Workers, newest first: the scheduled AI writers set up
        in the SuperX app, each with its voice, topic source, batch size,
        schedule and next run time.

        Read what they wrote with `GET /v1/workers/suggestions`. Workers are
        created, edited and run in the app, so there is no create or run
        endpoint here. Not paginated: how many Workers an account can hold is
        capped by its plan.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The account's Workers, created_at descending.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Worker" }
              example:
                data:
                  - id: 12
                    name: "Daily build in public"
                    status: "active"
                    voice_source: "my_voice"
                    voice_creator: null
                    topic_source: "product"
                    topic_config: { product_id: "aB3xWnA0kLpR7sVtYu3zc" }
                    style_instructions: null
                    use_account_rules: 1
                    format_collection: null
                    style_mode: "one_shot"
                    batch_size: 3
                    output_mode: "suggest"
                    schedule: "daily"
                    schedule_time: "09:00"
                    schedule_day: null
                    timezone: "Europe/London"
                    next_run_at: "2026-09-13T08:00:00.000Z"
                    created_at: "2026-08-01T10:00:00.000Z"
                    updated_at: "2026-09-01T10:00:00.000Z"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/workers/suggestions:
    get:
      tags: [Workers]
      operationId: listWorkerSuggestions
      summary: List Worker suggestions
      description: |
        The posts the account's Workers wrote, newest first. The default
        `status=to_review` is the app's "To review" queue: suggestions nobody
        has drafted, scheduled or dismissed yet.

        Each row carries the text, the Worker that wrote it, the timestamps of
        whatever has been done with it, and `post_id` once it was saved as a
        post (join it to `GET /v1/scheduled-posts`). `reference` says what kind
        of source the Worker wrote from; the Worker's own collection and
        reference ids stay private.

        Reading changes nothing and spends no AI credits. Act on a row with the
        draft, schedule or dismiss endpoints below.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: status
          in: query
          description: to_review (default) = waiting on a person; drafted, scheduled and dismissed = already acted on; all = every suggestion. A dismissed suggestion reads as dismissed even if it was saved first.
          schema:
            type: string
            enum: [to_review, drafted, scheduled, dismissed, all]
            default: to_review
        - name: worker_id
          in: query
          description: Narrow to one Worker by its numeric id (from `GET /v1/workers`). An unknown or foreign id returns 404 `worker_not_found`.
          schema:
            type: integer
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of suggestions, generated_at descending.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/WorkerSuggestion" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: 4821
                    worker_id: 12
                    text: "Three things we changed after watching ten people use the onboarding."
                    status: "to_review"
                    reference: { source: "collection" }
                    generated_at: "2026-09-12T08:00:00.000Z"
                    drafted_at: null
                    scheduled_at: null
                    dismissed_at: null
                    post_id: null
                  - id: 4818
                    worker_id: 12
                    text: "The onboarding rewrite doubled activation. Here is what we cut."
                    status: "drafted"
                    reference: null
                    generated_at: "2026-09-11T08:00:00.000Z"
                    drafted_at: "2026-09-11T09:14:00.000Z"
                    scheduled_at: null
                    dismissed_at: null
                    post_id: "V1StGXR8_Z5jdHi6B-myT"
                pagination: { page: 1, limit: 20, has_more: false, total: 2 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/WorkerNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/workers/suggestions/{id}/draft:
    post:
      tags: [Workers]
      operationId: draftWorkerSuggestion
      summary: Save a suggestion as a draft
      description: |
        Turn one Worker suggestion into a DRAFT post. Nothing is posted to X:
        the draft shows up in the user's Drafts in the app and at the top of
        `GET /v1/scheduled-posts?status=draft`, and the response gives you its
        `post_id` directly.

        The text is saved exactly as the Worker wrote it. To change the wording
        first, rewrite it with `POST /v1/posts/remix` or
        `POST /v1/tools/rephrase`, save your version with
        `POST /v1/scheduled-posts`, then dismiss the original.

        One suggestion can only be saved once: a second draft or schedule call
        on the same id returns 400 `already_saved`, and a dismissed one returns
        400 `already_dismissed`. Naturally one-shot, so there is no
        `Idempotency-Key` support. Works for your main account or any account
        linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - $ref: "#/components/parameters/SuggestionId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              account_id: "1178367350552305665"
      responses:
        "200":
          description: The suggestion and the draft it became.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/WorkerSuggestionDrafted" }
              example:
                data:
                  id: 4821
                  post_id: "V1StGXR8_Z5jdHi6B-myT"
                  drafted_at: "2026-09-12T09:14:00.000Z"
        "400": { $ref: "#/components/responses/SuggestionNotActionable" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/SuggestionNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/workers/suggestions/{id}/schedule:
    post:
      tags: [Workers]
      operationId: scheduleWorkerSuggestion
      summary: Schedule a suggestion
      description: |
        Turn one Worker suggestion into a SCHEDULED post. The SuperX scheduler
        posts it at `scheduled_for`, the same as any other queued post, so
        confirm the time and the text with the user before you call this.

        Afterwards it is an ordinary scheduled post: retime it with
        `PATCH /v1/scheduled-posts/{id}` or cancel it with
        `DELETE /v1/scheduled-posts/{id}`, using the `post_id` in the response.

        A Worker post never inherits the account's Default Post Settings, only
        what you send here. One suggestion can only be saved once: a second
        call returns 400 `already_saved`, a dismissed one 400
        `already_dismissed`. Naturally one-shot, so there is no
        `Idempotency-Key` support. Works for your main account or any account
        linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - $ref: "#/components/parameters/SuggestionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [scheduled_for]
              properties:
                scheduled_for:
                  type: string
                  format: date-time
                  description: When to post it. UTC ISO-8601 with an explicit Z or offset, at least 60 seconds in the future.
                autoRetweet:
                  type: object
                  nullable: true
                  description: Auto Retweet settings for this post, in the composer's own shape. Omit to leave it off.
                autoPlug:
                  type: object
                  nullable: true
                  description: Auto Plug settings for this post, in the composer's own shape. Omit to leave it off.
                autoDelete:
                  type: object
                  nullable: true
                  description: Auto Delete settings for this post, in the composer's own shape. Omit to leave it off.
                enablePostingTwitter:
                  type: boolean
                  description: Post it to X. Omit to leave the account's own behaviour unchanged.
                enablePostingBsky:
                  type: boolean
                  description: Also cross-post it to Bluesky. Omit to leave the account's own behaviour unchanged.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              scheduled_for: "2026-09-15T14:00:00Z"
      responses:
        "200":
          description: The suggestion and the scheduled post it became.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/WorkerSuggestionScheduled" }
              example:
                data:
                  id: 4821
                  post_id: "V1StGXR8_Z5jdHi6B-myT"
                  scheduled_at: "2026-09-12T09:14:00.000Z"
                  scheduled_for: "2026-09-15T14:00:00.000Z"
        "400": { $ref: "#/components/responses/SuggestionNotActionable" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/SuggestionNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/workers/suggestions/{id}/dismiss:
    post:
      tags: [Workers]
      operationId: dismissWorkerSuggestion
      summary: Dismiss a suggestion
      description: |
        Drop one Worker suggestion out of the "To review" queue. No post is
        created and no post is removed: a suggestion already saved as a draft
        or a scheduled post keeps that post, which you delete separately with
        `DELETE /v1/scheduled-posts/{id}` if that is what the user wants.

        Dismissing is allowed on any suggestion the account owns, including one
        already saved, and it still reads back under
        `GET /v1/workers/suggestions?status=dismissed`. Naturally idempotent in
        effect, so there is no `Idempotency-Key` support. Works for your main
        account or any account linked to it (`account_id`); accounts shared
        with you are read-only.
      parameters:
        - $ref: "#/components/parameters/SuggestionId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              account_id: "1178367350552305665"
      responses:
        "200":
          description: The dismissed suggestion.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/WorkerSuggestionDismissed" }
              example:
                data:
                  id: 4821
                  dismissed_at: "2026-09-12T09:20:00.000Z"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/SuggestionNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/scheduled-posts:
    get:
      tags: [Scheduling]
      operationId: listScheduledPosts
      summary: List drafts and scheduled posts
      description: |
        The account's drafts and scheduled posts, most recently updated first,
        so page 1 is the newest work. Use `page` to walk back through older
        ones rather than raising `limit`. That ordering covers every `status`,
        not just drafts: scheduled and sent posts come back most recently
        updated first too.

        A `from` or `to` filter makes this a calendar query instead: those
        results come back in post-time order, oldest first, whatever the
        status.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: status
          in: query
          description: Comma list of statuses to include. Default is all four. `draft` also covers legacy board drafts stored under internal statuses; they come back as `"draft"`.
          schema:
            type: string
            example: "scheduled,draft"
        - name: tags
          in: query
          description: Comma list of tag ids (from `GET /v1/tags`). Returns posts carrying ANY of the listed tags.
          schema:
            type: string
            example: "V1StGXR8_Z5jdHi6B-myT"
        - name: from
          in: query
          description: Include posts scheduled at or after this UTC ISO-8601 time (explicit Z or offset required).
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Include posts scheduled at or before this UTC ISO-8601 time (explicit Z or offset required).
          schema:
            type: string
            format: date-time
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of scheduled posts and drafts.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ScheduledPost" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "V1StGXR8_Z5jdHi6B-myT"
                    status: "scheduled"
                    scheduled_for: "2026-07-08T15:00:00.000Z"
                    parts:
                      - text: "Shipping day. Here is everything new in this release:"
                      - text: "1. A public API. Docs at docs.superx.so"
                    title: null
                    scratchpad: null
                    tags: []
                    created_at: "2026-07-06T09:30:00.000Z"
                  - id: "Fq2xWnA0kLpR7sVtYu3zc"
                    status: "draft"
                    scheduled_for: null
                    parts:
                      - text: "Rough idea: why most scheduling tools get threads wrong."
                    title: "Threads take"
                    scratchpad: null
                    tags:
                      - { id: "aB3xWnA0kLpR7sVtYu3zc", name: "Launch week", color: "amber" }
                    created_at: "2026-07-05T20:11:00.000Z"
                pagination: { page: 1, limit: 20, has_more: false, total: 2 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    post:
      tags: [Scheduling]
      operationId: createScheduledPost
      summary: Create a draft or scheduled post, or publish now
      description: |
        Creates a post: a draft when `scheduled_for` is omitted, a scheduled
        post otherwise. Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only. Supports the
        `Idempotency-Key` header for safe retries (see the Idempotency guide).
        Successful creation returns `201`.

        **Publishing now.** Send `scheduled_for: "now"` and the post goes to X
        immediately. This is irreversible, so the rules are tighter:

        - `Idempotency-Key` is REQUIRED (`400 invalid_parameter` without it).
          Reuse the same key on any retry: it never publishes twice.
        - `title` and `scratchpad` are rejected with `400` (a published post
          has no draft to organize), and Bluesky is never cross-posted.
        - A retry that arrives while the first attempt is still publishing
          returns `409 idempotency_in_flight` with a `Retry-After` header.
          After that window the API checks whether the first attempt landed
          and replays its result instead of posting again.
        - The `201` carries `status: "sent"` with `posted_at`, `x_post_id`
          and `url`, and `scheduled_for: null`.
        - Advanced settings and Auto DM inherit your Default Post Settings
          exactly as they do for a scheduled post.
        - A thread that fails part way through leaves no scheduled-post record
          at all, so check `GET /v1/posts` before retrying: the parts X did
          accept are already live.

        Images attach per part: `parts[].media` is an array of
        `{ object_key, alt_text? }` items, with `object_key` from
        `POST /v1/media` (uploaded bytes required first). Up to 4 images or
        exactly 1 GIF per part; `alt_text` is capped at 1,000 characters.
        Video is not supported yet.

        Advanced settings (`auto_retweet`, `auto_delete`, `auto_plug`,
        `auto_dm`, `super_followers_only`): omit a setting to inherit its
        Default Post Settings value from the SuperX app, send an object (or
        `true`/`false` for `super_followers_only`) to override, or send
        `null` to turn it off for this post. Exactly five settings inherit
        this way: auto retweet, auto delete, auto plug, auto DM, and Super
        Followers only. Other composer defaults (for example Bluesky
        cross-posting or share-with-followers) are never applied to API
        posts.

        `auto_dm` direct-messages the people who reply to or repost the post
        once it is live. Your plan caps how many posts a month may carry one
        (`posts_with_auto_dm` in `GET /v1/dm/limits`): when that cap stops
        it, the post is still created and the response carries
        `auto_dm_skipped: true` instead of failing. Reading a post back shows
        that an auto DM is attached but never its message text.
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: >-
            Unique key (max 64 characters) for safe retries. Replays carry the
            "Idempotency-Replayed" response header set to "true". Keys are
            retained for 24 hours.
          schema:
            type: string
            maxLength: 64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                text:
                  type: string
                  description: The post text (single post). Provide either `text` or `parts`.
                parts:
                  type: array
                  description: Thread parts, 1 to 25 items. Total text across parts is limited to 25,000 characters.
                  items:
                    type: object
                    required: [text]
                    properties:
                      text:
                        type: string
                      media:
                        type: array
                        maxItems: 4
                        description: Images for this part (up to 4, or exactly 1 GIF). `object_key` from `POST /v1/media`.
                        items:
                          type: object
                          required: [object_key]
                          properties:
                            object_key:
                              type: string
                            alt_text:
                              type: string
                              maxLength: 1000
                              description: Accessibility description delivered to X with the image.
                scheduled_for:
                  description: >-
                    UTC ISO-8601 with explicit Z or offset: at least 60
                    seconds in the future, at most 18 months out. Omit to
                    create a draft. The literal string `"now"` publishes to X
                    immediately and requires an `Idempotency-Key` (see the
                    operation description).
                  oneOf:
                    - type: string
                      format: date-time
                    - type: string
                      enum: ["now"]
                title:
                  type: string
                  maxLength: 300
                  description: Draft title shown in the SuperX app. Organizational only, never posted.
                scratchpad:
                  type: string
                  maxLength: 30000
                  description: Private working notes attached to the post. Never posted.
                tags:
                  type: array
                  maxItems: 20
                  description: Tag ids to assign (from `GET /v1/tags`). Unknown ids fail the whole request with `400 unknown_tag` before anything is created.
                  items:
                    type: string
                auto_retweet:
                  type: object
                  nullable: true
                  description: Auto retweet. Omit to inherit your Default Post Settings; `null` turns it off for this post.
                  required: [after_hours]
                  properties:
                    after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                      description: Retweet the post this many hours after it goes live.
                    remove_after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                      description: Remove the retweet this many hours later. Omit to keep it.
                auto_delete:
                  type: object
                  nullable: true
                  description: Auto delete underperforming posts. Omit to inherit your defaults; `null` turns it off for this post.
                  required: [after_hours]
                  properties:
                    after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                      description: Delete the post this many hours after it goes live if it is under the views threshold.
                    threshold:
                      type: integer
                      minimum: 0
                      description: Views threshold (default 1000).
                auto_plug:
                  type: object
                  nullable: true
                  description: "Auto plug: reply with a template once the post hits a likes threshold. Omit to inherit your defaults; `null` turns it off for this post. Unknown template ids fail with `400 unknown_plug_template`."
                  required: [template_id, threshold]
                  properties:
                    template_id:
                      type: string
                      description: Plug template id (from `GET /v1/plug-templates`).
                    threshold:
                      type: integer
                      minimum: 1
                      description: Likes threshold that triggers the plug reply.
                auto_dm:
                  allOf:
                    - $ref: "#/components/schemas/AutoDm"
                  description: "Auto DM: message the people who reply to or repost this post once it is live. Omit to inherit your defaults; `null` turns it off for this post."
                super_followers_only:
                  type: boolean
                  description: Post to Super Followers only. Omit to inherit your defaults.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            examples:
              single:
                summary: One scheduled post
                value:
                  text: "Shipping day."
                  scheduled_for: "2026-07-08T15:00:00Z"
              thread:
                summary: A thread draft
                value:
                  parts:
                    - text: "Why most scheduling tools get threads wrong:"
                    - text: "They treat a thread as one blob of text instead of parts."
              organized:
                summary: A draft with title and tags
                value:
                  text: "Rough cut of the launch teaser."
                  title: "Launch teaser v1"
                  tags: ["V1StGXR8_Z5jdHi6B-myT"]
              with_image:
                summary: A scheduled post with an image
                value:
                  parts:
                    - text: "Chart of the week."
                      media:
                        - object_key: "u123/api_1751980800000_9f3a1c2b_chart.png"
                          alt_text: "Line chart of weekly revenue trending up"
                  scheduled_for: "2026-07-08T15:00:00Z"
              advanced:
                summary: Explicit advanced settings
                value:
                  text: "Shipping day."
                  scheduled_for: "2026-07-08T15:00:00Z"
                  auto_retweet: { after_hours: 6, remove_after_hours: 4 }
                  auto_plug: { template_id: "tpl_abc123", threshold: 50 }
                  auto_delete: null
              publish_now:
                summary: Publish immediately (Idempotency-Key required)
                value:
                  text: "Shipping now."
                  scheduled_for: "now"
      responses:
        "201":
          description: >-
            The created post (echo of the accepted request, not the stored
            row). Replayed idempotent requests also return this body with the
            "Idempotency-Replayed" header set to "true". Applied advanced
            settings, inherited or explicit, are NOT echoed here; read them
            back via GET /v1/scheduled-posts, adding `?status=sent` for a post
            published with `scheduled_for: "now"`.
          headers:
            Idempotency-Replayed:
              description: Present and "true" when this response was replayed from a previous request with the same Idempotency-Key.
              schema:
                type: string
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      status:
                        type: string
                        enum: [draft, scheduled, sent]
                        description: '`sent` only for `scheduled_for: "now"`.'
                      scheduled_for:
                        type: string
                        format: date-time
                        nullable: true
                        description: 'Null for a draft and for a post published with `scheduled_for: "now"`.'
                      parts:
                        type: array
                        items:
                          type: object
                          properties:
                            text:
                              type: string
                            media:
                              type: array
                              description: Present only when the part carries media.
                              items:
                                type: object
                                properties:
                                  object_key:
                                    type: string
                                  url:
                                    type: string
                                  file_type:
                                    type: string
                                  size:
                                    type: integer
                                    nullable: true
                                  alt_text:
                                    type: string
                                    nullable: true
                      title:
                        type: string
                        nullable: true
                      scratchpad:
                        type: string
                        nullable: true
                      tags:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            name:
                              type: string
                            color:
                              type: string
                      created_at:
                        type: string
                        format: date-time
                      posted_at:
                        type: string
                        format: date-time
                        nullable: true
                        description: 'When the post went out. Present only for `scheduled_for: "now"`.'
                      x_post_id:
                        type: string
                        nullable: true
                        description: 'The X post id. Present only for `scheduled_for: "now"`.'
                      url:
                        type: string
                        nullable: true
                        description: 'Permalink to the post on X. Present only for `scheduled_for: "now"`.'
                      auto_dm_skipped:
                        type: boolean
                        description: >-
                          Present (and true) only when a plan limit stripped
                          the inherited Auto DM from this post. Absent
                          otherwise, and always absent from a result the API
                          recovered after an interrupted publish (the flag is
                          not stored on the post), so treat its absence as
                          "unknown" on a retried request.
              example:
                data:
                  id: "V1StGXR8_Z5jdHi6B-myT"
                  status: "scheduled"
                  scheduled_for: "2026-07-08T15:00:00.000Z"
                  parts:
                    - text: "Shipping day."
                  title: null
                  scratchpad: null
                  tags: []
                  created_at: "2026-07-06T10:00:00.000Z"
        "400":
          description: >-
            Invalid parameter, invalid request, unknown tag id (`unknown_tag`),
            unknown plug template id (`unknown_plug_template`),
            or a media problem: unknown or foreign `object_key`
            (`invalid_media`), bytes never uploaded (`media_not_uploaded`),
            not a supported image (`unsupported_media_type`), or over the size
            cap (`media_too_large`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: invalid_media
                  message: "Unknown object_key: u123/api_..._chart.png. Upload media first via POST /v1/media."
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: |
            Read-only key (`insufficient_scope`), an account shared with you
            (`writes_main_account_only`; shared accounts are read-only through
            the API, your own linked accounts are not), onboarding not finished
            (`onboarding_required`), post quota used up
            (`post_quota_exceeded`), the connected X account needing to be
            re-authorized in the SuperX app (`reauth_required`, publishing
            only), or a lapsed subscription (legacy string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: writes_main_account_only
                  message: "Accounts shared with you are read-only through the API. Writes work on your own main and linked accounts."
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "409":
          description: >-
            The Idempotency-Key was already used with a different request body
            (`idempotency_key_reuse`); or, when publishing now, an earlier
            request with the same key is still publishing
            (`idempotency_in_flight`, with a `Retry-After` header: wait, then
            retry the SAME key) or the post has already been published
            (`post_already_published`).
          headers:
            Retry-After:
              description: Seconds to wait before retrying, on `idempotency_in_flight`.
              schema:
                type: integer
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: idempotency_key_reuse
                  message: "This Idempotency-Key was already used with a different request body."
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/scheduled-posts/bulk/retime:
    post:
      tags: [Scheduling]
      operationId: bulkRetimeScheduledPosts
      summary: Retime queued posts in bulk
      description: |
        Moves up to 500 queued posts to new times in ONE transaction: either
        every move lands or none do.

        Only posts in the queue move. Drafts, sent posts and error rows are
        counted in `skipped` and are never retimed. Each `scheduled_for`
        follows the same window as a single create: at least 60 seconds in the
        future, at most 18 months out.

        Costs one write. `Idempotency-Key` is not supported and not needed:
        re-running the same moves converges on the same result.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [moves]
              properties:
                moves:
                  type: array
                  minItems: 1
                  maxItems: 500
                  description: The moves to apply. Post ids must be unique within one call.
                  items:
                    type: object
                    required: [id, scheduled_for]
                    properties:
                      id:
                        type: string
                        description: Post id (from `GET /v1/scheduled-posts`).
                      scheduled_for:
                        type: string
                        format: date-time
                        description: New time, UTC ISO-8601 with explicit Z or offset.
                account_id:
                  type: string
                  description: Any account you own. An account shared with you returns `403 writes_main_account_only`.
            example:
              moves:
                - id: "V1StGXR8_Z5jdHi6B-myT"
                  scheduled_for: "2026-09-08T15:00:00Z"
                - id: "Fq2xWnA0kLpR7sVtYu3zc"
                  scheduled_for: "2026-09-08T18:30:00Z"
      responses:
        "200":
          description: How many posts moved and how many were skipped.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      updated:
                        type: integer
                      skipped:
                        type: integer
              example:
                data: { updated: 2, skipped: 0 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/scheduled-posts/bulk/auto-retweet:
    post:
      tags: [Scheduling]
      operationId: bulkEnableAutoRetweet
      summary: Enable auto retweet on queued posts in bulk
      description: |
        Turns Auto Retweet on for up to 100 queued posts at once.

        A post that already carries its own auto retweet is counted in
        `skipped` and left untouched: this never overwrites a per-post
        setting. Only posts in the queue are changed, so drafts, sent posts
        and error rows are counted in `skipped` too. `failed` counts posts
        whose stored settings could not be read.

        Worth knowing: posts created through the API inherit the account's
        Default Post Settings, so if Auto Retweet is on there, every API post
        already carries one and this endpoint reports them all as `skipped`.
        To bulk-apply a different auto retweet, create the posts with
        `auto_retweet: null` (or clear each one with
        `PATCH /v1/scheduled-posts/{id}`) first.

        Costs one write. `Idempotency-Key` is not supported and not needed:
        a second run skips everything the first one set.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ids, auto_retweet]
              properties:
                ids:
                  type: array
                  minItems: 1
                  maxItems: 100
                  description: Post ids (from `GET /v1/scheduled-posts`). Repeated ids are deduplicated.
                  items:
                    type: string
                auto_retweet:
                  type: object
                  required: [after_hours]
                  description: The auto retweet to apply to every listed post.
                  properties:
                    after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                      description: Retweet each post this many hours after it goes live.
                    remove_after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                      description: Remove the retweet this many hours later. Omit to keep it.
                account_id:
                  type: string
                  description: Any account you own. An account shared with you returns `403 writes_main_account_only`.
            example:
              ids: ["V1StGXR8_Z5jdHi6B-myT", "Fq2xWnA0kLpR7sVtYu3zc"]
              auto_retweet: { after_hours: 6, remove_after_hours: 4 }
      responses:
        "200":
          description: How many posts were changed, skipped and failed.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      updated:
                        type: integer
                      skipped:
                        type: integer
                      failed:
                        type: integer
              example:
                data: { updated: 1, skipped: 1, failed: 0 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/scheduled-posts/bulk/delete:
    post:
      tags: [Scheduling]
      operationId: bulkDeleteScheduledPosts
      summary: Delete queued posts in bulk
      description: |
        Deletes up to 100 QUEUED posts at once and refunds their post quota.
        `POST`, not `DELETE`, because the ids travel in a body.

        Only posts in the queue are deleted: a leaked id for a sent post
        cannot wipe history, and drafts are left alone (delete those with
        `DELETE /v1/scheduled-posts/{id}`). `deleted` can therefore be lower
        than the number of ids you sent.

        Costs one write. `Idempotency-Key` is not supported and not needed:
        deleting an already-deleted id is a no-op.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ids]
              properties:
                ids:
                  type: array
                  minItems: 1
                  maxItems: 100
                  description: Post ids (from `GET /v1/scheduled-posts`). Repeated ids are deduplicated.
                  items:
                    type: string
                account_id:
                  type: string
                  description: Any account you own. An account shared with you returns `403 writes_main_account_only`.
            example:
              ids: ["V1StGXR8_Z5jdHi6B-myT", "Fq2xWnA0kLpR7sVtYu3zc"]
      responses:
        "200":
          description: How many posts were deleted.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      deleted:
                        type: integer
              example:
                data: { deleted: 2 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/scheduled-posts/{id}:
    patch:
      tags: [Scheduling]
      operationId: updateScheduledPost
      summary: Edit a draft or scheduled post
      description: |
        Partial update: only the fields you send change; everything else on
        the post (including fields the API does not expose) is preserved.
        Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only.

        Status transitions are explicit: sending `scheduled_for` alone never
        schedules a draft. Set `status` to `"scheduled"` to promote (needs a
        future time, provided or already stored: at least 60 seconds ahead,
        at most 18 months) or `"draft"` to pull a post back out of the queue.
        Both directions ride the normal post-quota deduct/refund.

        `parts` is a FULL replace, media included: a part sent without
        `media` drops any media it carried. Re-include the current
        `object_key`s (visible on GET responses) to keep them, add new ones
        from `POST /v1/media`, or omit `parts` entirely to edit only
        title/scratchpad/time/status without touching text or media.
        No `Idempotency-Key` handling: PATCH is naturally idempotent.
        Concurrent edits are last-write-wins.

        Advanced settings (`auto_retweet`, `auto_delete`, `auto_plug`,
        `auto_dm`, `super_followers_only`) use the same shapes as create but
        never inherit defaults on update: provided = override, omitted = keep
        the post's current setting, `null` = remove it. `auto_dm: null` also
        gives back the month's posts-with-auto-DM slot; setting one consumes
        a slot.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                text:
                  type: string
                  description: Replacement text (single post). Provide either `text` or `parts`, not both.
                parts:
                  type: array
                  description: Replacement parts (full replace, media included), 1 to 25 items, 25,000 characters total. A part without `media` drops the media it carried.
                  items:
                    type: object
                    required: [text]
                    properties:
                      text:
                        type: string
                      media:
                        type: array
                        maxItems: 4
                        description: Images for this part (up to 4, or exactly 1 GIF). Re-list existing `object_key`s to keep them; new ones come from `POST /v1/media`.
                        items:
                          type: object
                          required: [object_key]
                          properties:
                            object_key:
                              type: string
                            alt_text:
                              type: string
                              maxLength: 1000
                scheduled_for:
                  type: string
                  format: date-time
                  description: New time, UTC ISO-8601 with explicit Z or offset. On its own it never promotes a draft.
                status:
                  type: string
                  enum: [draft, scheduled]
                  description: Explicit transition. `scheduled` requires a future time (via `scheduled_for` or already stored).
                title:
                  type: string
                  maxLength: 300
                  nullable: true
                  description: New title; `null` clears it.
                scratchpad:
                  type: string
                  maxLength: 30000
                  nullable: true
                  description: New notes; `null` clears them.
                tags:
                  type: array
                  maxItems: 20
                  description: Full replacement tag id set. `[]` clears all tags. Unknown ids fail with `400 unknown_tag`.
                  items:
                    type: string
                auto_retweet:
                  type: object
                  nullable: true
                  description: Auto retweet override; `null` removes it. Omit to keep the current setting.
                  required: [after_hours]
                  properties:
                    after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                    remove_after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                auto_delete:
                  type: object
                  nullable: true
                  description: Auto delete override; `null` removes it. Omit to keep the current setting.
                  required: [after_hours]
                  properties:
                    after_hours:
                      type: integer
                      minimum: 1
                      maximum: 12
                    threshold:
                      type: integer
                      minimum: 0
                      description: Views threshold (default 1000).
                auto_plug:
                  type: object
                  nullable: true
                  description: Auto plug override; `null` removes it. Omit to keep the current setting. Unknown template ids fail with `400 unknown_plug_template`.
                  required: [template_id, threshold]
                  properties:
                    template_id:
                      type: string
                      description: Plug template id (from `GET /v1/plug-templates`).
                    threshold:
                      type: integer
                      minimum: 1
                auto_dm:
                  allOf:
                    - $ref: "#/components/schemas/AutoDm"
                  description: "Auto DM override; `null` removes it and gives back the month's slot. Omit to keep the current setting. When a plan limit stops a new one from attaching, the edit still lands and the response carries `auto_dm_skipped: true`."
                super_followers_only:
                  type: boolean
                  description: Post to Super Followers only. Omit to keep the current setting.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            examples:
              retitle:
                summary: Change only the title
                value:
                  title: "Launch teaser v2"
              remove_auto_retweet:
                summary: Remove the post's auto retweet
                value:
                  auto_retweet: null
              promote:
                summary: Promote a draft to the queue
                value:
                  scheduled_for: "2026-08-01T15:00:00Z"
                  status: "scheduled"
              retag:
                summary: Replace the tag set
                value:
                  tags: ["V1StGXR8_Z5jdHi6B-myT"]
      responses:
        "200":
          description: The full updated post.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/ScheduledPost"
                      - type: object
                        properties:
                          auto_dm_skipped:
                            type: boolean
                            description: >-
                              Present (and true) only when a plan limit
                              stopped this edit from attaching an Auto DM.
                              The edit itself landed; the post simply carries
                              no Auto DM.
              example:
                data:
                  id: "Fq2xWnA0kLpR7sVtYu3zc"
                  status: "draft"
                  scheduled_for: null
                  parts:
                    - text: "Rough idea: why most scheduling tools get threads wrong."
                  title: "Launch teaser v2"
                  scratchpad: null
                  tags: []
                  created_at: "2026-07-05T20:11:00.000Z"
        "400":
          description: >-
            Invalid parameter, unknown tag id (`unknown_tag`), unknown plug
            template id (`unknown_plug_template`), a missing future
            time for `status:"scheduled"`, or a media problem: unknown or
            foreign `object_key` (`invalid_media`), bytes never uploaded
            (`media_not_uploaded`), not a supported image
            (`unsupported_media_type`), or over the size cap
            (`media_too_large`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: invalid_media
                  message: "Unknown object_key: u123/api_..._chart.png. Upload media first via POST /v1/media."
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an account shared with you (`writes_main_account_only`; shared accounts are read-only through the API, your own linked accounts are not), or a lapsed subscription (legacy string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: insufficient_scope
                  message: "This API key is read-only. Create a key with the write scope to use this endpoint."
        "404":
          description: No scheduled post with that id belongs to this account.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: not_found
                  message: "No scheduled post with that id belongs to this account."
        "409":
          description: The post is not editable (already sent or errored).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: post_not_editable
                  message: "Only drafts and scheduled posts can be edited."
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    delete:
      tags: [Scheduling]
      operationId: deleteScheduledPost
      summary: Delete a draft or scheduled post
      description: Deletes the post and refunds any quota it held. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: Deleted.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      deleted:
                        type: boolean
              example:
                data:
                  id: "V1StGXR8_Z5jdHi6B-myT"
                  deleted: true
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an account shared with you (`writes_main_account_only`; shared accounts are read-only through the API, your own linked accounts are not), or a lapsed subscription (legacy string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: insufficient_scope
                  message: "This API key is read-only. Create a key with the write scope to use this endpoint."
        "404":
          description: No scheduled post with that id belongs to this account.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: not_found
                  message: "No scheduled post with that id belongs to this account."
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/plug-templates:
    get:
      tags: [Scheduling]
      operationId: listPlugTemplates
      summary: List plug templates
      description: >-
        Lists your auto-plug reply templates (authored in the SuperX app
        under Default Post Settings). Pass a template id as
        `auto_plug.template_id` when creating or updating a scheduled post.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: All plug templates.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/PlugTemplate" }
              example:
                data:
                  - id: "tpl_abc123"
                    text: "Enjoyed this? Follow for more, and grab the full guide here:"
                    has_media: false
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/context:
    get:
      tags: [Context]
      operationId: getContext
      summary: Get context settings
      description: >-
        The account's Context settings in one document: the background
        SuperX's AI uses when writing for the account. `profile_description`
        grounds the voice and personalizes the daily content mix and search;
        `interests` are the highest-priority content topics; `rules` are
        mandatory instructions on every AI surface; `reply` steers generated
        replies; `voice.favorite_creators` (max 3) inspire the writing style;
        `style_guide` holds the manual overrides plus the read-only generated
        guide; `products` (max 5) are mentioned naturally in generated
        content.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The context document.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Context" }
              example:
                data:
                  profile_description:
                    text: "Indie hacker building SuperX, an X growth platform."
                    enabled: true
                  interests: ["indie hacking", "SaaS", "AI agents"]
                  rules: "Never use hashtags. Keep posts under 200 characters."
                  reply:
                    custom_instructions: "Be helpful, never salesy."
                    include_author_name: false
                  voice:
                    favorite_creators:
                      - screen_name: "levelsio"
                        id: "1577241624"
                        name: "Pieter Levels"
                        profile_image_url_https: "https://pbs.twimg.com/profile_images/example.jpg"
                    use_own_posts_as_examples: true
                  style_guide:
                    audience_override: null
                    vocabulary_override: null
                    generated:
                      audience: "Bootstrapped SaaS founders and indie hackers."
                      vocabulary: "Plain, direct, numbers-first."
                      generated_at: "2026-07-20T09:00:00.000Z"
                  products:
                    - id: 3
                      url: "https://superx.so"
                      name: "SuperX"
                      description: "X growth platform"
                      positioning: null
                      features: null
                      updates: null
                      last_scraped_at: "2026-07-25T12:00:00.000Z"
                      created_at: "2026-07-01T09:00:00.000Z"
                      updated_at: "2026-07-25T12:00:00.000Z"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    patch:
      tags: [Context]
      operationId: updateContext
      summary: Update context settings
      description: >-
        Edit any subset of the Context settings; only the fields you send
        change. Strings accept `null` to clear (style-guide overrides then
        revert to the generated guide). Arrays fully REPLACE the stored list:
        send every value the account should keep, `[]` clears.
        `voice.favorite_creators` takes X usernames (with or without @, as
        bare strings or as the { screen_name } objects GET returns); SuperX
        resolves names and avatars. Unlike most write endpoints, context
        writes accept `account_id` for any linked or shared account; on a
        share with `editor` permission they return `403 editor_restricted`,
        because only the account owner may change what the AI knows. These
        settings shape all future AI output for the account. Returns the full
        updated document.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                profile_description:
                  type: object
                  properties:
                    text:
                      type: string
                      maxLength: 500
                      nullable: true
                    enabled:
                      type: boolean
                interests:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    maxLength: 50
                rules:
                  type: string
                  maxLength: 500
                  nullable: true
                reply:
                  type: object
                  properties:
                    custom_instructions:
                      type: string
                      maxLength: 500
                      nullable: true
                    include_author_name:
                      type: boolean
                voice:
                  type: object
                  properties:
                    favorite_creators:
                      type: array
                      maxItems: 3
                      items:
                        oneOf:
                          - type: string
                            description: X username, with or without the leading @.
                          - type: object
                            required: [screen_name]
                            properties:
                              screen_name: { type: string }
                            description: GET output round-trips; fields other than screen_name are ignored and re-derived.
                    use_own_posts_as_examples:
                      type: boolean
                style_guide:
                  type: object
                  properties:
                    audience_override:
                      type: string
                      maxLength: 600
                      nullable: true
                    vocabulary_override:
                      type: string
                      maxLength: 1000
                      nullable: true
                account_id:
                  type: string
            example:
              rules: "Never use hashtags. Keep posts under 200 characters."
              interests: ["indie hacking", "SaaS", "AI agents"]
              style_guide:
                audience_override: null
      responses:
        "200":
          description: >-
            The full updated context document. If the save landed but the
            read-back failed, `data` is null and `note` says to fetch
            GET /v1/context for the updated document.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    oneOf:
                      - $ref: "#/components/schemas/Context"
                      - type: "null"
                  note:
                    type: string
                    description: Present only when data is null (save succeeded, read-back failed).
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ContextWriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/context/products:
    put:
      tags: [Context]
      operationId: setContextProducts
      summary: Replace the product list
      description: >-
        Reconciles the account's products to exactly the submitted list (max
        5). CAUTION: this is a FULL REPLACE by url - any existing product
        whose url is missing from the payload is removed. To edit one product
        without touching the others, use PATCH /v1/context/products/{id}.
        Removal is reversible: re-adding the same url restores the product
        with its scraped details intact. Fields are written only when
        present; `null` clears a field.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [products]
              properties:
                products:
                  type: array
                  maxItems: 5
                  items:
                    type: object
                    required: [url]
                    properties:
                      url:
                        type: string
                        maxLength: 2048
                        description: http(s) product url; the upsert key.
                      name: { type: string, maxLength: 200, nullable: true }
                      description: { type: string, maxLength: 1000, nullable: true }
                      positioning: { type: string, maxLength: 2000, nullable: true }
                      features: { type: string, maxLength: 2000, nullable: true }
                      updates: { type: string, maxLength: 2000, nullable: true }
                account_id:
                  type: string
            example:
              products:
                - url: "https://superx.so"
                  name: "SuperX"
                  description: "X growth platform"
      responses:
        "200":
          description: The full product list after the reconcile.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/ContextProduct" }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ContextWriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/context/products/{id}:
    patch:
      tags: [Context]
      operationId: updateContextProduct
      summary: Add or edit one product
      description: >-
        Edits one product without touching the others. `{id}` is either a
        numeric product id (from GET /v1/context) or a URL-encoded product
        url. By url the call upserts: it edits the product with that url or
        creates it when it does not exist yet (cap 5) - this is the
        single-product create path. Only the fields you send change; `null`
        clears a field.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
          description: Numeric product id, or a URL-encoded http(s) product url.
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string, maxLength: 200, nullable: true }
                description: { type: string, maxLength: 1000, nullable: true }
                positioning: { type: string, maxLength: 2000, nullable: true }
                features: { type: string, maxLength: 2000, nullable: true }
                updates: { type: string, maxLength: 2000, nullable: true }
                account_id: { type: string }
            example:
              updates: "Shipped the public API"
      responses:
        "200":
          description: The saved product.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ContextProduct" }
        "400":
          description: Invalid field value (`invalid_parameter`), or the 5-product cap is reached on a create-by-url (`product_limit`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: product_limit
                  message: "A maximum of 5 products is allowed. Remove one first."
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ContextWriteForbidden" }
        "404": { $ref: "#/components/responses/ProductNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    delete:
      tags: [Context]
      operationId: deleteContextProduct
      summary: Remove one product
      description: >-
        Removes one product by its numeric id (from GET /v1/context).
        Reversible: re-adding the same url via PATCH or PUT restores the
        product with its scraped details intact.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
          description: Numeric product id.
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: Deletion confirmation.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id: { type: integer }
                      deleted: { type: boolean }
              example:
                data:
                  id: 3
                  deleted: true
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ContextWriteForbidden" }
        "404": { $ref: "#/components/responses/ProductNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/context/style-guide/regenerate:
    post:
      tags: [Context]
      operationId: regenerateStyleGuide
      summary: Rebuild the generated style guide
      description: |
        Rewrites the account's GENERATED style guide from its recent posts,
        the same way the Style Guide button on the SuperX app's Context page
        does. The result is exactly the `style_guide.generated` block
        `GET /v1/context` returns.

        Your manual overrides (`style_guide.audience_override` and
        `vocabulary_override`) are untouched and keep outranking the
        generated guide everywhere.

        Free: no AI credits are charged, so no `X-Credits-Charged` header is
        sent. `X-Credits-Remaining` and `X-Credits-Reset` still are.

        Three ceilings apply, all reported as `429 ai_action_limited`.
        Regeneration is limited to once an hour per account
        (`scope: "account"`; the hour runs from the last SUCCESSFUL run). An
        account with fewer than 5 recent posts stored in SuperX has them read
        live instead: that live read is limited to 3 attempts per account per
        day (`scope: "account"`, resets at midnight UTC) and also draws on the
        platform-wide fair-use ceiling shared by every account
        (`scope: "platform"`). An account with enough stored posts meets
        neither of the last two. Takes up to a minute.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id: { type: string }
      responses:
        "200":
          description: The rewritten style guide.
          headers:
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      generated: { $ref: "#/components/schemas/StyleGuideGenerated" }
                  meta:
                    type: object
                    properties:
                      account_id: { type: string }
              example:
                data:
                  generated:
                    audience: "Indie founders and solo builders shipping in public."
                    vocabulary: "Lowercase starts on ~70% of posts. Emojis on ~10%."
                    generated_at: "2026-09-10T12:00:00.000Z"
                meta:
                  account_id: "1178367350552305665"
        "400":
          description: The account does not have enough recent posts to read a style from (`not_enough_posts`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: not_enough_posts
                  message: "This account needs at least 5 recent posts before a style guide can be written from them."
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/context/products/{id}/scrape:
    post:
      tags: [Context]
      operationId: scrapeContextProduct
      summary: Refresh a product from its page
      description: |
        Re-reads a saved product's page and refreshes its stored name,
        description and details, the same way the SuperX app's Context page
        does. The url comes from the SAVED product, never from the request:
        change it with `PATCH /v1/context/products/{id}` first if it moved.

        Free: no AI credits are charged, so no `X-Credits-Charged` header is
        sent. The read runs on the account's allowance of 20 page reads a
        day, shared with the SuperX app, so an exhausted allowance returns
        `429 ai_action_limited` with `scope: "account"`. Takes up to a minute.

        Returns the refreshed product, with a new `last_scraped_at`.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
          description: Numeric product id, from GET /v1/context.
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id: { type: string }
      responses:
        "200":
          description: The refreshed product.
          headers:
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/ContextProduct" }
                  meta:
                    type: object
                    properties:
                      account_id: { type: string }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ContextWriteForbidden" }
        "404":
          description: No product with that id belongs to this account (`product_not_found`), or account_id is not one of your accounts (`account_not_found`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: product_not_found
                  message: "No product with that id belongs to this account."
        "422":
          description: The product page could not be read (`scrape_failed`). The product is unchanged apart from the attempt being recorded.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: scrape_failed
                  message: "This page could not be read. Check the product url and try again."
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/dm/campaigns:
    post:
      tags: [DMs]
      operationId: queueDmCampaign
      summary: Queue a DM campaign
      description: |
        Queues direct messages to up to 100 X users.

        NOTHING IS SENT BY THIS CALL. The messages go into the account's own
        DM queue, the same one the SuperX app fills, and the app's scheduler
        sends them within the account's daily and monthly DM limits. The 201
        is therefore COUNTS, not deliveries: read
        `GET /v1/dm/campaigns/{id}` to see what has actually gone out.

        `[name]`, `[first]` and `[handle]` in the message are filled in per
        recipient. A recipient may carry its own `message`, which wins over
        the shared one; when every recipient has one, the top-level `message`
        may be omitted.

        People this account already messaged in the last 24 hours are skipped
        and counted in `duplicates`, and the sending account is never
        messaged. `spread: false` (the default) queues only what today's
        remaining daily allowance holds and skips the rest; `spread: true`
        places the rest over the coming days, up to 30 days ahead.

        When EVERY recipient is skipped, nothing is queued: the answer is
        `200` with `data.id: null` and a note saying so, rather than a `201`
        carrying an id that would resolve to nothing.

        Costs no AI credits. It counts against the writes rate-limit bucket
        and against the account's own DM allowances
        (`GET /v1/dm/limits`). Takes up to 30 seconds.

        With an `Idempotency-Key`, the campaign id IS the reservation id, so a
        retry after a timeout converges on the same campaign instead of
        orphaning the rows the first attempt queued.

        You are responsible for these messages under X's rules on automation
        and unsolicited contact.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [recipients]
              properties:
                recipients:
                  type: array
                  minItems: 1
                  maxItems: 100
                  items:
                    type: object
                    required: [x_user_id]
                    properties:
                      x_user_id:
                        type: string
                        pattern: "^[0-9]{1,25}$"
                        description: The recipient's X user id (digits).
                      handle:
                        type: string
                        maxLength: 15
                        description: The recipient's @handle, used to fill `[handle]`.
                      name:
                        type: string
                        maxLength: 100
                        description: The recipient's display name, used to fill `[name]` and `[first]`.
                      message:
                        type: string
                        minLength: 1
                        maxLength: 1000
                        description: This recipient's own message, instead of the shared one.
                      source_post_id:
                        type: string
                        pattern: "^[0-9]{1,25}$"
                        description: The post this person was found on, so later imports of it can skip them.
                message:
                  type: string
                  minLength: 1
                  maxLength: 1000
                  description: The shared message. Required unless every recipient carries its own.
                spread:
                  type: boolean
                  default: false
                  description: "Spread what today's daily allowance cannot hold over the coming days instead of skipping it. Ignored when `GET /v1/dm/limits` reports `scheduling_enabled: false`; the overflow is then counted in `skipped`."
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              recipients:
                - x_user_id: "44196397"
                  handle: "levelsio"
                  name: "Pieter Levels"
              message: "Hey [first], loved your thread on shipping fast."
      responses:
        "200":
          description: Nothing was queued because every recipient was skipped. `data.id` is null.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/DmCampaign" }
                  note: { type: string }
              example:
                data:
                  id: null
                  queued: 0
                  queued_now: 0
                  scheduled: []
                  scheduled_total: 0
                  skipped: 2
                  duplicates: 2
                  remaining_this_month: 450
                note: "Every recipient was skipped (already queued in the last 24 hours, over quota, or the sender); nothing was queued."
        "201":
          description: The campaign was queued. Nothing has been sent.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
            Idempotency-Replayed:
              description: Present and "true" when this response was replayed from a previous request with the same Idempotency-Key.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/DmCampaign" }
                  note:
                    type: string
                    description: A reminder that nothing was sent by this call.
              example:
                data:
                  id: "V1StGXR8_Z5jdHi6B-myT"
                  queued: 2
                  queued_now: 2
                  scheduled: []
                  scheduled_total: 0
                  skipped: 0
                  duplicates: 0
                  remaining_this_month: 448
                note: "Queued into your DM pipeline. Nothing is sent by this call; the scheduler sends within your daily and monthly limits. You are responsible for these messages under X's automation rules."
        "400":
          description: A parameter is invalid (`invalid_parameter`), or the account has no X account connected (`account_not_linked`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: account_not_linked
                  message: "This account has no X account connected, so it cannot send DMs."
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: The key is read-only (`insufficient_scope`), the account is shared with you (`writes_main_account_only`), the plan has no DM allowance (`dm_not_in_plan`), or the X account must be reconnected in the app (`reauth_required`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: dm_not_in_plan
                  message: "This account's plan does not include DM sending."
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "409":
          description: The Idempotency-Key was already used with a different request body.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: idempotency_key_reuse
                  message: "This Idempotency-Key was already used with a different request body."
        "429": { $ref: "#/components/responses/DmLimitReached" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/dm/campaigns/{id}:
    get:
      tags: [DMs]
      operationId: getDmCampaign
      summary: Read one DM campaign
      description: |
        The messages this campaign queued, with a count per status.
        `pending` has not gone out yet and can still be cancelled;
        `sending` is in flight and cannot be stopped.

        The rows ARE the campaign, so a campaign whose messages have all been
        cancelled returns `404 campaign_not_found`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The campaign.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id: { type: string }
                      counts:
                        type: object
                        properties:
                          pending: { type: integer }
                          sending: { type: integer }
                          sent: { type: integer }
                          failed: { type: integer }
                          skipped: { type: integer }
                      items:
                        type: array
                        items: { $ref: "#/components/schemas/DmQueueItem" }
              example:
                data:
                  id: "V1StGXR8_Z5jdHi6B-myT"
                  counts: { pending: 2, sending: 0, sent: 0, failed: 0, skipped: 0 }
                  items:
                    - id: "aZ7q2kR0pLmX"
                      campaign_id: "V1StGXR8_Z5jdHi6B-myT"
                      recipient:
                        x_user_id: "44196397"
                        handle: "levelsio"
                        name: "Pieter Levels"
                        avatar_url: null
                      message: "Hey Pieter, loved your thread on shipping fast."
                      template: "Hey [first], loved your thread on shipping fast."
                      status: "pending"
                      error: null
                      created_at: "2026-09-11T09:00:00.000Z"
                      scheduled_at: null
                      sent_at: null
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/CampaignNotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    delete:
      tags: [DMs]
      operationId: cancelDmCampaign
      summary: Cancel a campaign's unsent messages
      description: |
        Deletes every message of this campaign still waiting to go out,
        queued-for-now and scheduled-for-a-later-day alike, and releases the
        monthly allowance they were holding.

        Messages already sent are untouched and cannot be recalled, and one
        the scheduler has already picked up (`sending`) cannot be stopped. A
        campaign that exists but has nothing left to cancel (everything
        already sent, failed or cancelled) answers `cancelled: 0`; an id that
        never queued anything on this account answers
        `404 campaign_not_found`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: How many unsent messages were cancelled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      cancelled: { type: integer }
              example:
                data: { cancelled: 2 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/CampaignNotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/dm/queue:
    get:
      tags: [DMs]
      operationId: listDmQueue
      summary: List the DM queue
      description: |
        The account's bulk DM rows, newest first: what is waiting, what went
        out and what failed. Rows queued in the SuperX app appear here too,
        with `campaign_id: null`.

        `meta.total` counts the rows matching your filters; `meta.pending` is
        the account's whole unsent count, the figure the app shows.
      parameters:
        - name: limit
          in: query
          required: false
          description: Rows per page, 1 to 200 (default 50).
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, sending, sent, failed, skipped]
        - name: campaign_id
          in: query
          required: false
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: A page of the DM queue.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/DmQueueItem" }
                  meta:
                    type: object
                    properties:
                      total: { type: integer }
                      pending: { type: integer }
                      limit: { type: integer }
                      offset: { type: integer }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/dm/limits:
    get:
      tags: [DMs]
      operationId: getDmLimits
      summary: DM allowances and usage
      description: |
        The account's DM caps and how much of each is used. These are PLAN
        limits, not AI credits, and nothing here is charged.

        `monthly.committed` is the messages already queued and not yet sent:
        they are held against the monthly allowance until they go out or are
        cancelled. `posts_with_auto_dm` is the separate cap on how many
        scheduled posts a month may carry an `auto_dm`.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The account's DM limits.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/DmLimits" }
                  meta:
                    type: object
                    properties:
                      account_id: { type: string }
              example:
                data:
                  tier: "ultra"
                  monthly:
                    max: 500
                    used: 50
                    committed: 2
                    remaining: 448
                    reset_at: "2026-10-01T00:00:00.000Z"
                  daily_bulk: { max: 500, used: 2, remaining: 498 }
                  per_post_max: 100
                  posts_with_auto_dm: { max: 30, used: 1, remaining: 29 }
                  scheduled_ahead: 0
                  scheduling_enabled: true
                meta:
                  account_id: "1178367350552305665"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/queue-settings:
    get:
      tags: [Queue]
      operationId: getQueueSettings
      summary: Get queue settings
      description: >-
        The account's posting schedule: the predefined time slots the queue
        fills and the timezone they run in (the Edit Queue modal in the
        SuperX app). `slots` are `{ time, days }` entries with 0 = Sunday,
        sorted by time; `slot_count` is the total number of time and day
        pairs. `timezone_is_default` and `slots_are_default` are true when
        the account has never set that value and SuperX is using its own
        default. Works for your main account, a linked account, or an account
        shared with you.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The queue settings.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/QueueSettings" }
              example:
                data:
                  timezone: "Europe/London"
                  timezone_is_default: false
                  slots:
                    - time: "09:00"
                      days: [1, 2, 3, 4, 5]
                    - time: "17:30"
                      days: [1, 3, 5]
                  slots_are_default: false
                  slot_count: 8
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    patch:
      tags: [Queue]
      operationId: updateQueueSettings
      summary: Update queue settings
      description: >-
        Changes the account's posting schedule. Send `slots`, `timezone`, or
        both; only what you send changes. `slots` is a FULL REPLACE (max 50
        entries, one per unique time, each with at least one weekday, 0 =
        Sunday); `[]` clears every predefined slot and leaves the queue
        all-custom.


        Changing `slots` also RE-FLOWS the queue the way the app does: a
        queued post sitting exactly on an occurrence of an old slot moves to
        the matching new occurrence (Nth old to Nth new), so deliberate gaps
        are preserved and hand-picked custom times stay put. Scheduled X
        Articles and chained quote posts never move. Changing only `timezone`
        never moves posts.


        `reflow.bailed: true` means the settings were saved but the queue was
        deliberately left untouched (a post had nowhere to land, or the move
        set was too large); the response then carries a `note`. Retrying the
        same request is safe and idempotent.


        Changing the timezone and the slots in one call usually moves nothing,
        because the existing posts were placed under the old timezone; to
        re-flow them, change the timezone first, then send the slots in a
        second call.


        This is a per-account write: pass `account_id` for any linked or
        shared account. Editor-permission shares CAN change queue settings.
      parameters:
        - $ref: "#/components/parameters/AccountId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                slots:
                  type: array
                  maxItems: 50
                  items: { $ref: "#/components/schemas/QueueSlot" }
                  description: Full replace of the predefined posting slots. `[]` clears them all.
                timezone:
                  type: string
                  maxLength: 64
                  description: IANA timezone name the slot times run in, for example `Europe/London`.
                account_id: { type: string }
            example:
              slots:
                - time: "09:00"
                  days: [1, 2, 3, 4, 5]
                - time: "17:30"
                  days: [1, 3, 5]
              timezone: "Europe/London"
      responses:
        "200":
          description: The updated settings plus what the reflow did.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/QueueSettings" }
                  reflow: { $ref: "#/components/schemas/ReflowSummary" }
                  note:
                    type: string
                    description: Present only when `reflow.bailed` is true.
              example:
                data:
                  timezone: "Europe/London"
                  timezone_is_default: false
                  slots:
                    - time: "09:00"
                      days: [1, 2, 3, 4, 5]
                    - time: "17:30"
                      days: [1, 3, 5]
                  slots_are_default: false
                  slot_count: 8
                reflow:
                  moved: 3
                  skipped: 0
                  bailed: false
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/QueueWriteForbidden" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/tags:
    get:
      tags: [Tags]
      operationId: listTags
      summary: List tags
      description: Tags organize drafts and scheduled posts. They are scoped to the workspace owner and shared across linked accounts. Sorted by name A-Z.
      responses:
        "200":
          description: All tags.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Tag" }
              example:
                data:
                  - id: "aB3xWnA0kLpR7sVtYu3zc"
                    name: "Launch week"
                    color: "amber"
                    created_at: "2026-07-01T09:00:00.000Z"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    post:
      tags: [Tags]
      operationId: createTag
      summary: Create a tag
      description: Names are unique per workspace. `color` is a palette name the SuperX app renders (rose, amber, lime, emerald, teal, cyan, blue, indigo, violet, fuchsia, slate, stone); it defaults to `blue`. Tags are workspace-wide, so this is a main-account write and any other `account_id` returns `403 writes_main_account_only`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 40
                color:
                  type: string
                  minLength: 1
                  maxLength: 32
                  default: blue
                account_id:
                  type: string
                  description: Only your main account (the default when omitted). Tags are workspace-wide, so any other account you own returns `403 writes_main_account_only`.
            example:
              name: "Launch week"
              color: "amber"
      responses:
        "201":
          description: The created tag.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Tag" }
              example:
                data:
                  id: "aB3xWnA0kLpR7sVtYu3zc"
                  name: "Launch week"
                  color: "amber"
                  created_at: "2026-07-07T09:00:00.000Z"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an `account_id` other than your main account (`writes_main_account_only`; tags are workspace-wide), or a lapsed subscription (legacy string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: insufficient_scope
                  message: "This API key is read-only. Create a key with the write scope to use this endpoint."
        "409":
          description: A tag with that name already exists.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: duplicate_name
                  message: "A tag with that name already exists."
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/tags/{id}:
    patch:
      tags: [Tags]
      operationId: updateTag
      summary: Rename or recolor a tag
      description: Provide `name` and/or `color`. Tags are workspace-wide, so this is a main-account write and any other `account_id` returns `403 writes_main_account_only`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 40
                color:
                  type: string
                  minLength: 1
                  maxLength: 32
                account_id:
                  type: string
                  description: Only your main account (the default when omitted). Tags are workspace-wide, so any other account you own returns `403 writes_main_account_only`.
            example:
              name: "Launch"
              color: "violet"
      responses:
        "200":
          description: The updated tag.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Tag" }
              example:
                data:
                  id: "aB3xWnA0kLpR7sVtYu3zc"
                  name: "Launch"
                  color: "violet"
                  created_at: "2026-07-01T09:00:00.000Z"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an `account_id` other than your main account (`writes_main_account_only`; tags are workspace-wide), or a lapsed subscription (legacy string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: insufficient_scope
                  message: "This API key is read-only. Create a key with the write scope to use this endpoint."
        "404":
          description: No tag with that id belongs to this account.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: not_found
                  message: "No tag with that id belongs to this account."
        "409":
          description: A tag with that name already exists.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: duplicate_name
                  message: "A tag with that name already exists."
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    delete:
      tags: [Tags]
      operationId: deleteTag
      summary: Delete a tag
      description: Deletes the tag and removes it from every draft and scheduled post that carries it (the posts themselves are untouched). Tags are workspace-wide, so this is a main-account write and any other `account_id` returns `403 writes_main_account_only`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/AccountId"
      responses:
        "204":
          description: Deleted. No body.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an `account_id` other than your main account (`writes_main_account_only`; tags are workspace-wide), or a lapsed subscription (legacy string envelope).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: insufficient_scope
                  message: "This API key is read-only. Create a key with the write scope to use this endpoint."
        "404":
          description: No tag with that id belongs to this account.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: not_found
                  message: "No tag with that id belongs to this account."
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/articles:
    get:
      tags: [Articles]
      operationId: listArticles
      summary: List articles
      description: Long-form X Articles for the selected account, newest-updated first. Article bodies do NOT appear in lists; fetch one article for its markdown body.
      parameters:
        - $ref: "#/components/parameters/AccountId"
        - name: status
          in: query
          description: "Comma list of statuses to include: draft, scheduled, publishing, published, failed. Omit for all."
          schema:
            type: string
            example: "draft,scheduled"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: Articles plus pagination.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Article" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "k9GdPzR4wq2xVbY0nT1sc"
                    title: "How we grew to 10k followers"
                    status: "draft"
                    cover: { url: "https://media.superx.so/covers/example.jpg" }
                    scheduled_for: null
                    published_at: null
                    x_post_id: null
                    created_at: null
                    updated_at: "2026-07-07T10:00:00.000Z"
                pagination: { page: 1, limit: 20, has_more: false, total: 1 }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    post:
      tags: [Articles]
      operationId: createArticle
      summary: Create an article draft
      description: |
        Creates a draft. `content_markdown` (max 400KB) converts to the article's stored rich-text form: headings (h1-h3), bullet and numbered lists (one nesting level), blockquotes, bold/italic/strikethrough, links, images by http(s) URL, and bare X post URLs alone on a line as embeds. Constructs the format cannot express (code blocks, horizontal rules) degrade to plain text and are reported in a `warnings` array. Non-http(s) image or link URLs are rejected with 400.

        Supports `Idempotency-Key` (max 64 chars): retries with the same key and body replay the original response with `Idempotency-Replayed: true`. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title:
                  type: string
                  minLength: 1
                  maxLength: 300
                content_markdown:
                  type: string
                  description: Article body as markdown. Omit for an empty draft.
                account_id:
                  type: string
                  description: Any account you own, meaning your main account (the default when omitted) or one linked to it. An account shared with you returns `403 writes_main_account_only`.
            example:
              title: "How we grew to 10k followers"
              content_markdown: "# The system\n\nConsistency beats intensity.\n\n- Post daily\n- Reply to your top contacts"
      responses:
        "201":
          description: The created article (body echoed as markdown).
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Article" }
                  warnings:
                    type: array
                    items: { type: string }
                    description: Markdown degradations applied during conversion (absent when none).
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "409":
          description: Idempotency-Key reused with a different request body.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: idempotency_key_reuse
                  message: "This Idempotency-Key was already used with a different request body."
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/articles/{id}:
    get:
      tags: [Articles]
      operationId: getArticle
      summary: Get an article
      description: One article including `content_markdown`. Articles edited in the SuperX editor can carry rich-text constructs markdown cannot express exactly; those degrade to their text content in the markdown view.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The article with its markdown body.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Article" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/ArticleNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    patch:
      tags: [Articles]
      operationId: updateArticle
      summary: Edit an article
      description: Partial edit. Only provided fields change. `content_markdown` replaces the WHOLE body. `cover_url` attaches an http(s) image as the cover; `null` removes the cover. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                  minLength: 1
                  maxLength: 300
                content_markdown:
                  type: string
                  description: Full replacement body as markdown (max 400KB).
                cover_url:
                  type: string
                  nullable: true
                  description: http(s) image URL to set as the cover, or null to remove it.
                account_id:
                  type: string
            example:
              title: "How we grew to 10k followers (updated)"
      responses:
        "200":
          description: The updated article (body echoed as markdown).
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Article" }
                  warnings:
                    type: array
                    items: { type: string }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ArticleNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }
    delete:
      tags: [Articles]
      operationId: deleteArticle
      summary: Delete an article
      description: Deletes the article. Scheduled articles refund the quota their scheduling deducted. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: "#/components/parameters/AccountId"
      responses:
        "204":
          description: Deleted. No body.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ArticleNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/articles/{id}/publish:
    post:
      tags: [Articles]
      operationId: publishArticle
      summary: Publish an article to X now
      description: |
        Publishes the article immediately from its STORED title, body, and cover. Irreversible: the article goes live publicly and post quota is spent. Requires X Premium on the connected account; without it the publish fails with 403 `x_premium_required`. X also enforces its own article limits (10 drafts and 5 publishes per day), which surface as 502 `x_publish_failed` with the failure detail.

        Supports `Idempotency-Key`, strongly recommended. A 504 `upstream_timeout` is AMBIGUOUS: the publish may still have completed; GET the article and check its status before retrying.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id: { type: string }
      responses:
        "200":
          description: Published.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id: { type: string }
                      status: { type: string, enum: [published] }
                      post_id: { type: string, nullable: true }
                      url: { type: string, nullable: true }
                      published_at: { type: string, format: date-time, nullable: true }
              example:
                data:
                  id: "k9GdPzR4wq2xVbY0nT1sc"
                  status: "published"
                  post_id: "1790000000000000000"
                  url: "https://x.com/i/status/1790000000000000000"
                  published_at: "2026-07-07T12:00:00.000Z"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "402":
          description: Post quota exceeded for the current billing period.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: post_quota_exceeded
                  message: "Post quota exceeded for the current billing period."
        "403":
          description: Read-only key (`insufficient_scope`), an account shared with you (`writes_main_account_only`; shared accounts are read-only through the API, your own linked accounts are not), missing X Premium (`x_premium_required`), a connected account needing re-authorization (`reauth_required`), or a lapsed subscription.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: x_premium_required
                  message: "Publishing articles requires X Premium on the connected account."
        "404": { $ref: "#/components/responses/ArticleNotFound" }
        "409":
          description: The article's status does not allow publishing (already published, or a publish is in flight).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: article_not_editable
                  message: "This article is already published"
        "429": { $ref: "#/components/responses/RateLimited" }
        "502":
          description: The publish failed (`x_publish_failed`, article returned to draft, quota refunded) or could not be confirmed (`x_publish_ambiguous`, check the article's status).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: x_publish_failed
                  message: "The publish failed. The article was returned to draft."
        "504":
          description: AMBIGUOUS timeout. The publish may still have completed. GET the article to check its status before retrying.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: upstream_timeout
                  message: "The publish did not respond in time and may still have completed. GET the article to check its status before retrying."

  /v1/articles/{id}/schedule:
    post:
      tags: [Articles]
      operationId: scheduleArticle
      summary: Schedule or reschedule an article
      description: |
        Schedules the article for `scheduled_for` (UTC ISO-8601 with an explicit Z or offset, more than 2 minutes in the future; no maximum horizon). Scheduling a draft deducts post quota, refunded on unschedule or delete. Rescheduling an already scheduled article is a free time-only move. Supports `Idempotency-Key`. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [scheduled_for]
              properties:
                scheduled_for:
                  type: string
                  format: date-time
                account_id: { type: string }
            example:
              scheduled_for: "2026-08-01T15:00:00Z"
      responses:
        "200":
          description: The scheduled article.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Article" }
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "402":
          description: Post quota exceeded for the current billing period.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: post_quota_exceeded
                  message: "Post quota exceeded for the current billing period."
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ArticleNotFound" }
        "409":
          description: The article's status does not allow scheduling (already published or publishing), or the Idempotency-Key was reused with a different body.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: article_not_editable
                  message: "This article is already published"
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/articles/{id}/unschedule:
    post:
      tags: [Articles]
      operationId: unscheduleArticle
      summary: Unschedule an article
      description: Pulls a scheduled article back to draft and refunds exactly the quota its scheduling deducted. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id: { type: string }
      responses:
        "200":
          description: The article, back in draft.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Article" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/ArticleNotFound" }
        "409":
          description: The article is not scheduled.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: article_not_scheduled
                  message: "Only scheduled articles can be unscheduled."
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/articles/{id}/cover:
    post:
      tags: [Articles]
      operationId: generateArticleCover
      summary: Generate an AI cover image
      description: |
        Generates a 2.5:1 cover image from the article's TITLE (a title is required) and, by default, attaches it as the article's cover. `attach: false` generates without touching the current cover; attach later via PATCH `cover_url`. Steer the artwork with `style_id` (one of the account's saved styles, from `GET /v1/cover-styles`) or `style_text` (a one-off description, max 8000 chars) - sending BOTH is a 400, so the request always says exactly which look was rendered. An unknown `style_id` returns 404 `cover_style_not_found`.

        Slow (60-100 seconds) and paid: a FLAT 25 AI credits per generation on this API, whatever the render actually costs, reported in `meta.credits_charged` and the `X-Credits-Charged` header. The account's daily and monthly cover caps apply on top and are enforced server-side; cap 429s carry `remaining_day` / `remaining_month` / `limit_day` / `limit_month`, and a credit 429 carries `credits_required` / `credits_remaining` / `reset_at`.

        A generation that fails outright is refunded in full. A `504` is different: the render may well have completed and landed in the article's cover history, so the 25 credits stay charged and you should read the article before retrying.

        Supports `Idempotency-Key`, recommended. Works for your main account or any account linked to it (`account_id`); accounts shared with you are read-only.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                style_id:
                  type: string
                  minLength: 1
                  maxLength: 64
                  description: A saved cover style id from `GET /v1/cover-styles`. Not accepted together with style_text.
                style_text:
                  type: string
                  maxLength: 8000
                  description: Optional one-off style description steering the artwork. Not accepted together with style_id.
                attach:
                  type: boolean
                  default: true
                account_id: { type: string }
            example:
              style_text: "dark, minimal, geometric"
      responses:
        "200":
          description: The generated cover.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      cover:
                        type: object
                        properties:
                          url: { type: string }
                      attached:
                        type: boolean
                        description: Whether the cover was attached to the article.
                      remaining_day: { type: integer, nullable: true }
                      remaining_month: { type: integer, nullable: true }
                  meta:
                    type: object
                    properties:
                      credits_charged:
                        type: integer
                        description: Always 25. A generation is a flat price on this API.
              example:
                data:
                  cover: { url: "https://media.superx.so/covers/example.jpg" }
                  attached: true
                  remaining_day: 3
                  remaining_month: 11
                meta:
                  credits_charged: 25
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Read-only key (`insufficient_scope`), an account shared with you (`writes_main_account_only`; shared accounts are read-only through the API, your own linked accounts are not), cover generation disabled (`feature_disabled`), or a lapsed subscription.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/LegacyErrorEnvelope"
              example:
                error:
                  code: insufficient_scope
                  message: "This API key is read-only. Create a key with the write scope to use this endpoint."
        "404": { $ref: "#/components/responses/ArticleOrCoverStyleNotFound" }
        "409":
          description: A cover is already being generated for this article (`cover_gen_in_progress`), or the Idempotency-Key was reused with a different body.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: cover_gen_in_progress
                  message: "A cover is already being generated for this article. Try again once it finishes."
        "429":
          description: Rate limited, generation caps reached (`cover_gen_cap_daily` / `cover_gen_cap_monthly` with counters), or AI credits exhausted (`ai_credits_exhausted`, with the credit counters and `Retry-After`).
          headers:
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/CreditExhaustedError"
                  - $ref: "#/components/schemas/ErrorEnvelope"
              examples:
                cap_reached:
                  summary: Daily generation cap
                  value:
                    error:
                      code: cover_gen_cap_daily
                      message: "The daily cover generation cap has been reached."
                      remaining_day: 0
                      remaining_month: 7
                      limit_day: 6
                      limit_month: 13
                credits_exhausted:
                  summary: Credit pool empty
                  value:
                    error:
                      code: ai_credits_exhausted
                      message: "Not enough AI credits for a cover generation."
                      credits_required: 25
                      credits_remaining: 4
                      reset_at: 1790812800
        "502": { $ref: "#/components/responses/UpstreamError" }
        "504":
          description: AMBIGUOUS timeout. The render may still have completed. GET the article to check its cover before retrying.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: upstream_timeout
                  message: "Cover generation did not respond in time and may still have completed. The 25 credits stay charged; GET the article to check its cover before retrying."

  /v1/cover-styles:
    get:
      tags: [Articles]
      operationId: listCoverStyles
      summary: List saved cover styles
      description: |
        The article cover styles the account saved in the SuperX app: the art
        direction of a cover it kept, or a description it typed, with a
        thumbnail. Pass a style's `id` as `style_id` to
        `POST /v1/articles/{id}/cover` to render in that look.

        The style's own prompt text never leaves SuperX: styles are applied
        by id, exactly as the app applies them. Styles are saved and deleted
        in the app; this endpoint only reads them. Not paginated (styles are
        capped per account).
      parameters:
        - $ref: "#/components/parameters/AccountId"
      responses:
        "200":
          description: The account's saved cover styles, newest first.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/CoverStyle" }
              example:
                data:
                  - id: "V1StGXR8_Z5jdHi6B-myT"
                    name: "Dark geometric"
                    thumbnail_url: "https://media.superx.so/cover-styles/example.jpg"
                    source: cover
                    created_at: "2026-08-14T09:00:00.000Z"
                    last_used_at: "2026-09-01T12:30:00.000Z"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/AccountNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/datasets:
    get:
      tags: [Datasets]
      operationId: listDatasets
      summary: List datasets
      description: |
        The audience collections Ask SuperX built for the key owner in the
        app: the repliers, quoters or reposters of a post, the members of an
        X list, the owner's own posts or replies, or a research brief.
        Newest first, expired datasets excluded.

        Datasets belong to the person, not to one X account, so there is no
        `account_id` parameter here. The account a collection ran as is
        reported as `x_account_id`.

        Datasets are kept for 30 days and are created in the SuperX app for
        now; this API reads them and can copy the people in one into a
        contact list.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: The owner's live datasets, newest first.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Dataset" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - id: "VKcPAVU1FSkx4kzPuehGK"
                    title: "Repliers to my launch post"
                    source: repliers
                    status: ready
                    target_ref: "1234567890123456789"
                    x_account_id: "1178367350552305665"
                    ask_chat_id: "aB3dEf9"
                    columns: ["handle", "name", "bio", "website", "followers", "following", "location", "can_dm", "verified"]
                    row_count: 214
                    scanned_count: 214
                    total_estimate: 214
                    coverage_complete: true
                    has_people: true
                    created_at: "2026-09-05T10:12:00.000Z"
                    expires_at: "2026-10-05T10:12:00.000Z"
                pagination:
                  page: 1
                  limit: 20
                  has_more: false
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
    post:
      tags: [Datasets]
      operationId: createDataset
      summary: Collect an audience into a dataset
      description: |
        Collect the repliers, quote posters or reposters of a post, the
        members of a public X list, or the account's own posts or replies,
        into a stored dataset you can then read, export, or copy into a
        contact list.

        `source: "research"` is the exception: instead of walking an audience
        it reads people's recent posts and writes a short outreach brief for
        each one (what they do, what they are building, recent topics,
        personalization hooks that QUOTE their real posts, and an opener
        angle). Give it exactly one source - `handles`, `list_id`, `agent_id`
        or `dataset_id` - plus optional `focus`, `title` and `max_rows` (1-25,
        default 10). The briefs land as an ordinary dataset, so
        `GET /v1/datasets/{id}/rows`, `/export` and `/contacts` read them
        unchanged. More than 5 profiles research in the background and answer
        `202`. If none of them could be researched, the answer is `200` with
        `data: null`, the reasons in `meta.skipped`, and every credit
        returned.

        **This endpoint can answer before the work is finished.** A small
        collection runs inside the request and answers `200` with a `ready`
        dataset. A big one, **or one whose size cannot be established up
        front**, answers `202` with a `collecting` dataset: poll
        `GET /v1/datasets/{id}` until `status` is `ready` (or `failed`) before
        reading its rows. Only one background collection runs per account at a
        time; a second one gets `409 collection_in_progress`.

        If nothing matched the filters, the response is `200` with
        `data: null` and a `note`: no dataset is created for an empty result,
        and the daily collection slot is given back.

        `my_posts` and `my_replies` read the account's own synced posts: they
        take no `target`, always finish inside the request, and ignore the
        profile filters (the response `note` says so rather than dropping them
        silently).

        **Cost.** Every collection counts against a limit of 10 per day for
        the account, shared with the collections Ask SuperX runs in the app
        (`429 collection_quota_exceeded`). Collections that read X also spend
        the enrichment allowance: one unit for the size estimate, plus the
        pages the walk is expected to need, all charged before the walk
        starts so `X-RateLimit-Remaining` stays honest about work that
        outlives the response. Own-content collections spend nothing.

        **Research is charged in AI credits, not collections.**
        `source: "research"` costs a flat 1 credit per profile ACTUALLY
        researched: profiles that could not be researched come back in
        `meta.skipped` and their credits are returned. It spends one of the
        plan's daily profile-research runs (`429 ai_action_limited` with
        `scope: "account"`) and a slice of a platform-wide fair-use ceiling on
        live-data actions shared by every SuperX account
        (`429 ai_action_limited` with `scope: "platform"`), and it does NOT
        count against the 10 collections a day. An empty credit pool is
        `429 ai_credits_exhausted`. What was charged comes back in
        `meta.credits_charged` and in the `X-Credits-*` headers on the `200`;
        a background research run settles when it finishes, so its `202`
        carries neither, and the figure lands on `GET /v1/me` and your ledger.

        Datasets created here appear alongside the ones Ask SuperX built, and
        are kept for 30 days.

        Send an `Idempotency-Key` header to make retries safe: the same key
        with the same body replays the first response with
        `Idempotency-Replayed: true`, and the same key with a different body
        is rejected with `409 idempotency_key_reuse`.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateDatasetRequest" }
            example:
              source: repliers
              target: "https://x.com/robj3d3/status/1234567890123456789"
              title: "Repliers to my launch post"
              max_rows: 500
              filters:
                min_followers: 500
                require_can_dm: true
      responses:
        "200":
          description: |
            The collection finished inside the request. `data` is null when
            nothing matched, with a `note` explaining why. A research run
            adds `meta` (what was requested, what was researched, what it
            cost) and the `X-Credits-*` headers.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
            X-Credits-Charged:
              description: Credits kept for this call. Research runs only.
              schema:
                type: integer
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/DatasetDetail"
                    nullable: true
                  meta:
                    type: object
                    description: Research runs only. Absent on audience collections.
                    properties:
                      requested:
                        type: integer
                        description: Profiles the source resolved to.
                      researched:
                        type: integer
                        description: Profiles a brief was actually written for.
                      credits_charged:
                        type: integer
                        description: Credits kept, one per researched profile.
                      skipped:
                        type: array
                        description: Profiles that produced no brief, with the reason.
                        items:
                          type: object
                          properties:
                            handle:
                              type: string
                              nullable: true
                            reason:
                              type: string
                  note:
                    type: string
                    description: Present when something needs saying, such as an empty result or ignored filters.
              examples:
                collection:
                  value:
                    data:
                      id: "VKcPAVU1FSkx4kzPuehGK"
                      title: "Repliers to my launch post"
                      source: repliers
                      status: ready
                      target_ref: "1234567890123456789"
                      x_account_id: "1178367350552305665"
                      ask_chat_id: null
                      columns: ["handle", "name", "bio", "website", "followers", "following", "location", "can_dm", "verified"]
                      row_count: 64
                      scanned_count: 71
                      total_estimate: 71
                      coverage_complete: true
                      has_people: true
                      created_at: "2026-09-10T10:12:00.000Z"
                      expires_at: "2026-10-10T10:12:00.000Z"
                      coverage: "Includes all 71 direct replies to the post."
                      filters: { minFollowers: 500, requireCanDm: true }
                research:
                  summary: source "research", 3 profiles, finished in the request
                  value:
                    data:
                      id: "Rk2PAVU1FSkx4kzPuehGK"
                      title: "Outreach briefs (3 profiles)"
                      source: research
                      status: ready
                      target_ref: "handles"
                      x_account_id: "1178367350552305665"
                      ask_chat_id: null
                      columns: ["handle", "name", "followers", "can_dm", "bio", "does", "building", "recent_topics", "hooks", "opener_angle"]
                      row_count: 3
                      scanned_count: 3
                      total_estimate: 3
                      coverage_complete: true
                      has_people: true
                      created_at: "2026-09-10T10:12:00.000Z"
                      expires_at: "2026-10-10T10:12:00.000Z"
                      coverage: "Researched all 3 requested profiles."
                    meta:
                      requested: 3
                      researched: 3
                      credits_charged: 3
                    note: "Every hook quotes one of the person's real posts; proposed quotes that failed the verbatim check were dropped. Briefs are text for a person to act on: read the full rows with GET /v1/datasets/{id}/rows."
        "202":
          description: |
            The collection, or the research run, started in the background.
            Poll `GET /v1/datasets/{id}` until `status` is `ready`. Research
            settles its credits when the run finishes, so no `X-Credits-*`
            header is sent here and `meta` carries no charge.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/DatasetDetail" }
                  meta:
                    type: object
                    description: Research runs only. Absent on audience collections.
                    properties:
                      requested:
                        type: integer
                        description: Profiles the source resolved to.
                      skipped:
                        type: array
                        description: Profiles rejected before the run started, with the reason.
                        items:
                          type: object
                          properties:
                            handle:
                              type: string
                              nullable: true
                            reason:
                              type: string
                  note:
                    type: string
              example:
                data:
                  id: "Jc9PxVU1FSkx4kzPuehGK"
                  title: "Repliers to my launch post"
                  source: repliers
                  status: collecting
                  target_ref: "1234567890123456789"
                  x_account_id: "1178367350552305665"
                  ask_chat_id: null
                  columns: ["handle", "name", "bio", "website", "followers", "following", "location", "can_dm", "verified"]
                  row_count: 0
                  scanned_count: 0
                  total_estimate: 4200
                  coverage_complete: false
                  has_people: true
                  created_at: "2026-09-10T10:12:00.000Z"
                  expires_at: "2026-10-10T10:12:00.000Z"
                  progress:
                    page: 0
                    scanned: 0
                    matched: 0
                    total_estimate: 4200
                    started_at: "2026-09-10T10:12:00.000Z"
                note: "Collecting in the background; poll GET /v1/datasets/{id} until status is ready."
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404":
          description: |
            `account_not_found` when `account_id` is not one of your accounts.
            On `source: "research"` the source id is named too:
            `list_not_found`, `agent_not_found`, or `not_found` for a dataset
            id that is unknown or expired.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                account_not_found:
                  value:
                    error:
                      code: account_not_found
                      message: "No account with that id belongs to this key"
                list_not_found:
                  value:
                    error:
                      code: list_not_found
                      message: "No contact list with that id belongs to this account."
                agent_not_found:
                  value:
                    error:
                      code: agent_not_found
                      message: "No signal agent with that id belongs to this account."
                not_found:
                  value:
                    error:
                      code: not_found
                      message: "No dataset with that id. Use the dataset_id from a collect_audience or refine_dataset result."
        "409":
          description: |
            Another background collection is still running for this account
            (`collection_in_progress`), the `Idempotency-Key` was reused with a
            different request body (`idempotency_key_reuse`), or the dataset a
            research run was pointed at is not ready yet
            (`dataset_not_ready`, carrying `dataset_status`). None of them
            started anything.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                dataset_not_ready:
                  value:
                    error:
                      code: dataset_not_ready
                      message: "The source dataset is not ready yet. Poll GET /v1/datasets/{id} until status is ready, then research it."
                      dataset_status: collecting
                collection_in_progress:
                  value:
                    error:
                      code: collection_in_progress
                      message: "Another audience collection is still running for this account. Poll it with GET /v1/datasets/{id} and start this one when it finishes."
                idempotency_key_reuse:
                  value:
                    error:
                      code: idempotency_key_reuse
                      message: "This Idempotency-Key was already used with a different request body."
        "429":
          description: |
            Read `error.code`. A collection is refused when today's 10
            collections are used up (`collection_quota_exceeded`, shared with
            Ask SuperX in the app) or the enrichment allowance the walk needs
            is exhausted (`rate_limited`). A research run is refused when the
            plan's daily research runs are used up (`ai_action_limited` with
            `scope: "account"`), when the platform-wide fair-use ceiling on
            live-data actions is reached (`ai_action_limited` with
            `scope: "platform"`, shared by every account and not about your
            own quota), or when the AI credit pool is empty
            (`ai_credits_exhausted`). Every flavour carries `Retry-After`, and
            the two `ai_action_limited` ones carry `reset_at` (unix seconds)
            as well. Nothing was started or charged.
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: integer
            X-Credits-Remaining:
              description: Credits left in the pool. Sent only on `ai_credits_exhausted`.
              schema:
                type: integer
            X-Credits-Reset:
              description: Unix seconds when the pool refills. Sent only on `ai_credits_exhausted`.
              schema:
                type: integer
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/ErrorEnvelope"
                  - $ref: "#/components/schemas/ActionLimitedError"
                  - $ref: "#/components/schemas/CreditExhaustedError"
              examples:
                collection_quota_exceeded:
                  value:
                    error:
                      code: collection_quota_exceeded
                      message: "You have used today's audience collections. Existing datasets still read and export; the limit resets at midnight UTC."
                      retry_after: 20400
                      limit: 10
                      reset_at: 1789430400
                rate_limited:
                  value:
                    error:
                      code: rate_limited
                      message: "Rate limit exceeded for the Pro plan (5 enrichment requests/min). This collection needs 4 lookups."
                      retry_after: 42
                ai_action_limited_account:
                  summary: source "research", the plan's daily research runs are used up
                  value:
                    error:
                      code: ai_action_limited
                      message: "Daily limit reached for this action on your plan (10 profile research runs per day). It resets at midnight UTC."
                      scope: account
                      retry_after: 20400
                      reset_at: 1789430400
                ai_action_limited_platform:
                  summary: source "research", the shared fair-use ceiling is reached
                  value:
                    error:
                      code: ai_action_limited
                      message: "The platform-wide fair-use ceiling for live-data actions has been reached for today. It resets at midnight UTC."
                      scope: platform
                      retry_after: 20400
                      reset_at: 1789430400
                ai_credits_exhausted:
                  summary: source "research", not enough credits for the batch
                  value:
                    error:
                      code: ai_credits_exhausted
                      message: "Not enough AI credits. Resets in 5 hours."
                      credits_required: 10
                      credits_remaining: 4
                      reset_at: 1790812800
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/datasets/{id}:
    get:
      tags: [Datasets]
      operationId: getDataset
      summary: Get a dataset
      description: |
        One dataset's status, counts and coverage.

        Every status answers 200, so this is the endpoint to poll while a
        collection runs: `collecting` carries live `progress`, `failed`
        carries a `failure_note`, and `ready` carries the honest `coverage`
        sentence and the `filters` the collection applied. The counts on a
        collecting dataset are not final.
      parameters:
        - $ref: "#/components/parameters/DatasetId"
      responses:
        "200":
          description: The dataset, whatever its status.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/DatasetDetail" }
              example:
                data:
                  id: "VKcPAVU1FSkx4kzPuehGK"
                  title: "Repliers to my launch post"
                  source: repliers
                  status: ready
                  target_ref: "1234567890123456789"
                  x_account_id: "1178367350552305665"
                  ask_chat_id: "aB3dEf9"
                  columns: ["handle", "name", "bio", "website", "followers", "following", "location", "can_dm", "verified"]
                  row_count: 214
                  scanned_count: 214
                  total_estimate: 214
                  coverage_complete: true
                  has_people: true
                  created_at: "2026-09-05T10:12:00.000Z"
                  expires_at: "2026-10-05T10:12:00.000Z"
                  coverage: "Scanned all 214 direct replies available (relevance-ranked order)."
                  filters:
                    minFollowers: 500
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/DatasetNotFound" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }

  /v1/datasets/{id}/rows:
    get:
      tags: [Datasets]
      operationId: getDatasetRows
      summary: Get dataset rows
      description: |
        A page of a ready dataset's rows.

        Rows come back exactly as the collection stored them: the keys listed
        in the dataset's `columns`, plus `user_id` (the X account id as a
        string) and `avatar` on people datasets. Text fields are not
        shortened.

        Each call parses the whole dataset (up to about 5MB), so page through
        with `limit` and `page` rather than looping at `limit=1`. A dataset
        that is not `ready` returns 409 `dataset_not_ready`.
      parameters:
        - $ref: "#/components/parameters/DatasetId"
        - name: limit
          in: query
          required: false
          description: Rows per page, 1 to 100.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: A page of rows, with the dataset's row count as `total`.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/DatasetRow" }
                  pagination: { $ref: "#/components/schemas/Pagination" }
              example:
                data:
                  - handle: "@levelsio"
                    name: "levels.io"
                    bio: "Indie maker"
                    website: "https://levels.io"
                    followers: 600000
                    following: 1200
                    location: "Everywhere"
                    can_dm: true
                    verified: true
                    user_id: "944883311"
                    avatar: "https://pbs.twimg.com/profile_images/example.jpg"
                pagination:
                  page: 1
                  limit: 50
                  has_more: true
                  total: 214
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/DatasetNotFound" }
        "409": { $ref: "#/components/responses/DatasetNotReady" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }

  /v1/datasets/{id}/export:
    get:
      tags: [Datasets]
      operationId: exportDataset
      summary: Export a dataset as CSV
      description: |
        The whole dataset as a CSV file.

        This response is NOT wrapped in the `data` envelope: the body is the
        raw CSV, with a `Content-Disposition` filename matching the app
        download's naming pattern
        (`superx-dataset-<title>-<YYYY-MM-DD>.csv`). Treat the name as a
        convenience, not a contract: unusual titles can slug differently from
        the app's own download. Only the columns listed
        in `columns` are exported (the `user_id` and `avatar` extras stay
        out, exactly like the app download).

        CSV is the only format here; XLSX downloads stay in the SuperX app.
        A dataset that is not `ready` returns 409 `dataset_not_ready`.
      parameters:
        - $ref: "#/components/parameters/DatasetId"
        - name: format
          in: query
          required: false
          description: Only `csv` is supported. Any other value returns 400.
          schema:
            type: string
            enum: [csv]
            default: csv
      responses:
        "200":
          description: The dataset as CSV. Not wrapped in the `data` envelope.
          headers:
            Content-Disposition:
              description: 'attachment; filename="superx-dataset-<title>-<date>.csv"'
              schema:
                type: string
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            text/csv:
              schema:
                type: string
              example: |
                handle,name,followers
                @levelsio,levels.io,600000
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/DatasetNotFound" }
        "409": { $ref: "#/components/responses/DatasetNotReady" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }

  /v1/datasets/{id}/contacts:
    post:
      tags: [Datasets]
      operationId: addDatasetToContacts
      summary: Add a dataset's people to a contact list
      description: |
        Copy the people in a ready dataset into a contact list you created.

        Costs ONE WRITE and no enrichment: the profiles come from the dataset
        the collection already hydrated, which is why people SuperX has never
        stored still land in the list.

        People are deduped by X account id before the write, and rows without
        an X account id or a usable handle are counted in
        `skipped_without_id` rather than added as blank members, so
        `added + duplicates` can be lower than the dataset's `row_count`.
        Re-running is safe: people already in the list come back in
        `duplicates`.

        Own-content datasets (your posts, your replies) hold no people and
        return 400 `dataset_has_no_people`; so does a dataset whose rows all
        lack an X account id. A dataset that is not `ready` returns 409
        `dataset_not_ready`, and a system list returns 400
        `system_list_read_only`.

        Works for your main account or any account linked to it
        (`account_id`); accounts shared with you are read-only. Needs the
        write scope.
      parameters:
        - $ref: "#/components/parameters/DatasetId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [list_id]
              properties:
                list_id:
                  type: string
                  description: A contact list you created, from `GET /v1/contact-lists`.
                account_id:
                  type: string
            example:
              list_id: "cl_9f2b71"
      responses:
        "200":
          description: The add finished. Read the counters for what happened.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/DatasetContactsResult" }
              example:
                data:
                  added: 198
                  duplicates: 12
                  failed: 0
                  skipped_without_id: 4
                  total_in_list_after: 242
        "400": { $ref: "#/components/responses/DatasetHasNoPeople" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404":
          description: |
            Either end of the copy is missing: `dataset_not_found` (no dataset
            with that id belongs to this key, or it has expired) or
            `list_not_found` (no contact list with that id belongs to this
            account). Read `error.code` to tell them apart.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              example:
                error:
                  code: list_not_found
                  message: "No contact list with that id belongs to this account."
        "409": { $ref: "#/components/responses/DatasetNotReady" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/datasets/{id}/outreach-drafts:
    post:
      tags: [Datasets]
      operationId: draftDatasetOutreach
      summary: Draft outreach messages onto a research dataset
      description: |
        Writes one personalized message per person in a research dataset,
        following the `format` you supply.

        **The drafts are text and nothing is sent.** Each message is stored on
        the dataset's `message` column and read back with
        `GET /v1/datasets/{id}/rows`. No endpoint in this API sends a DM, and
        this one does not either: a person reviews the messages and sends them
        from the SuperX app.

        Personalization comes only from that person's stored brief and its
        verbatim hook quotes, never invented details. A brief with no usable
        hook gets an honest generic message that still follows your format,
        counted separately in `generic`. `[name]`, `[first]` and `[handle]`
        tokens are left intact for per-recipient fill-in at send time.

        A message that mentions a DIFFERENT recipient's handle is discarded
        rather than stored (counted in `contaminated`); run the endpoint again
        to retry those rows. Re-running overwrites every draft on the dataset.

        The dataset must have been created with `source: "research"` and be
        `ready`. Costs AI credits (measured, usually a few) and spends no
        live-data allowance: the briefs are already stored. Needs a key with
        the `write` scope.
      parameters:
        - $ref: "#/components/parameters/DatasetId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DraftOutreachRequest" }
            example:
              format: "hey [first]! been following what you're building. <personalization>. would love to trade notes sometime"
              instructions: "Keep it under three sentences and never pitch."
      responses:
        "200":
          description: The messages were drafted and saved onto the dataset.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/OutreachDraftsResult" }
                  meta:
                    type: object
                    properties:
                      credits_charged:
                        type: integer
                  note:
                    type: string
              example:
                data:
                  dataset_id: "VKcPAVU1FSkx4kzPuehGK"
                  title: "Outreach briefs (10 profiles)"
                  total: 10
                  drafted: 8
                  generic: 1
                  failed: 0
                  contaminated: 1
                  long_messages: 0
                  sample_messages:
                    - handle: "@robj3d3"
                      message: "hey [first]! been following what you're building..."
                meta:
                  credits_charged: 2
                note: "Drafts are text only and are stored on the dataset's `message` column; a person sends them from the SuperX app. The API never sends DMs."
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/DatasetNotFound" }
        "409": { $ref: "#/components/responses/DatasetNotReady" }
        "429": { $ref: "#/components/responses/AiActionLimited" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/datasets/{id}/refine:
    post:
      tags: [Datasets]
      operationId: refineDataset
      summary: Refine a dataset by what each person wrote
      description: |
        Judges each row's own text against a natural-language `criterion` and
        keeps or drops it, then optionally sorts and caps what is left into a
        NEW dataset. The source dataset is never modified.

        Only datasets whose rows carry text can be refined this way (a
        repliers or quoters collection). Anything else answers `400`: use the
        profile filters on `POST /v1/datasets` instead.

        Rows the classifier cannot judge confidently are **kept** and counted
        as `unclear`, never silently dropped. Classification is best-effort:
        read the kept rows before acting on them.

        **This endpoint can answer before the work is finished.** Up to 100
        rows with text classify inside the request and answer `200`; more than
        that answers `202` with a `collecting` dataset to poll through
        `GET /v1/datasets/{id}`. If nothing was kept, the answer is `200` with
        `data: null` and a `note`.

        **Cost.** A refinement creates a dataset, so it counts against the
        same 10 collections a day this account shares with Ask SuperX
        (`429 collection_quota_exceeded`), and it costs AI credits, measured
        from the classifier's real cost. It spends no live-data allowance.
        Needs a key with the `write` scope.
      parameters:
        - $ref: "#/components/parameters/DatasetId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RefineDatasetRequest" }
            example:
              criterion: "supportive or neutral; not mean, sarcastic, or hostile"
              keep_matching: true
              sort_by: followers
              limit: 200
      responses:
        "200":
          description: |
            The refinement finished inside the request. `data` is null when
            nothing was kept, with a `note` explaining why.
          headers:
            X-Credits-Charged: { $ref: "#/components/headers/X-Credits-Charged" }
            X-Credits-Remaining: { $ref: "#/components/headers/X-Credits-Remaining" }
            X-Credits-Reset: { $ref: "#/components/headers/X-Credits-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/DatasetDetail"
                    nullable: true
                  meta: { $ref: "#/components/schemas/RefineMeta" }
                  note:
                    type: string
              example:
                data:
                  id: "Jc9PxVU1FSkx4kzPuehGK"
                  title: "Refined: Repliers to my launch post"
                  source: repliers
                  status: ready
                  target_ref: "1234567890123456789"
                  x_account_id: "1178367350552305665"
                  ask_chat_id: null
                  columns: ["handle", "name", "bio", "text", "followers", "can_dm"]
                  row_count: 48
                  scanned_count: 64
                  total_estimate: 64
                  coverage_complete: true
                  has_people: true
                  created_at: "2026-09-10T10:20:00.000Z"
                  expires_at: "2026-10-10T10:20:00.000Z"
                meta:
                  source_dataset_id: "VKcPAVU1FSkx4kzPuehGK"
                  kept: 48
                  dropped: 14
                  unclear: 2
                  limited: 0
                  self_excluded: 0
                  credits_charged: 2
        "202":
          description: |
            The refinement started in the background. Poll
            `GET /v1/datasets/{id}` until `status` is `ready`. Credits are
            settled when it finishes, so no credit headers are sent here.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/DatasetDetail" }
                  meta: { $ref: "#/components/schemas/RefineMeta" }
                  note:
                    type: string
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/WriteForbidden" }
        "404": { $ref: "#/components/responses/DatasetNotFound" }
        "409":
          description: |
            The dataset is not ready yet (`dataset_not_ready`), or another
            background collection or refinement is still running for this
            account (`collection_in_progress`). Neither started anything, and
            `collection_in_progress` does not use up one of the day's 10.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                dataset_not_ready:
                  value:
                    error:
                      code: dataset_not_ready
                      message: "This dataset is not ready yet. Poll GET /v1/datasets/{id} until status is ready."
                      dataset_status: collecting
                collection_in_progress:
                  value:
                    error:
                      code: collection_in_progress
                      message: "Another collection or refinement is still running for this account. Poll it with GET /v1/datasets/{id} and start this one when it finishes."
        "429":
          description: |
            Today's 10 dataset collections are used up
            (`collection_quota_exceeded`, shared with Ask SuperX in the app),
            the AI credit pool is spent (`ai_credits_exhausted`), or a plan
            cap on refinements was hit (`ai_action_limited`). Read
            `error.code`; all carry `Retry-After`.
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: integer
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/CreditExhaustedError"
                  - $ref: "#/components/schemas/ActionLimitedError"
                  - $ref: "#/components/schemas/ErrorEnvelope"
              example:
                error:
                  code: collection_quota_exceeded
                  message: "You have used today's dataset collections. Existing datasets still read and export; the limit resets at midnight UTC."
                  retry_after: 20400
                  limit: 10
                  reset_at: 1789430400
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/AccountsUnavailable" }

  /v1/x/posts/{id}:
    get:
      tags: [X Lookups]
      operationId: lookupXPost
      summary: Look up one public post live
      description: |
        Read one public X post as it is right now: full text (never
        shortened), author, engagement counts, media, link card, and the
        quoted post one level deep when there is one.

        Owner-scoped, so there is no `account_id`: nothing upstream is
        per-X-account. Costs ONE live-enrichment unit, TWO with
        `include_quotes`, and also draws on the shared allowance of 300 live
        lookups per UTC day that Ask SuperX in the app uses (see
        [Rate limits](/rate-limits#live-x-lookups)). Repeat lookups of the
        same post within 15 minutes may be served from a server-side cache:
        they still cost their enrichment units but do not touch the daily
        allowance.

        `404 post_not_found` means the post is deleted, protected, or the id
        is wrong. Because the upstream batch read reports a missing post and
        a failed read identically, an occasional transient upstream failure
        surfaces here too, so retry once before concluding the post is gone.

        The `X-RateLimit-*` headers on this endpoint report the ENRICHMENT
        window, not the ordinary read window.
      parameters:
        - name: id
          in: path
          required: true
          description: Numeric X post id (the digits at the end of a post URL).
          schema: { type: string, pattern: "^[0-9]{1,25}$" }
        - name: include_quotes
          in: query
          description: |
            Also return a page of the posts quoting this one (up to 20, not
            exhaustive). Costs a second enrichment unit.
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: The post.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      post: { $ref: "#/components/schemas/XPost" }
                      quotes:
                        type: array
                        description: Present only when `include_quotes` was set.
                        items: { $ref: "#/components/schemas/XPost" }
                      quotes_note:
                        type: string
                        description: |
                          What the `quotes` page covers, or why it is empty.
                          Present only when `include_quotes` was set.
              example:
                data:
                  post:
                    id: "1938765432109876543"
                    url: "https://x.com/buildersam/status/1938765432109876543"
                    text: "I built my first product in 30 days with zero audience. Here is exactly what I would do differently:"
                    created_at: "2026-06-20T15:04:00.000Z"
                    author:
                      x_user_id: "44196397"
                      username: "buildersam"
                      name: "Sam Fields"
                      avatar_url: "https://pbs.twimg.com/profile_images/1/avatar.jpg"
                      verified: true
                      followers_count: 8421
                    metrics:
                      likes: 4211
                      replies: 312
                      reposts: 388
                      quotes: 44
                      views: 512000
                      bookmarks: 1904
                    in_reply_to_id: null
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/PostNotFound" }
        "429": { $ref: "#/components/responses/LookupQuotaExceeded" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/x/posts/{id}/replies:
    get:
      tags: [X Lookups]
      operationId: getXPostReplies
      summary: Top replies to a public post
      description: |
        The best-liked DIRECT replies to a public post, read live from X.

        This walks up to 3 relevance-ranked upstream pages (about 60
        candidate replies), ranks what it found by likes and returns the top
        `limit`. It is a SAMPLE of the strongest replies, not every reply and
        not chronological order, and it cannot page further. To collect
        everyone who replied to a post, use the audience collections built in
        the SuperX app (`GET /v1/datasets`).

        Unlike the same feature inside the SuperX app, replies written by the
        account owner themselves are NOT filtered out: an owner-scoped read
        has no "active account" to compare against.

        Costs a flat THREE live-enrichment units and draws on the shared
        300/day live-lookup allowance (one unit of that per page actually
        fetched). `data.post` is the post being replied to when the upstream
        payload carried it, and null otherwise; it is never a second lookup.

        The `X-RateLimit-*` headers on this endpoint report the ENRICHMENT
        window, not the ordinary read window.
      parameters:
        - name: id
          in: path
          required: true
          description: Numeric X post id.
          schema: { type: string, pattern: "^[0-9]{1,25}$" }
        - name: limit
          in: query
          description: Replies to return, best-liked first (1-20).
          schema: { type: integer, minimum: 1, maximum: 20, default: 10 }
      responses:
        "200":
          description: The top replies found.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      post_id: { type: string }
                      post:
                        allOf:
                          - $ref: "#/components/schemas/XPost"
                        nullable: true
                        description: The post being replied to, when the payload carried it.
                      replies:
                        type: array
                        items: { $ref: "#/components/schemas/XPost" }
                      replies_scanned:
                        type: integer
                        description: Direct replies seen across the pages walked, before the `limit` slice.
                      note:
                        type: string
                        description: What this sample covers, in plain words.
              example:
                data:
                  post_id: "1938765432109876543"
                  post: null
                  replies:
                    - id: "1938767777109876543"
                      url: "https://x.com/janedev/status/1938767777109876543"
                      text: "The 30-day constraint is the whole trick. Deadlines beat motivation."
                      created_at: "2026-06-20T15:31:00.000Z"
                      author:
                        x_user_id: "1290011"
                        username: "janedev"
                        name: "Jane"
                        avatar_url: "https://pbs.twimg.com/profile_images/2/avatar.jpg"
                        verified: false
                        followers_count: 3110
                      metrics:
                        likes: 288
                        replies: 12
                        reposts: 4
                        quotes: 0
                        views: 41022
                        bookmarks: 19
                      in_reply_to_id: "1938765432109876543"
                  replies_scanned: 47
                  note: "Top direct replies by likes, from up to 3 relevance-ranked pages (about 60 candidates). Not exhaustive and not chronological. Replies the account owner wrote themselves are included."
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/PostNotFound" }
        "429": { $ref: "#/components/responses/LookupQuotaExceeded" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/x/users/{handle}:
    get:
      tags: [X Lookups]
      operationId: lookupXUser
      summary: Look up one public profile live
      description: |
        Read one public X profile as it is right now: bio, location, link,
        follower and following counts, post count, join date and pinned post
        id. The handle may be given with or without a leading `@`.

        Owner-scoped, no `account_id`. Costs ONE live-enrichment unit and
        draws on the shared 300/day live-lookup allowance. Repeat lookups
        within 15 minutes may be served from a server-side cache.

        This is a public lookup for ANY handle, unlike `GET /v1/contacts/{id}`,
        which is limited to people this account already has a relationship
        with and returns SuperX's stored profile plus the lists they are in.

        The `X-RateLimit-*` headers on this endpoint report the ENRICHMENT
        window, not the ordinary read window.
      parameters:
        - name: handle
          in: path
          required: true
          description: The account's @handle, 1-15 letters, numbers or underscores (a leading @ is allowed).
          schema: { type: string }
      responses:
        "200":
          description: The profile.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/XProfile" }
              example:
                data:
                  x_user_id: "44196397"
                  username: "buildersam"
                  name: "Sam Fields"
                  avatar_url: "https://pbs.twimg.com/profile_images/1/avatar.jpg"
                  bio: "Building small products in public."
                  location: "Lisbon"
                  url: "https://buildersam.com"
                  verified: true
                  followers_count: 8421
                  following_count: 512
                  posts_count: 9210
                  created_at: "2019-03-04T09:12:00.000Z"
                  pinned_post_id: "1938765432109876543"
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/UserNotFound" }
        "429": { $ref: "#/components/responses/LookupQuotaExceeded" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/x/users/{handle}/posts:
    get:
      tags: [X Lookups]
      operationId: getXUserPosts
      summary: Latest posts of a public account
      description: |
        One live timeline page (about 20 posts) for any public account,
        newest first, with engagement counts. The up-to-the-minute source for
        what an account just posted.

        Replies are included and carry `in_reply_to_id`; a post flagged
        `is_pinned` is the account's pinned post and is usually old, not
        recent. `exclude_reposts=true` drops reposts, and because only ONE
        page is fetched, fewer than `limit` posts can come back after the
        filter.

        Owner-scoped, no `account_id`. Costs ONE live-enrichment unit when
        SuperX already knows the handle and TWO when it does not (a live
        profile lookup resolves the handle first);
        `data.profile_resolved_locally` reports which happened. Draws on the
        shared 300/day live-lookup allowance either way.

        The `X-RateLimit-*` headers on this endpoint report the ENRICHMENT
        window, not the ordinary read window.
      parameters:
        - name: handle
          in: path
          required: true
          description: The account's @handle, 1-15 letters, numbers or underscores (a leading @ is allowed).
          schema: { type: string }
        - name: limit
          in: query
          description: Posts to return (1-20).
          schema: { type: integer, minimum: 1, maximum: 20, default: 10 }
        - name: exclude_reposts
          in: query
          description: Return only the account's own posts, dropping reposts.
          schema: { type: boolean, default: false }
      responses:
        "200":
          description: The account's latest posts.
          headers:
            X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
            X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
            X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      handle: { type: string }
                      profile_resolved_locally:
                        type: boolean
                        description: |
                          True when SuperX already knew the handle, so this
                          call cost one unit rather than two.
                      posts:
                        type: array
                        items: { $ref: "#/components/schemas/XPost" }
                      note:
                        type: string
                        description: What this page covers, in plain words.
              example:
                data:
                  handle: "buildersam"
                  profile_resolved_locally: true
                  posts:
                    - id: "1938765432109876543"
                      url: "https://x.com/buildersam/status/1938765432109876543"
                      text: "I built my first product in 30 days with zero audience."
                      created_at: "2026-06-20T15:04:00.000Z"
                      author:
                        x_user_id: "44196397"
                        username: "buildersam"
                        name: "Sam Fields"
                        avatar_url: "https://pbs.twimg.com/profile_images/1/avatar.jpg"
                        verified: true
                        followers_count: 8421
                      metrics:
                        likes: 4211
                        replies: 312
                        reposts: 388
                        quotes: 44
                        views: 512000
                        bookmarks: 1904
                      in_reply_to_id: null
                  note: "One live timeline page, newest first. A post flagged is_pinned is the pinned post and is usually old. Replies are included (they carry in_reply_to_id)."
        "400": { $ref: "#/components/responses/InvalidParameter" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/SubscriptionRequired" }
        "404": { $ref: "#/components/responses/UserNotFound" }
        "429": { $ref: "#/components/responses/LookupQuotaExceeded" }
        "500": { $ref: "#/components/responses/InternalError" }
        "502": { $ref: "#/components/responses/UpstreamError" }
        "503": { $ref: "#/components/responses/UpstreamUnavailable" }

  /v1/docs:
    get:
      tags: [Meta]
      operationId: getDocs
      summary: Machine-readable quickstart
      description: A short markdown quickstart (auth header, base URL, endpoint list, rate limits). No authentication required.
      security: []
      responses:
        "200":
          description: The quickstart document.
          content:
            text/markdown:
              schema:
                type: string
              example: |
                # SuperX API v1 quickstart

                Base URL: `https://api.superx.so/v1`
                ...

components:
  securitySchemes:
    apiKey:
      type: http
      scheme: bearer
      description: A SuperX API key ("sxk_..."), created in the SuperX app under Account > API / MCP / CLI. Keys are server-side secrets.

  headers:
    X-RateLimit-Limit:
      description: The limit of the rate window closest to exhaustion.
      schema:
        type: integer
    X-RateLimit-Remaining:
      description: Requests remaining in that window.
      schema:
        type: integer
    X-RateLimit-Reset:
      description: Unix timestamp (seconds) when that window resets.
      schema:
        type: integer
    X-Credits-Charged:
      description: >-
        AI credits kept for this call. Sent on every endpoint that settles its
        charge before answering, which is all of them except the ones that
        answer 202 and finish in the background.
      schema:
        type: integer
    X-Credits-Remaining:
      description: AI credits left in the account's pool, bonus credits included.
      schema:
        type: integer
    X-Credits-Reset:
      description: Unix timestamp (seconds) when the AI credit pool refills.
      schema:
        type: integer

  parameters:
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: >-
        Unique key (max 64 characters) for safe retries. Replays carry the
        "Idempotency-Replayed" response header set to "true". Keys are
        retained for 24 hours.
      schema:
        type: string
        maxLength: 64

    AccountId:
      name: account_id
      in: query
      required: false
      description: Account to act on, from `GET /v1/accounts`. Defaults to your main account. An id outside your accounts returns `404 account_not_found`.
      schema:
        type: string
    Since:
      name: since
      in: query
      required: false
      description: UTC ISO-8601 lower bound (explicit Z or offset required).
      schema:
        type: string
        format: date-time
    Until:
      name: until
      in: query
      required: false
      description: UTC ISO-8601 upper bound (explicit Z or offset required).
      schema:
        type: string
        format: date-time
    Limit:
      name: limit
      in: query
      required: false
      description: Page size, 1 to 100.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    Page:
      name: page
      in: query
      required: false
      description: 1-based page number.
      schema:
        type: integer
        minimum: 1
        default: 1
    DatasetId:
      name: id
      in: path
      required: true
      description: The dataset id from `GET /v1/datasets`.
      schema:
        type: string
        pattern: "^[A-Za-z0-9_-]{1,64}$"
    SuggestionId:
      name: id
      in: path
      required: true
      description: The suggestion's numeric id, from `GET /v1/workers/suggestions`.
      schema:
        type: integer
        minimum: 1

  schemas:
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              description: Stable machine-readable error code.
            message:
              type: string
            retry_after:
              type: integer
              description: Seconds to wait before retrying (rate-limit errors only).

    CreditExhaustedError:
      type: object
      required: [error]
      description: |
        The `429 ai_credits_exhausted` body: the standard envelope plus the
        credit counters. Sent with a `Retry-After` header and the
        `X-Credits-Remaining` / `X-Credits-Reset` headers. `code` is stable;
        the counters are additive fields, safe to ignore.
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              enum: [ai_credits_exhausted]
            message:
              type: string
            credits_required:
              type: integer
              description: Credits the call needed.
            credits_remaining:
              type: integer
              nullable: true
              description: Credits available when it was refused.
            reset_at:
              type: integer
              nullable: true
              description: Unix timestamp (seconds) when the pool refills.

    ActionLimitedError:
      type: object
      required: [error]
      description: |
        The `429 ai_action_limited` body: a per-day cap on how many times an
        expensive action can run, counted separately from the credit pool and
        from the request rate limits. Sent with a `Retry-After` header. Caps
        reset at midnight UTC.

        Read `scope` before telling a user anything: `account` means the
        plan's own daily cap for that action, `platform` means the
        platform-wide fair-use ceiling on live-data actions, which is shared
        with every SuperX account. A `platform` refusal is not about this
        account's quota, so wait and retry rather than reporting the account
        as out.
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              enum: [ai_action_limited]
            message:
              type: string
            scope:
              type: string
              enum: [account, platform]
              description: |
                Whose ceiling refused the call: this account's own per-day cap
                for the action, or the platform-wide fair-use ceiling on
                live-data actions.
            retry_after:
              type: integer
              description: Seconds to wait before the cap resets.
            reset_at:
              type: integer
              description: Unix seconds when the cap resets (the next UTC midnight).

    LegacyErrorEnvelope:
      type: object
      required: [error]
      description: |
        Legacy shape used ONLY by the shared subscription middleware: a plain
        string `error` field. Seen on `403` when the subscription has lapsed
        (string starts with "subscription_required:") and on `500` when
        subscription verification fails (string starts with "internal_error:").
      properties:
        error:
          type: string
          example: "subscription_required: The SuperX API requires an active subscription"

    Pagination:
      type: object
      required: [page, limit, has_more]
      properties:
        page:
          type: integer
        limit:
          type: integer
        has_more:
          type: boolean
        total:
          type: integer
          description: Present only where cheap to compute.

    CursorPagination:
      type: object
      description: |
        Keyset pagination. `next_cursor` is present only when `has_more` is
        true; pass it back as `cursor` to read the next page. Cursors are
        opaque and are not valid across different queries.
      required: [has_more]
      properties:
        limit:
          type: integer
          description: Absent on endpoints whose page size is fixed upstream.
        has_more:
          type: boolean
        next_cursor:
          type: string

    AudienceMember:
      type: object
      description: |
        One person in a system audience list. Field names match
        ContactListMember so one mapping covers both, but `id` here is the X
        user id rather than a membership id (there is nothing to remove).
      properties:
        id:
          type: string
          description: The person's X user id (same value as x_user_id).
        x_user_id:
          type: string
        username:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        followers_count:
          type: integer
          nullable: true
        following_count:
          type: integer
          nullable: true
        engaged_count:
          type: integer
          description: |
            Replies + reposts to the account in the last 90 days for
            followers/following; this list's own action count for
            repliers/reposters.
        source:
          type: string
          description: The audience kind this row came from.
        added_at:
          type: string
          format: date-time
          nullable: true
          description: |
            When SuperX first saw the follow edge, or the person's most
            recent reply or repost.
        icp_score:
          type: integer
          nullable: true
          description: Always null here; lead scoring belongs to signal-agent deposits.
        icp_rationale:
          type: string
          nullable: true
          description: Always null here.
        can_dm:
          type: boolean
          nullable: true
          description: Whether the person's DMs looked open when SuperX last saw them.

    AudienceMeta:
      type: object
      description: The state of the whole audience list, not of this page.
      properties:
        kind:
          type: string
          enum: [followers, following, repliers, reposters]
        account_id:
          type: string
          description: |
            The account id you pass as `account_id`, echoed back. Same id
            space as every other /v1 surface.
        x_account_id:
          type: string
          description: The X user id the audience is stored under.
        synced_count:
          type: integer
          description: |
            How many people SuperX has synced for this list. For followers
            and following this counts synced edges rather than re-checking
            every one against X, so it may exceed the rows a full walk
            returns: edges that were later removed are still counted.
        status:
          type: string
          description: |
            Sync state: missing (never started), pending, running, complete
            or paused for the follow lists; missing, running or complete for
            repliers and reposters.
        backfill_cap:
          type: integer
          nullable: true
          description: |
            How deep the plan syncs the follow graph. Null for repliers and
            reposters.
        is_capped:
          type: boolean
          nullable: true
          description: |
            True when the account is deeper than backfill_cap, so the list is
            the most recent slice rather than everyone. Null for repliers and
            reposters.
        window_days:
          type: integer
          nullable: true
          description: |
            The rolling window repliers and reposters are measured over (90).
            Null for the follow lists, which are not windowed.

    Account:
      type: object
      properties:
        id:
          type: string
        username:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        is_main:
          type: boolean
        shared:
          type: boolean
          description: True when this account was shared with you by its owner (manual share or team share) rather than owned by you.
        permission:
          type: string
          enum: [full, editor]
          description: What you may change on this account. Your own accounts are always `full`. On an `editor` share you can change queue settings but not context settings.

    Me:
      type: object
      properties:
        owner:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
              nullable: true
            email:
              type: string
              nullable: true
        plan:
          type: object
          nullable: true
          properties:
            tier:
              type: integer
            label:
              type: string
            is_trial:
              type: boolean
        key:
          type: object
          properties:
            id:
              type: integer
            name:
              type: string
            key_prefix:
              type: string
            scopes:
              type: array
              items:
                type: string
                enum: [read, write]
            created_at:
              type: string
              format: date-time
              nullable: true
            last_used_at:
              type: string
              format: date-time
              nullable: true
        credits:
          type: object
          nullable: true
          description: >-
            The AI credit pool the paid endpoints draw from, or null when it
            cannot be read. One credit is one cent of measured model spend.
          properties:
            remaining:
              type: integer
              description: Credits left, bonus credits included.
            pool:
              type: integer
              description: Size of the plan's pool for the current period.
            period:
              type: string
              enum: [month, day]
              description: Paid plans refill monthly; trials refill daily.
            period_end:
              type: string
              format: date-time
              nullable: true
              description: When the pool refills.
            bonus:
              type: integer
              description: Bonus credits, spent before the pool.

    PostMetrics:
      type: object
      properties:
        likes:
          type: integer
        replies:
          type: integer
        reposts:
          type: integer
        quotes:
          type: integer
        bookmarks:
          type: integer
        impressions:
          type: integer

    Post:
      type: object
      properties:
        id:
          type: string
        text:
          type: string
        posted_at:
          type: string
          format: date-time
          nullable: true
        url:
          type: string
        is_reply:
          type: boolean
        in_reply_to_id:
          type: string
          nullable: true
        is_quote:
          type: boolean
        thread_id:
          type: string
          nullable: true
        metrics:
          $ref: "#/components/schemas/PostMetrics"
        metrics_pending:
          type: boolean
          description: >-
            Present (and true) only on a reply that was sent from the SuperX
            app in the last few hours and whose engagement X has not reported
            yet. Its `metrics` are all zero and are not a measurement. Absent
            on every other post. See GET /v1/replies.

    Analytics:
      type: object
      properties:
        range:
          type: object
          properties:
            since:
              type: string
              format: date-time
            until:
              type: string
              format: date-time
        totals:
          $ref: "#/components/schemas/AnalyticsTotals"
        followers:
          type: object
          properties:
            start:
              type: integer
              nullable: true
            end:
              type: integer
              nullable: true
            change:
              type: integer
              nullable: true
        series:
          type: array
          items:
            $ref: "#/components/schemas/AnalyticsDay"

    AnalyticsTotals:
      type: object
      properties:
        posts:
          type: integer
        likes:
          type: integer
        replies:
          type: integer
        reposts:
          type: integer
        quotes:
          type: integer
        bookmarks:
          type: integer
        impressions:
          type: integer

    AnalyticsDay:
      allOf:
        - $ref: "#/components/schemas/AnalyticsTotals"
        - type: object
          properties:
            date:
              type: string
              description: UTC day, YYYY-MM-DD.
            followers:
              type: integer
              nullable: true
              description: Follower sample for the day, when tracked.

    InspirationPost:
      type: object
      properties:
        id:
          type: string
        url:
          type: string
          description: Link to the post on X.
        text:
          type: string
        posted_at:
          type: string
          format: date-time
          nullable: true
        lang:
          type: string
          nullable: true
        likes:
          type: integer
        replies:
          type: integer
        reposts:
          type: integer
        bookmarks:
          type: integer
        impressions:
          type: integer
        outlier_score:
          type: number
          nullable: true
          description: Engagement rate vs the expected rate for the author's follower tier, x10 (10 = expected, higher = overperformed). Null when the author's follower count is unknown.
        author:
          type: object
          properties:
            handle:
              type: string
              nullable: true
            name:
              type: string
              nullable: true
            followers:
              type: integer
            verified:
              type: boolean

    Contact:
      type: object
      properties:
        id:
          type: string
        username:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        replies_90d:
          type: integer
        reposts_90d:
          type: integer

    ContactReply:
      type: object
      properties:
        id:
          type: string
        text:
          type: string
        posted_at:
          type: string
          format: date-time
          nullable: true
        metrics:
          type: object
          properties:
            likes:
              type: integer
        in_reply_to:
          type: object
          nullable: true
          properties:
            id:
              type: string
            text:
              type: string
            posted_at:
              type: string
              format: date-time
              nullable: true

    ReceivedReply:
      type: object
      properties:
        id:
          type: string
        text:
          type: string
        posted_at:
          type: string
          format: date-time
          nullable: true
        metrics:
          type: object
          properties:
            likes:
              type: integer
        author:
          type: object
          description: The replier. Profile fields are null when no stored profile exists.
          properties:
            id:
              type: string
            username:
              type: string
              nullable: true
            name:
              type: string
              nullable: true
            avatar_url:
              type: string
              nullable: true
        in_reply_to:
          type: object
          nullable: true
          properties:
            id:
              type: string
            text:
              type: string
            posted_at:
              type: string
              format: date-time
              nullable: true

    ContactDetail:
      type: object
      description: One person's stored profile plus the lists you created that they are in.
      properties:
        id:
          type: string
          description: The person's numeric X user id.
        username:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        bio:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        url:
          type: string
          nullable: true
          description: The website on their X profile, expanded.
        followers_count:
          type: integer
          nullable: true
        following_count:
          type: integer
          nullable: true
        is_blue_verified:
          type: boolean
          nullable: true
          description: null when the verified state is unknown; never fabricated.
        verified_type:
          type: string
          nullable: true
        lists:
          type: array
          description: Lists you created that this person is a member of. System lists are never listed here.
          items:
            type: object
            properties:
              id:
                type: string
              name:
                type: string
              member_id:
                type: string
                description: The membership id the remove endpoints take.

    ContactNote:
      type: object
      description: A private note about one person. Notes live inside SuperX and are never posted.
      properties:
        id:
          type: string
        body:
          type: string
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true
        created_by:
          type: object
          description: The account the note was written as. Notes written through the API carry the account named by account_id.
          properties:
            x_user_id:
              type: string
              nullable: true
            username:
              type: string
              nullable: true

    ContactList:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        is_system:
          type: boolean
        kind:
          type: string
          nullable: true
          enum: [followers, following, repliers, reposters, null]
          description: Set on system lists only.
        member_count:
          type: integer
          description: Members in the list. Always 0 for system lists (their membership is maintained automatically and is not exposed here).
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true

    ContactListMember:
      type: object
      properties:
        id:
          type: string
          description: The membership id used by the remove endpoint.
        x_user_id:
          type: string
        username:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        followers_count:
          type: integer
          nullable: true
        following_count:
          type: integer
          nullable: true
        engaged_count:
          type: integer
          description: Replies + reposts to the account in the last 90 days.
        source:
          type: string
          nullable: true
          description: How the member entered the list (for example manual).
        added_at:
          type: string
          format: date-time
          nullable: true

    AddedContactListMember:
      type: object
      description: Compact member shape returned by the add endpoint.
      properties:
        id:
          type: string
        x_user_id:
          type: string
        username:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        added_at:
          type: string
          format: date-time
          nullable: true

    BulkAddResult:
      type: object
      description: |
        Counters from a bulk member add. added + duplicates + failed equals the number of resolvable ids; not_found ids never reached the list.
        Members are inserted one at a time upstream, so a 502 or 503 can land after some of them were already added. Retrying the same request is safe: the list dedupes on (list, person), so anything that got in the first time comes back in `duplicates` rather than being added twice.
      properties:
        added:
          type: integer
          description: New members created.
        duplicates:
          type: integer
          description: Ids already in the list. Nothing was written for them.
        failed:
          type: integer
          description: Resolvable ids whose insert failed.
        not_found:
          type: array
          description: Submitted ids with no stored profile. Nothing was sent for them and no placeholder member was created; add these by handle instead.
          items:
            type: string
        total_in_list_after:
          type: integer
          description: Members in the list once the call finished.

    BulkRemoveResult:
      type: object
      properties:
        deleted:
          type: integer
          description: Members actually removed. Lower than the ids sent when some were not in this list.
        total_in_list_after:
          type: integer

    SignalAgentSignal:
      type: object
      description: One watched signal on an agent. Which fields are set depends on the type (handle/name/avatar for profile and follower watches, query for keyword watches, list_name for list watches).
      properties:
        id:
          type: integer
        type:
          type: string
          enum: [profile_watch, follower_watch, keyword_watch, list_watch]
        handle:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        avatar:
          type: string
          nullable: true
        query:
          type: string
          nullable: true
        list_name:
          type: string
          nullable: true
        status:
          type: string
        status_detail:
          type: string
          nullable: true
        last_run_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true

    SignalAgent:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        status:
          type: string
          description: active, paused, or error.
        status_detail:
          type: string
          nullable: true
        icp_description:
          type: string
          description: The ideal customer profile the agent scores people against.
        precision_mode:
          type: string
          enum: [high, discovery]
        destination_list_id:
          type: string
          nullable: true
          description: The contact list deposits go to; joins to `GET /v1/contact-lists`.
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true
        deposited_count:
          type: integer
          description: Leads already saved to the destination list.
        last_checked:
          type: string
          format: date-time
          nullable: true
          description: The most recent signal run across the agent's signals.
        signals:
          type: array
          items: { $ref: "#/components/schemas/SignalAgentSignal" }

    SignalLead:
      type: object
      properties:
        id:
          type: integer
        agent:
          type: object
          description: The agent that found this lead, with its current status.
          properties:
            id:
              type: integer
            name:
              type: string
            status:
              type: string
        user:
          type: object
          description: The lead's stored profile. Profile fields are null when no snapshot is stored; the lead is still returned.
          properties:
            x_user_id:
              type: string
              nullable: true
            username:
              type: string
              nullable: true
            name:
              type: string
              nullable: true
            avatar_url:
              type: string
              nullable: true
            followers_count:
              type: integer
              nullable: true
            following_count:
              type: integer
              nullable: true
            location:
              type: string
              nullable: true
        icp_score:
          type: integer
          nullable: true
          description: How strongly the person matched the agent's ideal customer profile.
        icp_rationale:
          type: string
          nullable: true
        deposited:
          type: boolean
          description: Whether the lead has been saved to the agent's contact list yet.
        deposited_at:
          type: string
          format: date-time
          nullable: true
        discovered_at:
          type: string
          format: date-time
          nullable: true
        feedback:
          type: string
          nullable: true
          enum: [fit, not_fit, null]
          description: The account's own verdict on this lead, set through `POST /v1/signals/leads/{id}/feedback`. Null when no verdict was recorded.
        feedback_at:
          type: string
          format: date-time
          nullable: true
          description: When the verdict was recorded.
        provenance:
          type: object
          nullable: true
          description: How the lead was discovered. Null when no provenance is stored.
          properties:
            type:
              type: string
              nullable: true
            action:
              type: string
              nullable: true
            source_ref:
              type: string
              nullable: true
            tweet_id:
              type: string
              nullable: true
            tweet_text:
              type: string
              nullable: true
            target_handle:
              type: string
              nullable: true

    SignalInput:
      type: object
      required: [type]
      description: >-
        One thing for a signal agent to watch. The target field depends on
        `type`: `query` for keyword_watch, `handle` for profile_watch and
        follower_watch, `list` for list_watch.
      properties:
        type:
          type: string
          enum: [keyword_watch, profile_watch, follower_watch, list_watch]
        query:
          type: string
          maxLength: 180
          description: keyword_watch only. A plain-language description of what the target customer posts about, or an X search. Operators pass through; engagement filters such as min_faves are rejected.
        handle:
          type: string
          maxLength: 15
          description: profile_watch and follower_watch only. An X username, with or without a leading @.
        list:
          type: string
          description: list_watch only. A public X list id, or a link like https://x.com/i/lists/1234567890.

    SignalWarning:
      type: object
      description: One entry a create could not add. The agent was still created from the entries that landed.
      properties:
        type:
          type: string
          description: The signal type of the rejected entry.
        target:
          type: string
          description: The query, handle, or list reference as you sent it.
        code:
          type: string
          description: >-
            Why it was rejected: `user_not_found`, `x_list_not_found`,
            `duplicate_signal`, `cap_reached`, `invalid_parameter`,
            `agent_not_found`, `upstream_unavailable`, or `upstream_error`.

    LeadFeedback:
      type: object
      properties:
        id:
          type: integer
          description: The lead id.
        feedback:
          type: string
          nullable: true
          enum: [fit, not_fit, null]
        feedback_at:
          type: string
          format: date-time
          nullable: true

    CoverStyle:
      type: object
      properties:
        id:
          type: string
          description: Pass this as `style_id` to POST /v1/articles/{id}/cover.
        name:
          type: string
        thumbnail_url:
          type: string
          nullable: true
          description: A preview image of the style. Null until a render made with it backfills one.
        source:
          type: string
          nullable: true
          description: "`cover` = saved from a generated cover, `custom` = typed by hand."
        created_at:
          type: string
          format: date-time
          nullable: true
        last_used_at:
          type: string
          format: date-time
          nullable: true

    Dataset:
      type: object
      properties:
        id:
          type: string
          description: Pass this to the other dataset endpoints.
        title:
          type: string
        source:
          type: string
          enum: [repliers, quoters, reposters, list_members, my_replies, my_posts, research]
          description: What the collection walked. `my_posts` / `my_replies` hold posts, everything else holds people.
        status:
          type: string
          enum: [ready, collecting, failed]
        target_ref:
          type: string
          description: What the collection was pointed at - a post id, an X list id, or free text for a research brief.
        x_account_id:
          type: string
          description: The X account the collection ran as. Not a filter - datasets belong to the key owner, not to one account.
        ask_chat_id:
          type: string
          nullable: true
          description: The Ask SuperX chat the dataset was built in, when known.
        columns:
          type: array
          items:
            type: string
          description: The row keys this dataset carries, in export order.
        row_count:
          type: integer
        scanned_count:
          type: integer
          description: What the walk went through (people datasets), or the rows kept (own-content datasets).
        total_estimate:
          type: integer
          nullable: true
        coverage_complete:
          type: boolean
          description: False when the collection hit its row cap before the end.
        has_people:
          type: boolean
          description: False for own-content datasets - there is nobody in them to add to a contact list.
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          description: Datasets are kept for 30 days.

    DatasetProgress:
      type: object
      description: Live progress of a collection that is still running.
      properties:
        page:
          type: integer
        scanned:
          type: integer
        matched:
          type: integer
        total_estimate:
          type: integer
          nullable: true
        started_at:
          type: string
          format: date-time

    DatasetDetail:
      allOf:
        - $ref: "#/components/schemas/Dataset"
        - type: object
          properties:
            coverage:
              type: string
              description: An honest one-line coverage statement. Ready datasets only.
            filters:
              type: object
              additionalProperties: true
              description: The filters the collection applied. Ready datasets only.
            progress:
              $ref: "#/components/schemas/DatasetProgress"
            failure_note:
              type: string
              description: Why the collection stopped. Failed datasets only.

    CreateDatasetRequest:
      type: object
      required: [source]
      properties:
        source:
          type: string
          enum: [repliers, quoters, reposters, list_members, my_replies, my_posts, research]
          description: |
            Who to collect. repliers, quoters and reposters need a post
            target; list_members needs a public X list; my_posts and
            my_replies read the account's own synced posts and take no
            target.

            `research` is different: it reads people's recent posts and writes
            an outreach brief per person instead of walking an audience. It
            takes exactly ONE of `handles`, `list_id`, `agent_id` or
            `dataset_id`, plus `focus` and a `max_rows` of 1-25 (default 10),
            and ignores `target` and `filters`.
        target:
          type: string
          description: |
            An x.com post URL or numeric post id, or for list_members an
            x.com list URL (/i/lists/<id>) or numeric list id. Omit for
            my_posts and my_replies.
        title:
          type: string
          maxLength: 120
          description: Title for the dataset. A sensible one is generated when omitted.
        max_rows:
          type: integer
          minimum: 1
          maximum: 1000
          default: 500
          description: |
            Rows to collect at most. For `source: "research"` the range is
            1-25 and the default is 10 (one profile per row).
        account_id:
          type: string
          description: Which of your accounts to collect as. Omit for the main account.
        handles:
          type: array
          maxItems: 25
          items:
            type: string
          description: "`research` only: research these X handles (with or without the @). Exactly one source field."
        list_id:
          type: string
          description: "`research` only: research the members of this contact list. Exactly one source field."
        agent_id:
          type: integer
          minimum: 1
          description: "`research` only: research this signal agent's leads. Exactly one source field."
        dataset_id:
          type: string
          description: "`research` only: research the people in this dataset. Exactly one source field."
        focus:
          type: string
          maxLength: 300
          description: "`research` only: an optional steer, e.g. \"founders who might need audience-growth tooling\"."
        filters:
          type: object
          properties:
            keywords:
              type: array
              items:
                type: string
              description: |
                Keep only rows whose reply, quote or (own content) post text
                contains one of these. Not applied to reposters or list
                members, whose rows carry no text.
            bio_keywords:
              type: array
              items:
                type: string
              description: Keep only people whose X bio contains one of these. Ignored for own content.
            min_followers:
              type: integer
              minimum: 0
              description: Keep only people with at least this many followers. Ignored for own content.
            require_website:
              type: boolean
              description: Keep only people with a website link in their profile. Ignored for own content.
            require_can_dm:
              type: boolean
              description: Keep only people whose DMs look open. Ignored for own content.
            since_days:
              type: integer
              minimum: 1
              description: "Own content only: keep posts from the last N days."
            sort:
              type: string
              enum: [recent, likes, impressions]
              description: "Own content only: which posts to keep when max_rows cuts the list."

    LeadSearchRequest:
      type: object
      required: [keywords, icp_description]
      properties:
        keywords:
          oneOf:
            - type: string
            - type: array
              items:
                type: string
          description: |
            Plain-language phrases these people would post, as one
            comma-separated string or a list of strings (a list is joined with
            commas). 3-300 characters in total. No search operators.
        icp_description:
          type: string
          minLength: 3
          maxLength: 500
          description: |
            Who counts as a good lead, in one or two sentences: role, domain,
            and the buying intent or pain that qualifies them.
        precision:
          type: string
          enum: [high, discovery]
          default: discovery
          description: high keeps only confident matches; discovery also returns adjacent ones.
        max_leads:
          type: integer
          minimum: 1
          maximum: 30
          default: 10
        account_id:
          type: string
          description: Which of your accounts to search as. Omit for the main account.

    LeadSearchLead:
      type: object
      properties:
        user:
          type: object
          properties:
            x_user_id:
              type: string
              description: The X account id, as a string (X ids exceed 2^53).
            username:
              type: string
              nullable: true
              description: X handle without the @.
            name:
              type: string
              nullable: true
            avatar_url:
              type: string
              nullable: true
            followers_count:
              type: integer
              nullable: true
            posts_count:
              type: integer
              nullable: true
              description: Lifetime posts, not recent activity.
        icp_score:
          type: integer
          nullable: true
          description: How well this person matches the profile, 2-5 (anything lower is dropped).
        icp_rationale:
          type: string
          nullable: true
        provenance:
          type: object
          description: How the person was found. Mirrors the provenance on stored signal leads.
          properties:
            action:
              type: string
              description: Always "authored" here, meaning they wrote a matching post.
            tweet_text:
              type: string
              nullable: true
              description: The matched post, shortened.
            target_handle:
              type: string
              nullable: true

    LeadSearchResult:
      type: object
      properties:
        query_used:
          type: string
          description: The search actually run against X.
        query_source:
          type: string
          enum: [compiled, keywords]
          description: compiled = your phrases were turned into a query by a model; keywords = the deterministic fallback.
        precision:
          type: string
          enum: [high, discovery]
        requests_used:
          type: integer
          description: Live requests this search spent.
        posts_scanned:
          type: integer
        candidates_scored:
          type: integer
        credits_charged:
          type: integer
        leads:
          type: array
          items: { $ref: "#/components/schemas/LeadSearchLead" }

    ToolMeta:
      type: object
      description: What the call cost and which account it ran as.
      properties:
        credits_charged:
          type: integer
          description: AI credits kept for this call, settled from the models' real cost.
        account_id:
          type: string
          description: The X account the call ran as.

    ReplyPostShape:
      type: object
      required: [text]
      description: A post, supplied by you rather than read from X.
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 4000
        author_name:
          type: string
          maxLength: 200
          description: The author's display name.
        author_handle:
          type: string
          maxLength: 200
          description: The author's handle. A leading @ is accepted and stripped.

    ReplyDraftRequest:
      type: object
      description: Exactly one of post_id and post is required.
      properties:
        post_id:
          type: string
          pattern: "^[0-9]{1,25}$"
          description: An X post id. The API reads the post live, which costs one live lookup on top of the credit.
        post:
          $ref: "#/components/schemas/ReplyPostShape"
        conversation:
          type: array
          maxItems: 10
          description: The thread so far, OLDEST FIRST, so the reply fits the conversation.
          items: { $ref: "#/components/schemas/ReplyPostShape" }
        quoted_post:
          allOf:
            - $ref: "#/components/schemas/ReplyPostShape"
          description: The post that the post being replied to quotes. Filled in automatically when you pass post_id.
        thoughts:
          type: string
          maxLength: 2000
          description: What the person wants the reply to convey. Ask them; do not invent an opinion for them.
        tone:
          type: string
          enum: [engaging, humorous, creative, sarcastic, inspirational, concise]
        account_id:
          type: string

    RemixPostRequest:
      type: object
      required: [text, closeness]
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 4000
          description: The post to remix.
        closeness:
          type: integer
          minimum: 0
          maximum: 100
          description: 0 keeps only the idea, 100 stays very close to the original wording.
        instructions:
          type: string
          maxLength: 500
          description: Extra direction for this remix.
        account_id:
          type: string

    InlineEditRequest:
      type: object
      required: [text]
      description: At least one of instruction and edit_type is required.
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 4000
          description: The selected piece of the post to edit.
        full_text:
          type: string
          maxLength: 4000
          description: The whole post the selection sits in, so the edit matches its style.
        instruction:
          type: string
          maxLength: 500
          description: "Free-text direction, e.g. 'make this one line, lowercase'."
        edit_type:
          type: string
          enum:
            [
              grammar,
              translate,
              hook,
              details,
              concise,
              engaging,
              humorous,
              creative,
              sarcastic,
              inspirational,
              fix-grammar,
              expand,
              simplify,
              rewrite,
              change-tone,
            ]
          description: A preset edit. The last five are older names kept working.
        thread_context:
          type: array
          maxItems: 25
          description: The thread's other parts, in order, for context only.
          items:
            type: string
            maxLength: 4000
        account_id:
          type: string

    RephraseRequest:
      type: object
      required: [type, text]
      properties:
        type:
          type: string
          enum:
            [
              improve,
              grammar,
              translate,
              hook,
              details,
              clarity,
              engaging,
              humorous,
              positive,
              creative,
              sarcastic,
              inspirational,
              concise,
            ]
          description: Which rewrite to apply.
        text:
          type: string
          minLength: 1
          maxLength: 4000
        account_id:
          type: string

    FactCheckRequest:
      type: object
      required: [text]
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 4000
          description: The statement to check.
        account_id:
          type: string

    FactCheckResult:
      type: object
      properties:
        result:
          type: string
          enum: ["true", "false", unknown]
          description: The model's verdict. `unknown` is a real answer, not an error.
        comment:
          type: string
          nullable: true
          description: One sentence explaining the verdict.
        sources:
          type: array
          description: The search results the verdict was read from. Show these alongside it.
          items:
            type: object
            properties:
              link: { type: string }
              title: { type: string }
              snippet: { type: string }

    AlgorithmPredictRequest:
      type: object
      required: [version_a, version_b]
      properties:
        version_a:
          type: string
          minLength: 1
          maxLength: 4000
        version_b:
          type: string
          minLength: 1
          maxLength: 4000
        account_id:
          type: string

    AlgorithmPredictVersion:
      type: object
      properties:
        score:
          type: integer
          description: 0 to 100. A model's opinion, for comparing the two versions against each other.
        improvements:
          type: array
          items:
            type: object
            properties:
              text:
                type: string
                description: The change, in a handful of words.
              tooltip:
                type: string
                description: Why it would help, with an example.

    DraftOutreachRequest:
      type: object
      required: [format]
      properties:
        format:
          type: string
          minLength: 10
          maxLength: 1000
          description: |
            The template or example message every draft should follow. Keep
            `[name]`, `[first]` and `[handle]` tokens if you want them filled
            in per recipient at send time.
        instructions:
          type: string
          maxLength: 500
          description: "Optional extra steer: tone, what to emphasize, what to avoid."
        account_id:
          type: string
          description: Which of your accounts to draft as. Omit for the main account.

    OutreachDraftsResult:
      type: object
      properties:
        dataset_id:
          type: string
        title:
          type: string
        total:
          type: integer
          description: Rows in the dataset.
        drafted:
          type: integer
          description: Messages personalized from real hook quotes.
        generic:
          type: integer
          description: Messages that follow the format with no personal claims, because the brief had no usable hook.
        failed:
          type: integer
          description: Rows the model could not draft for. Their previous message, if any, was cleared.
        contaminated:
          type: integer
          description: Drafts discarded because they named a different recipient. Run again to retry those rows.
        long_messages:
          type: integer
          description: Drafts over 500 characters. Worth tightening before anyone sends them.
        sample_messages:
          type: array
          description: A few drafts, shortened. Read them all with GET /v1/datasets/{id}/rows.
          items:
            type: object
            properties:
              handle:
                type: string
                nullable: true
              message:
                type: string

    RefineDatasetRequest:
      type: object
      required: [criterion]
      properties:
        criterion:
          type: string
          minLength: 3
          maxLength: 500
          description: |
            What the rows to match look like, judged on each row's own text,
            e.g. "supportive or neutral; not mean, sarcastic, or hostile".
        keep_matching:
          type: boolean
          default: true
          description: true keeps the rows that MATCH the criterion; false keeps the rows that do NOT.
        sort_by:
          type: string
          enum: [followers, likes, none]
          default: none
          description: Sort the kept rows descending before the limit is applied.
        limit:
          type: integer
          minimum: 1
          maximum: 1000
          description: Keep at most this many rows after filtering and sorting.
        title:
          type: string
          maxLength: 120
          description: Title for the new dataset. A sensible one is generated when omitted.
        account_id:
          type: string
          description: Which of your accounts to refine as. Omit for the main account.

    RefineMeta:
      type: object
      description: Counts for the refinement. On a 202 only the source id and row count are known.
      properties:
        source_dataset_id:
          type: string
        rows_with_text:
          type: integer
          description: Rows that will be judged (202 only).
        kept:
          type: integer
        dropped:
          type: integer
        unclear:
          type: integer
          description: Rows the classifier could not judge. They were KEPT.
        limited:
          type: integer
          description: Kept rows cut by `limit`.
        self_excluded:
          type: integer
          description: Rows of your own account, always removed.
        credits_charged:
          type: integer

    DatasetRow:
      type: object
      additionalProperties: true
      description: |
        One collected row, verbatim. Its keys are the dataset's `columns`
        plus, on people datasets, `user_id` (the X account id as a string)
        and `avatar`. Values are not shortened.

    DatasetContactsResult:
      type: object
      properties:
        added:
          type: integer
        duplicates:
          type: integer
          description: People already in the list. Never an error.
        failed:
          type: integer
        skipped_without_id:
          type: integer
          description: |
            Rows with no X account id or no usable handle. Nothing was sent
            for them. Repeat rows for a person already collected are deduped
            silently and are not counted here.
        total_in_list_after:
          type: integer

    DatasetNotReadyError:
      allOf:
        - $ref: "#/components/schemas/ErrorEnvelope"
        - type: object
          properties:
            error:
              type: object
              properties:
                dataset_status:
                  type: string
                  enum: [collecting, failed]
                  description: |
                    The dataset's current status. `collecting` finishes on its
                    own, `failed` has to be rebuilt in the SuperX app. Named
                    `dataset_status` so it never reads as the HTTP status.
      description: |
        The `409 dataset_not_ready` body: the standard envelope plus the
        dataset's current status, so a poller can tell "wait" from "wrong id".

    XPost:
      type: object
      description: |
        One public X post read live. Text is never shortened. Optional keys
        (`is_pinned`, `is_repost`, `entities`, `media`, `card`, `quoted_post`)
        are present only when the post has them.
      properties:
        id:
          type: string
          description: Numeric X post id, as a string.
        url:
          type: string
        text:
          type: string
          description: The full post text.
        created_at:
          type: string
          format: date-time
          nullable: true
        author:
          type: object
          nullable: true
          properties:
            x_user_id: { type: string }
            username: { type: string, nullable: true }
            name: { type: string, nullable: true }
            avatar_url: { type: string, nullable: true }
            verified:
              type: boolean
              nullable: true
              description: |
                Null when the upstream payload for that read does not carry
                it (single-post lookups and their quoted posts).
            followers_count: { type: integer, nullable: true }
        metrics:
          type: object
          properties:
            likes: { type: integer }
            replies: { type: integer }
            reposts: { type: integer }
            quotes: { type: integer }
            views: { type: integer, nullable: true }
            bookmarks: { type: integer }
        in_reply_to_id:
          type: string
          nullable: true
          description: Set when this post is a reply; the id of the post it answers.
        is_pinned:
          type: boolean
          description: Present and true only on the account's pinned post (timeline reads).
        is_repost:
          type: boolean
          description: Present and true only on a repost of someone else's post.
        entities:
          type: array
          description: t.co link mappings, so short links can be rendered as their real destination.
          items:
            type: object
            properties:
              url: { type: string }
              display_url: { type: string, nullable: true }
              expanded_url: { type: string, nullable: true }
        media:
          type: array
          description: Attached media. `video_url` is a playable MP4 for videos and GIFs.
          items:
            type: object
            properties:
              type: { type: string, example: photo }
              url: { type: string, nullable: true }
              video_url: { type: string, nullable: true }
              width: { type: integer }
              height: { type: integer }
        card:
          type: object
          description: Link preview card, when the post has one.
          properties:
            url: { type: string, nullable: true }
            title: { type: string }
            description: { type: string, nullable: true }
            domain: { type: string, nullable: true }
            image: { type: string, nullable: true }
        quoted_post:
          allOf:
            - $ref: "#/components/schemas/XPost"
          nullable: true
          description: |
            The post this one quotes, ONE level deep only - a quote of a
            quote is not nested further.

    XProfile:
      type: object
      description: One public X profile read live.
      properties:
        x_user_id: { type: string }
        username: { type: string, nullable: true }
        name: { type: string, nullable: true }
        avatar_url: { type: string, nullable: true }
        bio:
          type: string
          description: Empty string when the account has no bio.
        location: { type: string, nullable: true }
        url:
          type: string
          nullable: true
          description: The link in the profile, expanded past t.co where X provides it.
        verified: { type: boolean }
        followers_count: { type: integer, nullable: true }
        following_count: { type: integer, nullable: true }
        posts_count: { type: integer, nullable: true }
        created_at:
          type: string
          format: date-time
          nullable: true
          description: When the account joined X.
        pinned_post_id:
          type: string
          nullable: true
          description: Pass it to GET /v1/x/posts/{id} for the pinned post itself.
        bio_entities:
          type: array
          description: t.co link mappings for links in the bio, when it has any.
          items:
            type: object
            properties:
              url: { type: string }
              display_url: { type: string, nullable: true }
              expanded_url: { type: string, nullable: true }

    MediaItem:
      type: object
      description: One item from the cross-platform inspiration media index.
      properties:
        id: { type: string }
        platform:
          type: string
          enum: [x, instagram, youtube, threads, reddit, linkedin]
        title: { type: string }
        caption: { type: string, nullable: true }
        summary:
          type: string
          nullable: true
          description: A short description of what the media shows.
        content_type: { type: string, nullable: true }
        source_url:
          type: string
          nullable: true
          description: |
            The original post on its own platform, and the ONLY link returned
            here. The media files themselves (thumbnail, video) are not
            exposed through the API; open `source_url` for the post.
        author_username: { type: string, nullable: true }
        author_url: { type: string, nullable: true }
        posted_at: { type: string, format: date-time, nullable: true }
        like_count: { type: integer }
        view_count: { type: integer }
        comment_count: { type: integer }
        share_count: { type: integer }
        engagement_tier: { type: string, nullable: true }
        score:
          type: number
          description: |
            Relevance against your query, 0 to 1, higher is closer. Present
            in `search` mode only (never when browsing).

    EngageFeed:
      type: object
      properties:
        id:
          type: string
          description: Pass this to GET /v1/engage/feeds/{id}/posts.
        name:
          type: string
        type:
          type: string
          enum: [keywords, list, x_list]
          description: keywords = a saved keyword search, list = a SuperX contact list, x_list = an X list.
        keywords:
          type: array
          description: Keyword feeds only.
          items:
            type: string
        list_id:
          type: string
          description: Contact list id, list feeds only.
        x_list_id:
          type: string
          description: X list id, x_list feeds only.
        list_name:
          type: string
          description: List feeds only.
        active:
          type: boolean
          description: Whether this is the feed currently selected in the app.
        fetch_units:
          type: integer
          description: >-
            Feed-bucket units one fetch of this feed costs. List feeds count
            as up to 3 (a pure imported X list resolves to 1 at fetch time);
            everything else is 1.

    EngageFeedPostAuthor:
      type: object
      nullable: true
      properties:
        id:
          type: string
          nullable: true
        username:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
          description: The author's X bio. Not always present on list feeds.
        followers_count:
          type: integer
          nullable: true
        following_count:
          type: integer
          nullable: true
        verified:
          type: boolean

    EngageFeedPost:
      type: object
      properties:
        id:
          type: string
        text:
          type: string
          description: Post text with shortened links expanded.
        created_at:
          type: string
          format: date-time
          nullable: true
        url:
          type: string
        author:
          $ref: "#/components/schemas/EngageFeedPostAuthor"
        metrics:
          type: object
          properties:
            likes:
              type: integer
            replies:
              type: integer
            reposts:
              type: integer
            bookmarks:
              type: integer
            impressions:
              type: integer
        media:
          type: array
          description: Image URLs attached to the post, best effort.
          items:
            type: string
        quoted_post:
          type: object
          description: Present only when the post quotes another one.
          properties:
            id:
              type: string
            text:
              type: string
            author_username:
              type: string
              nullable: true
        replied:
          type: boolean
          description: Present only when include_replied=true. True when the account already replied to this post.

    MentionParentPost:
      type: object
      description: A post a mention is replying to.
      properties:
        id:
          type: string
        text:
          type: string
        created_at:
          type: string
          format: date-time
          nullable: true
        author:
          type: object
          nullable: true
          properties:
            id:
              type: string
            username:
              type: string
              nullable: true
            name:
              type: string
              nullable: true
            avatar_url:
              type: string
              nullable: true
        parent_post:
          type: object
          description: |
            One further level of ancestry, present when the parent was itself
            a reply. Same shape as this object, without nesting any deeper.

    MentionPost:
      allOf:
        - $ref: "#/components/schemas/EngageFeedPost"
        - type: object
          properties:
            mention_type:
              type: string
              nullable: true
              enum: [reply, mention]
              description: |
                reply = a direct reply to one of the account's posts;
                mention = any other @-mention (standalone, in a chain, or
                tagged in someone else's reply).
            parent_post:
              allOf:
                - $ref: "#/components/schemas/MentionParentPost"
              nullable: true
              description: Null when the mention is not a reply, or the parent could not be fetched.

    Worker:
      type: object
      description: A scheduled AI writer set up in the SuperX app. Read-only here.
      properties:
        id:
          type: integer
        name:
          type: string
        status:
          type: string
          description: active or paused.
        voice_source:
          type: string
          enum: [my_voice, creator_style, hybrid]
          description: Whose voice the Worker writes in.
        voice_creator:
          type: object
          nullable: true
          description: The creator whose style is being borrowed, when voice_source is creator_style or hybrid.
        topic_source:
          type: string
          enum: [product, custom, niche_trending, for_you]
          description: Where the Worker gets what to write about.
        topic_config:
          type: object
          description: The topic settings behind topic_source, for example the product it writes about or the custom topic text.
        style_instructions:
          type: string
          nullable: true
          description: Per-Worker writing instructions, applied after the account's own rules.
        use_account_rules:
          type: integer
          enum: [0, 1]
          description: 1 when the account's SuperX rules apply to this Worker as well.
        format_collection:
          type: string
          nullable: true
          description: The post-format collection the Worker writes to, when one is pinned.
        style_mode:
          type: string
          enum: [one_shot, reference_remix]
          description: one_shot writes fresh; reference_remix mirrors the shape of a real high-performing post.
        batch_size:
          type: integer
          description: Posts produced per run.
        output_mode:
          type: string
          enum: [suggest, auto_draft]
          description: suggest leaves the run in "To review"; auto_draft saves each post as a draft straight away.
        schedule:
          type: string
          enum: [manual, daily, weekly]
        schedule_time:
          type: string
          nullable: true
          description: "Run time as HH:mm in the Worker's timezone."
        schedule_day:
          type: integer
          nullable: true
          description: Day of week for a weekly Worker, 0 = Sunday.
        timezone:
          type: string
          nullable: true
        next_run_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true

    WorkerSuggestion:
      type: object
      description: One post a Worker wrote, waiting for a person to act on it.
      properties:
        id:
          type: integer
          description: Pass this to the draft, schedule and dismiss endpoints.
        worker_id:
          type: integer
          description: The Worker that wrote it; joins to `GET /v1/workers`.
        text:
          type: string
        status:
          type: string
          enum: [to_review, drafted, scheduled, dismissed]
          description: A dismissed suggestion reads as dismissed even if it was saved first.
        reference:
          type: object
          nullable: true
          description: What kind of source the Worker wrote from. The collection and reference ids stay private.
          properties:
            source:
              type: string
              enum: [collection, search]
              description: collection = a saved post-format collection, search = a high-performing post the run found. Null `reference` means the Worker wrote without one.
        generated_at:
          type: string
          format: date-time
          nullable: true
        drafted_at:
          type: string
          format: date-time
          nullable: true
        scheduled_at:
          type: string
          format: date-time
          nullable: true
          description: When it was saved as a scheduled post, not when it goes out.
        dismissed_at:
          type: string
          format: date-time
          nullable: true
        post_id:
          type: string
          nullable: true
          description: The post it was saved as, once drafted or scheduled. Joins to `GET /v1/scheduled-posts`.

    WorkerSuggestionDrafted:
      type: object
      properties:
        id:
          type: integer
        post_id:
          type: string
          description: The new draft; read or edit it at `/v1/scheduled-posts`.
        drafted_at:
          type: string
          format: date-time
          nullable: true

    WorkerSuggestionScheduled:
      type: object
      properties:
        id:
          type: integer
        post_id:
          type: string
          description: The new scheduled post; retime or cancel it at `/v1/scheduled-posts/{id}`.
        scheduled_at:
          type: string
          format: date-time
          nullable: true
          description: When the suggestion was queued.
        scheduled_for:
          type: string
          format: date-time
          nullable: true
          description: When the post goes out.

    WorkerSuggestionDismissed:
      type: object
      properties:
        id:
          type: integer
        dismissed_at:
          type: string
          format: date-time
          nullable: true

    ScheduledPost:
      type: object
      properties:
        id:
          type: string
        status:
          type: string
          enum: [draft, scheduled, sent, error]
        scheduled_for:
          type: string
          format: date-time
          nullable: true
          description: Null for drafts.
        parts:
          type: array
          items:
            type: object
            properties:
              text:
                type: string
              media:
                type: array
                description: Present only when the part carries media.
                items:
                  type: object
                  properties:
                    object_key:
                      type: string
                      description: Re-use this on PATCH to keep the attachment.
                    url:
                      type: string
                    file_type:
                      type: string
                    size:
                      type: integer
                      nullable: true
                    alt_text:
                      type: string
                      nullable: true
        title:
          type: string
          nullable: true
          description: Draft title shown in the SuperX app. Never posted.
        scratchpad:
          type: string
          nullable: true
          description: Private working notes. Never posted.
        tags:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              name:
                type: string
              color:
                type: string
        created_at:
          type: string
          format: date-time
          nullable: true
        auto_retweet:
          type: object
          nullable: true
          description: Applied auto-retweet setting; null when off.
          properties:
            after_hours:
              type: integer
              nullable: true
            remove_after_hours:
              type: integer
              nullable: true
        auto_delete:
          type: object
          nullable: true
          description: Applied auto-delete setting; null when off.
          properties:
            after_hours:
              type: integer
              nullable: true
            threshold:
              type: integer
              nullable: true
        auto_plug:
          type: object
          nullable: true
          description: Applied auto-plug setting; null when off.
          properties:
            template_id:
              type: string
              nullable: true
            threshold:
              type: integer
              nullable: true
        auto_dm:
          type: object
          nullable: true
          description: "Applied Auto DM setting; null when off. Read-only here, and the DM text is never returned, only that one is attached."
          properties:
            enabled:
              type: boolean
            max_dms:
              type: integer
              nullable: true
            batch_mode:
              type: boolean
            triggers:
              type: object
              properties:
                reply:
                  type: boolean
                retweet:
                  type: boolean
        super_followers_only:
          type: boolean
        x_post_id:
          type: string
          description: >-
            The X post id. Present whenever the post has an X id, which in
            practice means a post that went out; absent on drafts and on
            queued posts that have not been published yet.
        url:
          type: string
          description: >-
            Permalink to the post on X. Present exactly when `x_post_id` is.

    PlugTemplate:
      type: object
      properties:
        id:
          type: string
        text:
          type: string
        has_media:
          type: boolean
          description: True when the template replies with media attached.

    Context:
      type: object
      description: >-
        The account's Context settings - the background SuperX's AI uses when
        writing for the account. `style_guide.generated` is read-only.
      properties:
        profile_description:
          type: object
          properties:
            text: { type: string }
            enabled: { type: boolean }
        interests:
          type: array
          items: { type: string }
        rules:
          type: string
          description: Mandatory instructions applied on every AI surface.
        reply:
          type: object
          properties:
            custom_instructions: { type: string }
            include_author_name: { type: boolean }
        voice:
          type: object
          properties:
            favorite_creators:
              type: array
              items: { $ref: "#/components/schemas/FavoriteCreator" }
            use_own_posts_as_examples: { type: boolean }
        style_guide:
          type: object
          properties:
            audience_override:
              type: string
              nullable: true
              description: Manual override; outranks the generated guide until cleared.
            vocabulary_override:
              type: string
              nullable: true
            generated:
              type: object
              description: The app-generated style guide (read-only).
              properties:
                audience: { type: string, nullable: true }
                vocabulary: { type: string, nullable: true }
                generated_at: { type: string, format: date-time, nullable: true }
        products:
          type: array
          items: { $ref: "#/components/schemas/ContextProduct" }

    FavoriteCreator:
      type: object
      properties:
        screen_name:
          type: string
        id:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
        profile_image_url_https:
          type: string
          nullable: true

    ContextProduct:
      type: object
      properties:
        id:
          type: integer
        url:
          type: string
        name:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        positioning:
          type: string
          nullable: true
        features:
          type: string
          nullable: true
        updates:
          type: string
          nullable: true
        last_scraped_at:
          type: string
          format: date-time
          nullable: true
          description: When SuperX last refreshed the product's details from its url (read-only).
        created_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true

    AutoDm:
      type: object
      nullable: true
      required: [message]
      description: |
        Auto DM settings for one post. The DM goes to the people who reply to
        or repost the post once it is live, sent by the SuperX app's
        scheduler inside the account's DM limits.
      properties:
        message:
          type: string
          minLength: 1
          maxLength: 1000
          description: "The direct message. `[name]`, `[first]` and `[handle]` are filled in per recipient. Always required on a non-null `auto_dm`."
        triggers:
          type: object
          additionalProperties: false
          description: "Who gets the DM. At least one must be true; defaults to reply only. Any other key is rejected with `400 invalid_parameter`."
          properties:
            reply:
              type: boolean
              description: DM the people who reply to the post.
            repost:
              type: boolean
              description: DM the people who repost the post.
            retweet:
              type: boolean
              description: "Alias of `repost`, accepted so the value a post reads back (which reports `retweet`) can be sent straight back. Sending both with different values is a `400`."
        enabled:
          type: boolean
          default: true
          description: "`false` behaves exactly like sending `auto_dm: null`: the Auto DM is cleared and nothing is stored. Prefer `null`."
        max_dms:
          type: integer
          minimum: 1
          maximum: 100
          default: 100
          description: Most people to DM for this post. The plan's per-post cap still applies.
        batch_mode:
          type: boolean
          description: Send the DMs in one batch rather than as engagement arrives.

    DmCampaign:
      type: object
      description: The result of queueing a campaign. Counts, not deliveries.
      properties:
        id:
          type: string
          nullable: true
          description: "Campaign id. Use it with `GET`/`DELETE /v1/dm/campaigns/{id}` and as `campaign_id` on `GET /v1/dm/queue`. **null when nothing was queued** (every recipient skipped): no row carries an id, so there is none to hand back."
        queued:
          type: integer
          description: Messages placed in the queue by this call.
        queued_now:
          type: integer
          description: How many of those are due to go out in the current daily window.
        scheduled:
          type: array
          description: Per-day placement of the rest, when `spread` was true.
          items:
            type: object
            properties:
              day:
                type: string
                format: date
              count:
                type: integer
        scheduled_total:
          type: integer
        skipped:
          type: integer
          description: Recipients not queued (duplicates, the monthly cap, or the daily cap without `spread`).
        duplicates:
          type: integer
          description: Recipients skipped because this account messaged them in the last 24 hours, or because they are the sending account.
        remaining_this_month:
          type: integer
          description: Messages left in the monthly allowance after this campaign.

    DmQueueItem:
      type: object
      description: One message in the DM queue.
      properties:
        id: { type: string }
        campaign_id:
          type: string
          nullable: true
          description: null for messages queued in the SuperX app.
        recipient:
          type: object
          properties:
            x_user_id: { type: string, nullable: true }
            handle: { type: string, nullable: true }
            name: { type: string, nullable: true }
            avatar_url: { type: string, nullable: true }
        message:
          type: string
          nullable: true
          description: The message as it will be sent, with the tokens already filled in.
        template:
          type: string
          nullable: true
          description: The message before token substitution.
        status:
          type: string
          enum: [pending, sending, sent, failed, skipped]
        error: { type: string, nullable: true }
        created_at: { type: string, format: date-time, nullable: true }
        scheduled_at:
          type: string
          format: date-time
          nullable: true
          description: null means the message is in the current daily window rather than placed on a later day.
        sent_at: { type: string, format: date-time, nullable: true }

    DmLimits:
      type: object
      description: The account's DM allowances. Plan limits, not AI credits.
      properties:
        tier: { type: string, nullable: true }
        monthly:
          type: object
          properties:
            max: { type: integer }
            used: { type: integer }
            committed:
              type: integer
              description: Messages queued and not yet sent. They are held against the monthly allowance until they go out or are cancelled.
            remaining: { type: integer }
            reset_at: { type: string, format: date-time, nullable: true }
        daily_bulk:
          type: object
          properties:
            max: { type: integer }
            used: { type: integer }
            remaining: { type: integer }
        per_post_max:
          type: integer
          description: Most people one post's auto DM may message.
        posts_with_auto_dm:
          type: object
          description: How many scheduled posts a month may carry an auto DM.
          properties:
            max: { type: integer }
            used: { type: integer }
            remaining: { type: integer }
        scheduled_ahead:
          type: integer
          description: Unsent messages placed on a later day.
        scheduling_enabled:
          type: boolean
          description: Whether placing messages on later days is currently available.

    StyleGuideGenerated:
      type: object
      description: The style guide SuperX writes from the account's recent posts. Your own style_guide overrides outrank it.
      properties:
        audience:
          type: string
          nullable: true
          description: Who the account writes for.
        vocabulary:
          type: string
          nullable: true
          description: How the account writes, with how often each habit shows up.
        generated_at:
          type: string
          format: date-time
          nullable: true

    KeywordSuggestion:
      type: object
      properties:
        query:
          type: string
          description: A plain-language phrase to watch. Not an X search query; SuperX compiles it when the keyword is added to an agent.

    IcpRubric:
      type: object
      description: The structured qualification rubric a signal agent scores people against.
      properties:
        who:
          type: string
          description: One sentence describing exactly who the ideal customer is.
        needs:
          type: array
          items: { type: string }
          description: The needs or pains that make someone a fit.
        disqualifiers:
          type: array
          items: { type: string }
          description: What rules a profile out even when it looks adjacent.
        example_match:
          type: string
          description: A hypothetical X bio of someone who clearly fits.
        example_non_match:
          type: string
          description: A hypothetical X bio of someone who looks close but does not fit.

    Tag:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        color:
          type: string
          description: Palette color name rendered by the SuperX app (for example rose, amber, blue, violet).
        created_at:
          type: string
          format: date-time
          nullable: true

    QueueSlot:
      type: object
      required: [time, days]
      properties:
        time:
          type: string
          pattern: "^([01][0-9]|2[0-3]):[0-5][0-9]$"
          description: 24-hour local time in the account's queue timezone, `HH:MM`.
          example: "09:00"
        days:
          type: array
          minItems: 1
          items:
            type: integer
            minimum: 0
            maximum: 6
          description: Weekdays this slot runs on, 0 = Sunday. Sorted ascending on read; duplicates are deduped on write.
          example: [1, 3, 5]

    QueueSettings:
      type: object
      properties:
        timezone:
          type: string
          description: IANA timezone the slot times run in.
        timezone_is_default:
          type: boolean
          description: True when the account has never set a timezone and SuperX is using its default (UTC).
        slots:
          type: array
          items: { $ref: "#/components/schemas/QueueSlot" }
          description: The predefined posting slots, sorted by time.
        slots_are_default:
          type: boolean
          description: True when the account has never set a schedule and SuperX is using its default slots. A stored empty list is explicit and reports false.
        slot_count:
          type: integer
          description: Total number of time and day pairs across all slots.

    ReflowSummary:
      type: object
      description: What changing the slots did to the already-queued posts.
      properties:
        moved:
          type: integer
          description: Queued posts retimed onto the new slots.
        skipped:
          type: integer
          description: Posts that published or were deleted while the reflow ran; nothing was lost.
        bailed:
          type: boolean
          description: >-
            True when the settings were saved but the queue was deliberately
            left untouched (a post had nowhere to land, or the move set was
            too large). Retrying the same request is safe.

    Article:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        status:
          type: string
          enum: [draft, scheduled, publishing, published, failed]
        content_markdown:
          type: string
          description: The article body as markdown. Present on single-article responses only, never in lists.
        cover:
          type: object
          nullable: true
          properties:
            url:
              type: string
        scheduled_for:
          type: string
          format: date-time
          nullable: true
        published_at:
          type: string
          format: date-time
          nullable: true
        x_post_id:
          type: string
          nullable: true
          description: The X post id once published.
        created_at:
          type: string
          format: date-time
          nullable: true
          description: Null in list responses.
        updated_at:
          type: string
          format: date-time
          nullable: true

  responses:
    InvalidParameter:
      description: A parameter is missing, malformed, or out of range.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: invalid_parameter
              message: "since must be a UTC ISO-8601 timestamp"

    Unauthorized:
      description: Missing/malformed Authorization header (`unauthorized`) or an unknown/revoked key (`invalid_api_key`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: invalid_api_key
              message: "Unknown or revoked API key"

    SubscriptionRequired:
      description: |
        Subscription lapsed. NOTE the legacy body shape: `error` is a plain
        string here, not the `{ code, message }` object. Read-only keys on
        write endpoints instead get the object envelope with code
        `insufficient_scope`.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/LegacyErrorEnvelope"
              - $ref: "#/components/schemas/ErrorEnvelope"
          example:
            error: "subscription_required: The SuperX API requires an active subscription"

    WriteForbidden:
      description: Read-only key (`insufficient_scope`), an account shared with you (`writes_main_account_only`; shared accounts are read-only through the API, your own linked accounts are not), or a lapsed subscription (legacy string envelope).
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - $ref: "#/components/schemas/LegacyErrorEnvelope"
          example:
            error:
              code: insufficient_scope
              message: "This API key is read-only. Create a key with the write scope to use this endpoint."

    ContextWriteForbidden:
      description: >-
        Read-only key (`insufficient_scope`), an account shared with you with
        `editor` permission (`editor_restricted` - only the owner can change
        context settings; queue settings stay editable), or a lapsed
        subscription (legacy string envelope). Context writes are per-account
        and never return `writes_main_account_only`.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - $ref: "#/components/schemas/LegacyErrorEnvelope"
          example:
            error:
              code: editor_restricted
              message: "This account is shared with you with Editor permission. Only the account owner can change these settings."

    QueueWriteForbidden:
      description: >-
        Read-only key (`insufficient_scope`) or a lapsed subscription (legacy
        string envelope). Queue settings are a per-account write that any
        linked or shared account can use, Editor-permission shares included,
        so `writes_main_account_only` and `editor_restricted` never apply
        here.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - $ref: "#/components/schemas/LegacyErrorEnvelope"
          example:
            error:
              code: insufficient_scope
              message: "This API key is read-only. Create a key with the write scope to use this endpoint."

    ArticleNotFound:
      description: No article with that id belongs to this account.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: not_found
              message: "No article with that id belongs to this account."

    AccountNotFound:
      description: account_id is not one of your accounts.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: account_not_found
              message: "No account with that id belongs to this key"

    ContactNotFound:
      description: That id is not one of the account's known contacts (`contact_not_found`), or account_id is not one of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: contact_not_found
              message: "No contact with that id belongs to this account. Contacts are the account's engagers, contact-list members and signal leads."

    NoteNotFound:
      description: No note with that id belongs to this contact (`note_not_found`), or account_id is not one of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: note_not_found
              message: "No note with that id belongs to this contact."

    ListNotFound:
      description: No contact list with that id belongs to this account.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: list_not_found
              message: "No contact list with that id belongs to this account."

    ListOrUserNotFound:
      description: The list id is not one of your lists (`list_not_found`), or the handle did not match an X account (`user_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: user_not_found
              message: "No X account matches that handle."

    MemberNotFound:
      description: The list id is not one of your lists (`list_not_found`), or the member id is not in that list (`member_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: member_not_found
              message: "No member with that id belongs to this list."

    ProductNotFound:
      description: No product with that id belongs to this account (`not_found`), or account_id is not one of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: not_found
              message: "No product with that id belongs to this account."

    WorkerNotFound:
      description: The worker_id is not one of your Workers (`worker_not_found`), or account_id is not one of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: worker_not_found
              message: "No worker with that worker_id belongs to this account."

    SuggestionNotFound:
      description: No Worker suggestion with that id belongs to this account (`suggestion_not_found`), or account_id is not one of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: suggestion_not_found
              message: "No worker suggestion with that id belongs to this account."

    SuggestionNotActionable:
      description: >-
        The suggestion was already saved as a draft or a scheduled post
        (`already_saved`), it was dismissed (`already_dismissed`), or a
        parameter is missing or malformed (`invalid_parameter`, including a
        `scheduled_for` that is not UTC ISO-8601 or is less than 60 seconds
        away).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: already_saved
              message: "This suggestion was already saved as a post."

    AgentNotFound:
      description: The agent_id is not one of your signal agents (`agent_not_found`), or account_id is not one of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: agent_not_found
              message: "No signal agent with that agent_id belongs to this account."

    SystemListReadOnly:
      description: A write against a system list (`system_list_read_only`) - renaming or deleting one, or adding/removing its members, singly or in bulk - or a malformed body (`invalid_parameter`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: system_list_read_only
              message: "System lists are managed automatically and can't be edited."

    SystemListNotSupported:
      description: Member reads on a system list are not supported.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: system_list_not_supported
              message: "System list members can't be read through the API. Use a list you created instead."

    RateLimited:
      description: Rate limit exceeded (limits vary by plan). Honor Retry-After.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
        X-RateLimit-Limit: { $ref: "#/components/headers/X-RateLimit-Limit" }
        X-RateLimit-Remaining: { $ref: "#/components/headers/X-RateLimit-Remaining" }
        X-RateLimit-Reset: { $ref: "#/components/headers/X-RateLimit-Reset" }
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: rate_limited
              message: "Rate limit exceeded for the Pro plan (30 requests/min). Upgrade for higher limits."
              retry_after: 42

    FeedNotFound:
      description: >-
        The feed id is not one of your Engage feeds (`feed_not_found`), the
        feed points at a contact list that no longer exists
        (`list_not_found`), or account_id is not one of your accounts
        (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: feed_not_found
              message: "No Engage feed with that id belongs to this account."

    FeedSourceNotFound:
      description: >-
        The feed's source could not be resolved: the X list is private,
        deleted, or never existed (`x_list_not_found`), the `list_id` is not
        one of your contact lists (`list_not_found`), the feed id is not one
        of your feeds (`feed_not_found`, on update), or account_id is not one
        of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: x_list_not_found
              message: "That X list is private or unavailable. Only public lists can be used as a feed."

    FeedLimitReached:
      description: The account already holds the maximum number of saved Engage feeds (8).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: feed_limit_reached
              message: "This account already has the maximum number of saved Engage feeds. Delete one before adding another."

    AgentOrListNotFound:
      description: >-
        The agent id is not one of your signal agents (`agent_not_found`),
        the `destination_list_id` is not one of your usable contact lists
        (`list_not_found`), or account_id is not one of your accounts
        (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: list_not_found
              message: "No contact list with that id belongs to this account. List yours at /v1/contact-lists."

    SignalTargetNotFound:
      description: >-
        The agent id is not one of your signal agents (`agent_not_found`),
        the handle did not match an X account (`user_not_found`), or the X
        list is private or unavailable (`x_list_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: user_not_found
              message: "No X account matches that handle."

    SignalNotFound:
      description: The agent id is not one of your signal agents (`agent_not_found`), or the signal id does not belong to that agent (`signal_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: signal_not_found
              message: "No signal with that id belongs to this agent."

    DuplicateSignal:
      description: The agent already watches that target. An agent may hold each target once.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: duplicate_signal
              message: "This agent already watches that target. List the agent's signals at /v1/signals/agents."

    LeadNotFound:
      description: No lead with that id belongs to this account (`lead_not_found`), or account_id is not one of your accounts (`account_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: lead_not_found
              message: "No lead with that id belongs to this account. List leads at /v1/signals/leads."

    ArticleOrCoverStyleNotFound:
      description: No article with that id belongs to this account (`article_not_found`), or the `style_id` is not one of your saved cover styles (`cover_style_not_found`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: cover_style_not_found
              message: "No saved cover style with that id belongs to this account. List them at /v1/cover-styles."

    FeedRateLimited:
      description: >-
        The plan's feed-fetch allowance is spent, or feed fetches are busy
        across all API users right now. Honor Retry-After. No feed units are
        charged when the request is rejected; the request still consumed one
        unit of the ordinary read budget, and the X-RateLimit-* headers on
        this response describe that read bucket, not the feed bucket.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: rate_limited
              message: "Rate limit exceeded for the Pro plan (5 feed fetches/min). Upgrade for higher limits. A list feed that rotates its members costs 3 fetches. Retry after 27 seconds."
              retry_after: 27
              remaining_day: 41

    InternalError:
      description: Unexpected server error. May also use the legacy string envelope when subscription verification fails.
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - $ref: "#/components/schemas/LegacyErrorEnvelope"
          example:
            error:
              code: internal_error
              message: "Failed to fetch posts"

    UpstreamError:
      description: A dependent SuperX service returned an unexpected response.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: upstream_error
              message: "Failed to fetch scheduled posts. Try again shortly."

    UpstreamUnavailable:
      description: The scheduling service is temporarily unreachable. Safe to retry; reuse your Idempotency-Key on POST.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: upstream_unavailable
              message: "The scheduling service is temporarily unavailable. Retry with the same Idempotency-Key."

    AccountsUnavailable:
      description: Linked-account verification is temporarily unavailable (fail closed). The main account keeps working.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: accounts_unavailable
              message: "Account information is temporarily unavailable. Try again shortly."

    DatasetNotFound:
      description: No dataset with that id belongs to this key, or it has expired.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: dataset_not_found
              message: "No dataset with that id belongs to this key, or it has expired (datasets are kept for 30 days)"

    DatasetNotReady:
      description: |
        The dataset id is valid but the dataset is not `ready`. The error
        carries the current `dataset_status`: `collecting` finishes on its own
        (poll `GET /v1/datasets/{id}`), `failed` has to be rebuilt in the
        SuperX app.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/DatasetNotReadyError" }
          example:
            error:
              code: dataset_not_ready
              message: "This dataset is not ready yet. Check GET /v1/datasets/{id}: a collecting dataset finishes on its own, and a failed one has to be rebuilt in the SuperX app."
              dataset_status: collecting

    DatasetHasNoPeople:
      description: The dataset holds no people to add (`dataset_has_no_people`), a system list was targeted (`system_list_read_only`), or the body was malformed (`invalid_parameter`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: dataset_has_no_people
              message: "This dataset has no people to add: own-content datasets hold your posts, and rows without an X account id are skipped."

    PostNotFound:
      description: |
        No public post with that id: it is deleted, protected, or the id is
        wrong. The upstream batch read reports a missing post and a failed
        read identically, so an occasional transient failure surfaces here
        too - retry once before concluding the post is gone.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: post_not_found
              message: "No public post with that id. It may be deleted, protected, or the id may be wrong. A live lookup that fails upstream can also report this."

    UserNotFound:
      description: No public X account matches that handle (suspended, renamed, or misspelled).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: user_not_found
              message: "No public X account matches that handle. It may be suspended, renamed, or misspelled."

    LookupQuotaExceeded:
      description: |
        A live-lookup 429. Either the plan's per-minute or per-day enrichment
        bucket is exhausted (`rate_limited`), or a daily live-lookup gate
        stopped the call (`lookup_quota_exceeded`). Read `error.code` to tell
        those two apart; both carry `Retry-After`.

        `lookup_quota_exceeded` covers three cases and the body says which:
        your own allowance is used up (`limit` is present, and that allowance
        is shared with Ask SuperX in the app, resetting at midnight UTC); the
        SuperX-wide allowance is used up (no `limit`, since it is not your
        account's budget, still resetting at midnight); or the counter could
        not be verified, in which case the call is refused rather than run
        unmetered (no `limit`, short `retry_after`). Always read
        `error.retry_after` rather than assuming midnight.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - type: object
                properties:
                  error:
                    type: object
                    properties:
                      retry_after:
                        type: integer
                        description: Seconds to wait before retrying.
                      limit:
                        type: integer
                        description: |
                          Your daily live-lookup allowance. Present ONLY when
                          it is your own allowance that ran out; absent for a
                          SuperX-wide cap or an unverifiable counter.
                      reset_at:
                        type: integer
                        description: |
                          Unix seconds at the next UTC midnight. Present on
                          the two allowance cases, absent when the counter
                          could not be verified.
          example:
            error:
              code: lookup_quota_exceeded
              message: "Today's live X lookup allowance (300 lookups) is used up. It is shared with Ask SuperX in the app and resets at midnight UTC."
              retry_after: 20400
              limit: 300
              reset_at: 1789430400

    ProfileNotSynced:
      description: |
        The account's X profile has not synced into SuperX yet, so there is
        no handle to search mentions for. Open the account in the SuperX app
        once and retry.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: profile_not_synced
              message: "This account's X profile has not synced yet. Open it in the SuperX app once, then retry."

    CampaignNotFound:
      description: No DM campaign with that id belongs to this account, or it queued nothing in the first place, or every message it queued is gone.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: campaign_not_found
              message: "No DM campaign with that id belongs to this account."

    DmLimitReached:
      description: >-
        The account's DM allowance is used up (`dm_limit_reached`). `scope` is
        `month` or `day`; the monthly refusal also carries `reset_at`. These
        are plan limits, not AI credits.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: dm_limit_reached
              message: "This account's monthly DM allowance is used up."
              scope: month
              retry_after: 86400
              reset_at: "2026-10-01T00:00:00.000Z"

    AiActionLimited:
      description: |
        Rate limited (`rate_limited`, honor `Retry-After`), the AI credit pool
        is used up (`ai_credits_exhausted`, with the credit counters), or a
        daily cap on the action was hit (`ai_action_limited`).

        On `ai_action_limited`, `error.scope` says which ceiling. `account`
        is one of your own: the plan's daily cap for a metered action, the
        once-an-hour limit on style guide regeneration (where `reset_at` is an
        hour after the last run rather than midnight), the 3 live style-guide
        attempts a day, or the 20 page reads a day shared with the SuperX app.
        `platform` is the platform-wide fair-use ceiling on live-data actions,
        shared with every SuperX account: a `platform` refusal is not about
        this account's quota, so wait for `reset_at` and retry.

        The free endpoints send `X-Credits-Remaining` and `X-Credits-Reset` on
        this 429 as they do on a 200, since nothing was charged either way.
      headers:
        Retry-After:
          description: Seconds to wait before retrying. Sent on every flavour of this 429.
          schema:
            type: integer
        X-Credits-Remaining:
          description: Credits left in the pool. Sent only on `ai_credits_exhausted`.
          schema:
            type: integer
        X-Credits-Reset:
          description: Unix seconds when the pool refills. Sent only on `ai_credits_exhausted`.
          schema:
            type: integer
      content:
        application/json:
          schema:
            anyOf:
              - $ref: "#/components/schemas/CreditExhaustedError"
              - $ref: "#/components/schemas/ActionLimitedError"
              - $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            ai_action_limited_account:
              value:
                error:
                  code: ai_action_limited
                  message: "Daily limit reached for this action on your plan (10 lead searches per day). It resets at midnight UTC."
                  scope: account
                  retry_after: 20400
                  reset_at: 1789430400
            ai_action_limited_platform:
              value:
                error:
                  code: ai_action_limited
                  message: "The platform-wide fair-use ceiling for live-data actions has been reached for today. It resets at midnight UTC."
                  scope: platform
                  retry_after: 20400
                  reset_at: 1789430400
            ai_credits_exhausted:
              value:
                error:
                  code: ai_credits_exhausted
                  message: "Not enough AI credits. Resets in 5 hours."
                  credits_required: 2
                  credits_remaining: 0
                  reset_at: 1790812800

    CollectionInProgress:
      description: |
        Another background collection is still running for this account. Poll
        it with `GET /v1/datasets/{id}` and start the next one when it
        finishes.

        Nothing was started, so this does NOT use up one of the day's 10
        collections. The one enrichment unit spent establishing the size of
        the requested audience is not refunded.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorEnvelope" }
          example:
            error:
              code: collection_in_progress
              message: "Another audience collection is still running for this account. Poll it with GET /v1/datasets/{id} and start this one when it finishes."

    CollectionQuotaExceeded:
      description: |
        Today's audience collections are used up. The limit is 10 per day and
        is shared with the collections Ask SuperX runs in the app; existing
        datasets still read and export.
      headers:
        Retry-After:
          description: Seconds to wait before retrying.
          schema:
            type: integer
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/ErrorEnvelope"
              - type: object
                properties:
                  error:
                    type: object
                    properties:
                      retry_after:
                        type: integer
                        description: Seconds to wait before retrying.
                      limit:
                        type: integer
                        description: Collections allowed per day.
                      reset_at:
                        type: integer
                        description: Unix seconds at the next UTC midnight.
          example:
            error:
              code: collection_quota_exceeded
              message: "You have used today's audience collections. Existing datasets still read and export; the limit resets at midnight UTC."
              retry_after: 20400
              limit: 10
              reset_at: 1789430400
